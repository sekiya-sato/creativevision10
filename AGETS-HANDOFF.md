# CV10 エージェント引継ぎ規約

> 複数エージェント、Subagent、Reviewer、Tester を使う場合だけ適用する。技術・リスク規約は `AGENTS.md` を正典とする。

## 原則

- 最終責任は親・統合エージェントが持ち、返却結果を確認して統合する。
- 同じ作業を重複委任しない。同一ファイルは同時に 1 Writer とする。
- 調査は原則 Read-only。Subagent の再委任は、親が許可した独立作業だけに限る。
- `AGENTS.md` の高リスク作業では、実装担当と独立した Reviewer / Tester を置く。

## 委任

```text
[種別: 調査 / 実装 / レビュー / Tester]
目的:
モード: Read-only / Write
対象・所有ファイル:
編集禁止:
完了条件:
```

所有外ファイルの変更が必要なら、勝手に範囲を広げず親へ返す。

## 返却

```text
確認した事実:
実施内容・判定:
変更ファイル:
検証:
未確認事項・残余リスク:
```

事実と推測を分ける。競合、仕様判断、破壊・互換性リスク、完了条件未達が判明した場合も親へ返す。
