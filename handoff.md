# CV10 複数エージェント作業・引継ぎ規約

## 1. 目的と適用判断

本書は、同一の CV10 ワークスペースで複数エージェントが調査、実装、検証、レビューを分担するときの規約である。全体規約は `AGENTS.md` を優先する。

- **軽微な変更は単一エージェントで完結する。** 対象が明確な一つか少数のファイル・メソッドに限られ、DBスキーマ、データ意味論、公開API、認可、業務計算、定時処理、複数画面の動作を変えない変更が該当する。単一エージェントは自己検証、`git diff --check`、必要な対象ビルドを実施する。
- 軽微でない変更、または単一エージェントが調査中に影響範囲の不確実性を見つけた変更は、本書の分担フローへ切り替える。
- DB、公開API、共有基盤、複数レイヤー、複数画面、設計判断を含む作業は、原則として「調査・計画」「実装」「独立レビュー」を分ける。
- 新機能の設計から行う大規模案件、複数の専門領域、段階導入、移行を含む案件は、通常フローではなく [`handoff-expert.md`](handoff-expert.md) を明示して適用する。これは例外的な大規模案件専用であり、通常作業には使用しない。
- 並列化は速度のためではなく、読み取り調査の分散または変更範囲が重ならない場合にだけ行う。同じファイルを同時に編集するための並列化は禁止する。

## 2. 役割と権限

| 役割 | 主な責務 | 変更権限 |
| --- | --- | --- |
| 調整役 | 依頼の分解、担当・ファイル所有権の決定、承認確認、最終報告 | 原則なし。明示的に実装担当を兼ねる場合のみ |
| 調査・設計担当 | 現状、依存、影響、最小設計、検証条件の提示 | なし。調査メモのみ必要時に作成 |
| 実装担当 | 承認済みの範囲を実装し、自己検証と変更記録を行う | 割り当てられたファイルのみ |
| Tester | 設計された受入条件に基づき、業務経路・境界値・既存動作を実装担当と独立して検証する | 原則なし。テスト資産の変更は調整役の明示委任時のみ |
| レビュー担当 | 実差分と関連コードを独立して確認し、具体的な問題を報告する | なし。ただし調整役が修正を明示委任した場合を除く |

一人のエージェントが複数役割を担える。ただし、Tester必須の作業では実装担当をTesterおよび最終承認担当に兼任させない。設計変更を伴う作業ではレビュー時に実装時の前提を再検証し、自己承認だけで完了にしない。

### Tester を必須にする条件

次のいずれかに該当する変更では、Tester を実施する。

1. DBスキーマ、データ移行、派生テーブル、V*列、JSON列、集計・再集計を変更する。
2. 価格、税、在庫、売掛・買掛、日付判定など、結果が業務データへ影響する計算・登録経路を変更する。
3. gRPC・公開API・認可・外部サービス・印刷データ契約を変更する。
4. Schedulerなどの定時・バックグラウンド処理を変更する。
5. 新規画面、画面の主要導線、または複数ViewModelにまたがる入力・登録・表示を変更する。

Tester は、実装担当が通した単体テストだけを再実行する役割ではない。設計担当の受入条件を基に、正常系、境界値、異常系、既存データとの互換性、実際の画面・サービス経路を確認し、結果と未実施理由を引継ぎに残す。純粋な局所メソッド修正で既存テストが十分な場合は、調整役がTesterを省略できる。

## 3. 共有ワークスペースの安全規約

1. 調整役は開始時に `git status --short` を確認し、既存差分・未追跡ファイルを引継ぎに記載する。
2. 各実装担当には、編集可能なファイルまたはディレクトリを明示的に割り当てる。所有権の重複は認めない。
3. 他担当の差分、未追跡ファイル、生成物を削除・整形・移動・stash・reset しない。競合しそうな変更を見つけたら、編集を止めて調整役へ報告する。
4. 共通ファイル（`Doc/aicoding_log.md`、プロジェクトファイル、共有リソース、migration）は最後に一担当が統合する。ログは各担当が文案を引継ぎ、統合担当だけが書き込む。
5. 依頼外の変更、ビルド出力、ロックファイル、環境起因の失敗は、原因を区別して報告する。ソース変更で隠蔽しない。

## 4. 標準フロー

```text
軽微な変更: 実装担当 → 自己検証 → 最終確認・報告

通常・高リスク変更:
受付・既存差分確認
  → 調査・計画
  → （仕様変更ならユーザー承認待ち）
  → 実装・担当者検証
  → Tester と レビュー担当の独立確認（並行可）
  → 修正・再検証・再レビュー（必要時のみ）
  → 最終確認・報告
```

- 調査担当は、実コード、既存規約、関連テストを確認して計画を作る。推測だけでファイルや責務を決めない。
- 要件・DB・公開API・既存動作への実質的影響がある場合、調整役は実装前にユーザーの承認を得る。
- 実装担当は計画と現コードに矛盾を見つけたとき、軽微な実装調整を除いて設計変更要求を出し、勝手に拡張しない。
- Tester は、対象の受入条件、利用したデータ、確認した経路、結果を記録する。失敗時は再現条件を実装担当へ渡し、修正後に同じ条件で再確認する。
- レビュー担当は説明文だけでなく `git diff`、変更箇所、呼出元、必要に応じてテスト結果を確認する。

## 5. 引継ぎの最小形式

すべての引継ぎは日本語で、次の項目を短く具体的に記載する。該当しない項目は「なし」と明記する。

```text
[種別: 調査 / 実装 / Tester / レビュー / 設計変更要求]
対象: <依頼と担当範囲>
前提・既存差分: <確認した状態>
ファイル所有権: <編集可のファイル、または読取専用>
確認した事実: <コード・DB・契約・UIの根拠>
実施内容または提案: <最小変更と理由>
影響範囲: <レイヤー、業務、互換性>
検証: <実行済みコマンドと結果、未実施理由>
未決事項・リスク: <なし、または具体的内容>
次担当への依頼: <次の判断または作業>
```

実装引継ぎには、変更ファイル、意図的な設計差異、`git diff --check` の結果を必ず含める。Tester引継ぎには、受入条件ごとの結果、使用データ、未実施理由を含める。レビュー引継ぎには、判定と根拠のある指摘だけを含める。

## 6. 設計変更要求とレビュー判定

次に該当する場合は `設計変更要求` として調整役へ戻す: DBスキーマ、公開API、認可、データ意味論、V*列の扱い、複数画面の既存動作、依存方向を変える場合。

```text
[設計変更要求]
当初計画: <内容>
判明した事実: <コードまたは検証結果>
問題と影響: <なぜ現計画では不十分か>
代替案: <選択肢と影響>
推奨案: <理由>
ユーザー判断の要否: はい / いいえ
```

レビューの判定は次のいずれかとする。

- `承認`: 受入を妨げる問題なし。
- `要修正`: 正確性、回帰、データ破損、セキュリティ、ビルド・テスト失敗、規約違反のいずれかがある。問題ごとに対象ファイル・再現条件・必要な修正を示す。
- `提案のみ`: 現依頼には不要な改善。実装完了を妨げない。

好みだけのスタイル差、依頼外のリファクタリング、根拠のない懸念は `要修正` にしない。

## 7. CV10 固有の検証観点

- 層の依存方向、既存の gRPC 契約、WPF の View / ViewModel 境界を確認する。
- `V*`列を変更する場合は、Tran系の監査値とMaster系の現行名称を混同していないこと、`VRules` と `json_valid()` 防御を確認する。
- WPF は共有リソース、XAML、バインディング、コマンド状態を確認し、変更規模に応じて実行時表示も確認する。
- QFM は cp932 と SQL 別名対 `itemN` の対応を確認する。
- 検証は変更箇所から開始し、共有基盤・DB・公開APIでは対象プロジェクトのビルドと関連テストへ拡大する。実行不能な確認は省略せず理由を残す。

## 8. Git と完了条件

- コミット、rebase、merge、push は調整役がユーザーの明示指示を確認してから行う。担当者が個別判断で履歴操作をしない。
- コミット前は、担当範囲、`git status`、`git diff --check`、変更一覧、作業ログを調整役が再確認する。
- `Doc/aicoding_log.md` は共有ファイルとして統合担当が先頭へ追記する。競合時は双方の記録を保持する。
- 完了報告には、実装内容、変更・追加・削除ファイル、検証結果、レビュー判定、残余リスク、意図的に未実施の事項を含める。

## 9. モデル選定

モデルの利用可否は実行環境ごとに確認し、表示・選択できるモデルだけを使う。GPT-5.6系の公式分類は、Solが高難度向け、Terraが能力とコストの均衡、Lunaが低コスト・高頻度向けである。推奨は固定の品質保証ではなく、CV10での役割分担を効率よく行うための初期値とする。

| 作業・役割 | 推奨モデル | reasoning の初期値 | 運用上の制約 |
| --- | --- | --- | --- |
| 軽微変更の単一エージェント、通常の実装担当 | `gpt-5.6-terra` | `medium` | 範囲が局所であることを確認し、検証を省略しない |
| 調査・設計、重要業務のレビュー、複数層の実装 | `gpt-5.6-sol` | `high` | DB・業務計算・公開契約・設計変更では優先する |
| Tester、通常のレビュー、既存コードの横断調査 | `gpt-5.6-terra` | `medium` または `high` | 高リスクではSolへ引き上げ、受入条件に基づく独立確認を行う |
| ファイル棚卸し、定型的な読取・分類、テスト観点の収集 | `gpt-5.6-luna` | `low` または `medium` | 実行環境で利用可能な場合のみ。設計決定、実装、最終レビュー、最終Tester判定を単独で担当させない |

- 難しい設計・レビューでSolの品質向上が実測できる場合だけ、`xhigh` または `max` を検討する。常に最高設定にはしない。
- Lunaが利用できない環境では、同じ定型作業をTerraの`low`で実施する。
