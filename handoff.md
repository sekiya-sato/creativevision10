# CV10 引継ぎ・レビュー規約

## 1. 基本方針

- 通常の調査・実装・検証は、単一エージェントで完結してよい。
- 分業は品質上の必要がある場合だけ行い、速度や形式維持だけを理由に増やさない。
- 既存の未コミット差分・未追跡ファイルは他作業として扱い、削除・上書き・stash・resetしない。
- コミット、rebase、merge、pushはユーザーの明示指示がある場合だけ行う。
- DBスキーマ、公開API、業務ルール、既存動作を実質変更する場合は、実装前に変更内容・影響・検証方法を短く提示し、必要なユーザー判断を得る。

## 2. 作業区分

### A. 通常作業（単独完結）

対象が明確で、既存パターンに沿う修正・画面追加・文書更新・局所的な業務修正。

実施すること:

1. `git status --short` と対象コードを確認する。
2. 必要なリポジトリスキルを読む。
3. 最小差分で実装する。
4. 対象ビルド、関連テスト、`git diff --check` を実行する。
5. 実施内容・検証結果・残余リスクを報告する。

独立レビュー、Tester、引継ぎ文書は必須ではない。

### B. 注意作業（単独実装＋セルフレビュー）

次のいずれかを含むが、既存の設計・契約・パターンの範囲内で完結する変更。

- DB列・migrationの追加
- 既存gRPC契約の拡張
- 複数View/ViewModelの追加・変更
- 在庫、残、税、金額、日付判定などの業務計算変更
- QFM帳票のデータ契約変更

通常作業に加え、実装前後で次を確認する。

- 既存データ互換性と失敗時の影響
- 呼出元・保存経路・副作用
- 正常系、代表的な境界値、異常系
- 実行時確認ができない場合、その理由と未確認範囲

独立レビューは推奨だが、必須ではない。

### C. 高リスク作業（独立確認を必須化）

次のいずれかに該当する場合だけ、実装担当と別のレビューまたはTesterを置く。

- データ破損・大量更新・移行失敗が起こり得る変更
- 既存DBデータの変換・削除・意味変更
- 認可、外部連携、公開APIの互換性破壊
- 金額・税・在庫・請求の結果を広範囲に変える変更
- Scheduler、バックグラウンド処理、同時更新制御
- 新規設計で影響範囲または受入条件が不確実な変更

独立確認者は、実装説明だけでなく差分・関連コード・受入条件を確認し、結論を短く残す。

## 3. 最小引継ぎ形式

分業時だけ、以下を使用する。該当しない項目は「なし」とする。

```text
[種別: 調査 / 実装 / レビュー / Tester]
対象:
既存差分:
確認した事実:
実施内容または判定:
影響範囲:
検証:
未確認事項・リスク:
次の担当への依頼:
