# 旧CVnet比較 — 未適用・保留課題台帳（2026-08-17）

更新確認時HEAD: `899099d`（発注残完了設定・受注残完了設定を実装）

## 1. 位置づけ

本書は [2026-08-17_旧cvnet比較_仕様決定判断材料.md](2026-08-17_旧cvnet比較_仕様決定判断材料.md) の後継課題台帳である。
同資料は、旧CV.net資料との比較根拠・2026-08-17時点の決定経緯を残す**履歴資料**として変更せず保存する。

本書には、同資料で決定済みだが未適用のもの、判断保留、未解決の運用条件だけを記載する。
最新コミットまでに実装済みの G0〜G4、I1、棚卸開始・棚卸確定、発注残・受注残完了設定は本台帳の対象外とする。

## 2. 決定済み・未適用

| ID | 内容 | 現在地 | 次の実装範囲 |
|---|---|---|---|
| G0-4.3.1 | 完了済み発注・受注に紐付く仕入・出荷を編集した際のワーニング | `CompletionDb.FindCompleted()` と完了設定画面は実装済み | 仕入入力・出荷売上入力の保存完了時に警告を表示 |
| I2 / I3 | 配分確定・確定取消・出荷処理入力 | `ShippingDb` は実装済み | 商品別・得意先別の出荷確定画面、出荷処理入力画面から gRPC 経由で呼び出す |
| I7 | 未出荷の確定済み配分を検出し、欠品・完了を例外処理する経路 | 通常の出荷処理は `ShippingDb` にある | 滞留一覧、欠品数・完了FLGを操作する例外画面の仕様と実装 |
| I9 | 配分・引当・有効在庫の照会 | View / ViewModel は空実装、引当集計は実装済み | 倉庫×SKU照会、CSV、受払表への遷移を実装 |
| F2 | 在庫強制調整入力 | `Tran61Chosei` と棚卸確定経路は実装済み | 強制調整の入力・取消・実績照会を設計して画面実装 |
| H1-H4 | 納品予定日、納期遅れ照会 | 伝票単位の保持・判定方針は決定済み | 発注・受注の入力、残管理表、納品予定照会／表へ反映 |

## 3. 判断保留・未解決

| ID | 論点 | 判断または前提 |
|---|---|---|
| E10 | 請求書番号 | 採番、再発行・差し替え識別、請求書印刷を1.0に含めるかを決める |
| E11 | その他売上列 | 区分99を売上に含めたままにするか、請求一覧表用に分離するかを決める |
| E7 | 親子締日チェック | `Id_Paysaki` が全件0の現状では不要。親子請求を開始する前に導入要否を再判定 |
| E8-g | 月末以外の締日の消費税 | 最大3締日へ拡張する前に、課税対象期間と月次税額の扱いを決める |
| Mini-UAT | Phase単位の受入確認 | 実施者、実施時期、合格条件、記録方法が未定義。Release Gateに含める前に確定 |
| F0' | 倉庫別棚卸日 | 現状は棚卸年月末の帳簿在庫を凍結。倉庫ごとの棚卸日を使うなら日付別集計が必要 |
| I8 | 自動補充2方式 | 1.1以降候補。売上基準／在庫基準の採否と運用責任者を決める |
| H5 | MDマップ | 絵型・画像管理を伴うため1.1以降候補 |

## 4. 実施順の提案

1. I2 / I3 と I9 を画面接続し、配分→確定→出荷→引当解除の業務経路を閉じる。
2. G0-4.3.1 と I7 を追加し、完了済み伝票・滞留配分の例外運用を閉じる。
3. H1-H4 と F2 を実装し、納期・在庫調整の実務画面を閉じる。
4. E10 / E11 / E7 / E8-g を決定後、請求・支払計算へ着手する。

## 5. 確認根拠

- `899099d`: 発注残・受注残の手動完了／解除画面、`PartialUpdateParam` による `EndFlag` 更新、`TestServer` 89件・`TestLogin` 7件成功。
- `09a82eb`: 棚卸開始・棚卸確定画面。
- `c9a01e3`: `ShippingDb`、`CompletionDb`、`StocktakeDb` と在庫調整伝票。
- `74b5b2f`: 引当対象と数量算定の実装、配分照会3画面の枠。
