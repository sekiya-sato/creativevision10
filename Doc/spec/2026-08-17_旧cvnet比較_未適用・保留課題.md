# 旧CVnet比較 — 未適用・保留課題台帳（2026-08-17）

更新確認時HEAD: `c431d48`（支払台帳〔発行控え〕新設、突合ツール `tools/summaryreconcile` 新設。開発DBのテストデータで請求/支払計算の数値突合・冪等性・締日変更警告(E7)が成立。2026-08-20）

なお `73ec7a9` のメニュー整理は、いずれも未実装の空画面（準備中表示）の削除であり、本台帳の決定済み・保留の各論点への影響は無い。展示会スワッチ・絵型一覧は 1.1、C.P.A は 1.2以降の計画として継続し、実装時にメニューへ再追加する。

## 1. 位置づけ

本書は [2026-08-17_旧cvnet比較_仕様決定判断材料.md](2026-08-17_旧cvnet比較_仕様決定判断材料.md) の後継課題台帳である。
同資料は、旧CV.net資料との比較根拠・2026-08-17時点の決定経緯を残す**履歴資料**として変更せず保存する。

本書には、同資料で決定済みだが未適用のもの、判断保留、未解決の運用条件だけを記載する。
最新コミットまでに実装済みの G0〜G4、I1、棚卸開始・棚卸確定、発注残・受注残完了設定は本台帳の対象外とする。

## 2. 決定済み・未適用

| ID | 内容 | 現在地 | 次の実装範囲 |
|---|---|---|---|
| G0-4.3.1 | 完了済み発注・受注に紐付く仕入・出荷を編集した際のワーニング | **2026-08-18 実装済み**。`BaseTranInputViewModel.WarnIfLinkedZanCompletedAsync` を仕入入力・出荷売上入力の保存後に呼ぶ | 完了 |
| E10 | 請求書番号（2026-08-18 決定） | 未着手 | 得意先Id＋年月日(yyyyMMdd)＋連番2桁でユニークな請求書番号を採番する。再発行・差し替えは連番で識別する。請求計算・請求書印刷と一体で実装 |
| E11 | その他売上（2026-08-18 決定） | 区分99は現在 `Uriage` へ畳んでいる | 請求一覧表用に区分99（その他売上）を分離集計する。売上本体（`Tran00Uriage`）の畳み込みは維持し、請求一覧の算式でのみ切り出す |
| E7 | 親子締日チェック（2026-08-18 決定） | `Id_Paysaki` は現状全件0 | 親（請求先）と子（得意先）の締日が異なる場合は警告を出す。警告時は「マスタ変更および請求再計算が必要」である旨をメッセージ表示する。ブロックはしない |
| E8-g | 月末以外の締日の消費税（2026-08-18 決定、同日訂正） | 第1段階は1得意先1締日で月末のみ | 締日が3つになる（第2段階）とき、**請求データ（締日ベースの請求計算）を取引先との間の正**とする。月末一括で計算される消費税との差が出た場合は、**手動で相殺データを登録して金額調整**する。自動での税再計算は行わず、差分は相殺伝票で吸収する |
| I2 / I3 | 配分確定・確定取消・出荷処理入力 | **2026-08-18 実装済み**。`ShippingDb` を `Msg201_Op_Execute` へ接続し、出荷指示確定(商品/得意先)・出荷処理入力の3画面を実装 | 完了。残りは滞留・欠品の例外画面(I7)と帳票 |
| I7 | 未出荷の確定済み配分を検出し、欠品・完了を例外処理する経路 | **2026-08-18 実装**（`ShippingConfirmList` を滞留・欠品例外画面へ interactive 化＋**CSV出力**）。詳細は [2026-08-18_I7_滞留・欠品例外_詳細設計.md](archive/2026-08-18_I7_滞留・欠品例外_詳細設計.md) | 完了。専用PDF帳票(qfm)のみ見送り（目視確認が要るため。CSVで代替） |
| I9 | 配分・引当・有効在庫の照会 | **2026-08-18 実装済み**（3画面 L3）。倉庫×SKU照会・CSVは完了 | 残りは受払表への遷移（横計→倉庫別受払表 / SKU→品番別受払表）のみ。1.0では見送り可 |
| F2 | 在庫強制調整入力 | **2026-08-18 実装**（入力＝`BaseStockSheetInputViewModel<Tran61Chosei>`＋実績照会・取消＝`StockForceHistory`）。**2026-08-19 follow-up 完了**：調整理由区分(`MasterMeisho` Kubun=`CHR`、10入庫/20紛失/21盗難/22破損/23検品ミス/29その他、CalcFlag=10〜19が＋・20〜29が−)を新設し入力で必須選択、実績表PDF(`StockForceReport.qfm`)を新設 | 完了。残りはリードタイム等なし（区分の追加編集は `MasterMeishoMente` から可） |
| H1-H4 | 納品予定日、納期遅れ照会 | **2026-08-18 実装**（`Tran13Hachu`/`Tran12Jyuchu` に `NouhinDay` 追加＋発注/受注入力＋発注側納品予定照会、受注側照会は H4fu）。**2026-08-19 follow-up 完了**：発注側 帳票版納品予定表(`DeliveryScheduleTable.qfm`)を新設、発注/受注残管理表(qfm)へ納品予定日・納期遅れ列を追加 | 完了。残りはリードタイム自動計算(2.0対象外)のみ |

## 3. 判断保留・未解決

| ID | 論点 | 判断または前提 |
|---|---|---|
| Mini-UAT | Phase単位の受入確認 | 実施者、実施時期、合格条件、記録方法が未定義（D-09）。SQL/計算層の再現ツール `tools/summaryreconcile`（seed/show/idempotent/closingcheck）は整備済みで、請求/支払の数値突合・冪等性・締日変更警告(E7)は開発DBで成立確認済み。残りは帳票PDF目視と体制確定。Release Gateに含める前に確定 |
| F0' | 倉庫別棚卸日 | 現状は棚卸年月末の帳簿在庫を凍結。倉庫ごとの棚卸日を使うなら日付別集計が必要 |
| I8 | 自動補充2方式 | 1.1以降候補。売上基準／在庫基準の採否と運用責任者を決める |
| H5 | MDマップ | 絵型・画像管理を伴うため1.1以降候補。なお絵型一覧表の空画面は2026-08-19のメニュー整理で削除済み（実装時に再追加） |

## 4. 実施順の提案

1. ~~I2 / I3 と I9 を画面接続し、配分→確定→出荷→引当解除の業務経路を閉じる。~~ **2026-08-18 完了**（I9・I2/I3 とも実装済み）。
2. ~~G0-4.3.1 と I7 を追加し、完了済み伝票・滞留配分の例外運用を閉じる。~~ **2026-08-18 完了**（G0-4.3.1・I7 とも実装済み）。
3. ~~H1-H4 と F2 を実装し、納期・在庫調整の実務画面を閉じる。~~ **2026-08-18 本体完了 / 2026-08-19 follow-up 完了**（H1-H4：発注側納品予定表PDF・残管理表への納品予定日/納期遅れ列。F2：調整理由区分マスタ＋選択UI・実績表PDF。残るはリードタイム自動計算=2.0のみ）。
4. ~~E10 / E11 / E7 / E8-g は 2026-08-18 に決定済み。請求・支払計算（`SummaryUriSei` / `SummaryKaiShi` 生成）へ着手する。~~ **2026-08-19〜20 完了**：請求・支払計算、請求台帳・支払台帳〔発行控え〕、突合ツール `tools/summaryreconcile` まで実装。開発DBのテストデータで数値突合・冪等性(D-02/D-03)・締日変更警告(E7)が成立。**次はこれ**：帳票PDFの目視確認と D-09 Mini-UAT 体制の確定、実運用移行データの整備（移行仕様は別フェーズ）。

## 5. 確認根拠

- `899099d`: 発注残・受注残の手動完了／解除画面、`PartialUpdateParam` による `EndFlag` 更新、`TestServer` 89件・`TestLogin` 7件成功。
- `09a82eb`: 棚卸開始・棚卸確定画面。
- `c9a01e3`: `ShippingDb`、`CompletionDb`、`StocktakeDb` と在庫調整伝票。
- `74b5b2f`: 引当対象と数量算定の実装、配分照会3画面の枠。
