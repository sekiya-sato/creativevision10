# H4 follow-up 受注側 納品予定照会 詳細設計（2026-08-18）

対象HEAD: `80c6ce6`（強制調整の実績照会・取消）
対象: 受注側の納品予定照会（`NouhinYoteiTable` を on-screen 照会として実装）＋発注側メニュー説明の修正
根拠: 未適用課題台帳 H1-H4 の follow-up、H1-H4 本体（`Tran12Jyuchu.NouhinDay` 追加済み）。

## 1. 目的とスコープ

H1-H4 本体で発注・受注ヘッダへ `NouhinDay` を追加し、**発注側**の納品予定照会（`DeliveryScheduleInquiry`）を実装した。
本 follow-up は**受注側**の納品予定照会を、発注側と同型で実装する（出荷予定・納期遅れの得意先視点）。

- **受注側納品予定照会**: 受注(`Tran12Jyuchu`)を `NouhinDay` で並べ、納期遅れ（予定日超過かつ未完了）を表示する。
- あわせて発注側 `DeliveryScheduleInquiry` のメニュー説明が「準備中」のままだったので実機能へ更新する。

**follow-up のまま残す（本作業外）**: 帳票版の納品予定表（発注側 `DeliveryScheduleTable` / 受注側PDF）、
発注残・受注残管理表への納期遅れ列（いずれも qfm レイアウトが必要）。

## 2. 実装方針（発注側のミラー）

`DeliveryScheduleInquiryViewModel`（発注側、`Tran13Hachu`/仕入先）をそのまま鏡写しにする。

- **実装**: `ViewModels/04Juchu/NouhinYoteiTableViewModel.cs` を空クラス→`BaseQueryViewModel` 派生へ。
  対象を `Tran12Jyuchu`、取引先を得意先(`Id_Tokui`/`VTokui`/`MasterTokui`)に置換。
  - 検索: 納品予定日 from-to、得意先、「未完了のみ」「納期遅れのみ」。
  - 一覧行: 受注No、受注日、納品予定日、得意先、受注数(`SuTotal`)、金額、状態(`EndFlag`)、納期遅れ日数。
  - 納期遅れ = `NouhinDay` 非空 かつ `NouhinDay < 今日` かつ `EndFlag=0`。
- **View**: `Views/04Juchu/NouhinYoteiTableView.xaml` を発注側 View のミラーで実装。
- **メニュー**: 受注側「納品予定表」を「納品予定照会(受注)」へ改名し addInfo 更新。発注側「納品予定照会」の「準備中」を外す。

サーバ・スキーマ変更なし（read-only 照会、列は追加済み）。

## 3. ファイル一覧（新規・修正）

| 種別 | パス | 内容 |
|---|---|---|
| 修正 | `ViewModels/04Juchu/NouhinYoteiTableViewModel.cs` | 空→受注側納品予定照会 |
| 修正 | `Views/04Juchu/NouhinYoteiTableView.xaml`(+`.cs`) | 照会画面 |
| 修正 | `Models/MenuData.cs` | 受注側改名・addInfo、発注側 addInfo の準備中除去 |

## 4. 受入確認（Given/When/Then）

- G/W/T-1: 受注入力で納品予定日を入れて保存した受注が、受注側納品予定照会に納品予定日つきで並ぶ。
- G/W/T-2: 納品予定日を過ぎた未完了(`EndFlag=0`)の受注 → 納期遅れ（遅延日数>0）で色分け表示。完了は遅延に出ない。
- G/W/T-3: 得意先・納品予定日範囲・「納期遅れのみ」で絞れる。

## 5. 検証方針

- `dotnet build creativevision10.slnx`（0 warnings / 0 errors）。サーバ変更なしのため `TestServer`/`TestLogin` 件数維持。

## 6. スコープ外（follow-up 継続）

帳票版納品予定表（qfm）、残管理表への納期遅れ列（qfm）、リードタイム自動計算（2.0）。
