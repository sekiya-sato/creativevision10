# CV10 機能完成度チェックリスト（マスタ〜発注〜掛〜請求〜棚卸）

調査日: 2026-08-12 / 更新: 2026-08-17 / 対象: `C:\gitroot\new2022\cv10`（更新確認時HEAD **`899099d`**、前回確認 `9833f24`）

> 2026-08-17 時点の状況: 2026-08-16 に掛集計（区分別内訳・`IsPay` 絞り込み・後続月の繰越再計算）と
> `KakeDay` 改名・元帳の `*` 印字を実装した（`9833f24`）。
> さらに 2026-08-17 に**旧CVnet資料との突き合わせで Phase 1〜3 の業務仕様がほぼ確定し**、
> 発注・受注の完了フラグと配分の欠品数を追加した。決定内容は
> [2026-08-17_旧cvnet比較_仕様決定判断材料.md](2026-08-17_旧cvnet比較_仕様決定判断材料.md) を正とする。
> その後、棚卸開始・棚卸確定処理、引当集計、発注残・受注残完了設定までを実装した。
> 旧比較資料は履歴として残し、未適用・判断保留・未解決の後続課題は
> [2026-08-17_旧cvnet比較_未適用・保留課題.md](2026-08-17_旧cvnet比較_未適用・保留課題.md) で管理する。

---

## 1. 判定基準と判定方法

| Level | 定義 | 本調査での判定根拠 |
|---|---|---|
| L0 | 未実装 | ViewModel が `BaseViewModel` を継承した空クラス、かつ View が `<Grid />` のみ（XAML 17行） |
| L1 | 画面表示可能 | View に UI があるが ViewModel にデータ取得が無い |
| L2 | CRUD可能 | 伝票/マスタの登録・修正・削除が成立する。ただし後続の在庫・掛へ波及しない |
| L3 | 業務ロジック接続済み | 集計SQL・帳票・照会が業務的に意味のある値を返す |
| L4 | 在庫・掛・集計まで整合 | 登録/修正/削除が `SummaryRealStock` / `SummaryStock` / `Summary*Kake` に反映される |
| L5 | E2Eテスト済み | 自動テストで業務シナリオが検証されている |

判定に使った実装上の事実:

- 在庫反映は汎用CRUDハンドラ側で自動実行される。`CvServer/Services/HandlerClass.cs` の
  Insert / BulkInsert / Update / Delete / DeleteById が、対象型が `ITranSoko` を実装していれば
  `SummaryDb.CalcTran2SummaryStock()` を呼ぶ。更新時は「旧値を反転して打ち消す→新値を加算」の2回呼び。
- `ITranSoko` を実装しているのは `Tran00Uriage` / `Tran01Tenuri` / `Tran03Shiire` /
  `Tran05Ido` / `Tran10IdoOut` / `Tran11IdoIn` の6テーブルのみ（`CvBase/BaseDb2Trans.cs`）。
- 取引区分ごとの在庫増減方向は `TranCalcBase.GetCalcSoko()` / `GetCalcIdosaki()` に集約済み
  （在庫・入庫・出庫・移動中の4フラグ）。
- 当初調査では「L4 かどうか」を **ViewModel が上記6テーブルのどれを保存するか** で判定した。
  `5399328` で、`CalcTran2SummaryStock()` は `idSoko == "Id_Ido"` の場合に
  `GetCalcIdosaki()` を使用するよう修正された。本P0ではさらに、`SummaryStock` の更新判定を4フラグ全体へ広げ、
  `TransitQty` だけを更新する積送出庫の移動先も反映した。Rebuildは対象範囲の削除・6伝票再生成・
  `SummaryRealStock` 再構築を単一トランザクションにし、再実行、消滅キー、前月値復帰をテスト済み。
  ただし元の L4 定義は「在庫・掛・集計まで整合」であり、掛を即時反映しない売上・仕入も含め、
  以下の L4=8 は**分類基準の確定前の暫定値**とする。

薄い ViewModel = 未実装ではない点に注意。`IdoInputSokuViewModel` は11行だが基底
`BaseIdoInputViewModel<T>`（503行）が実装本体であり、View は599行ある。逆に
`DeliveryScheduleInquiryViewModel` は39行あるがすべてコメントで、実体は空クラスである。

---

## 2. サマリ

対象は 1.0 必須範囲の 8 フォルダ、**130 画面**（View と ViewModel は 1:1 で対応、欠落なし）。

| 領域 | 画面数 | L0 | L2 | L3 | L4 | L5 |
|---|---:|---:|---:|---:|---:|---:|
| 01Master マスタ | 17 | 2 | 5 | 10 | 0 | 0 |
| 03Hatchu 発注 | 11 | 2 | 1 | 8 | 0 | 0 |
| 04Juchu 受注 | 14 | 5 | 0 | 9 | 0 | 0 |
| 05Shiire 仕入・買掛・支払 | 13 | 0 | 1 | 10 | 2 | 0 |
| 06Uriage 売上・売掛・入金・請求 | 17 | 1 | 2 | 12 | 2 | 0 |
| 07Haibun 配分・引当・出荷 | 21 | 20 | 1 | 0 | 0 | 0 |
| 08Zaiko 在庫・移動・棚卸 | 20 | 1 | 2 | 13 | 4 | 0 |
| 31Monthly 月次・締・原価 | 18 | 15 | 0 | 3 | 0 | 0 |
| **計** | **131** | **46** | **12** | **65** | **8（暫定）** | **0** |

読み方:

- **画面としては 85/131（65%）が動く**。帳票・照会系の作り込みは厚い。
- **L4 候補は 8 画面**（仕入・返品・出荷売上・店舗売上・移動4種）。在庫P0の不変条件は検証済みだが、
  売上・仕入の掛をL4必須条件とするか、掛が非該当の移動画面をどう扱うかの分類基準が未確定。
- **掛・請求・原価まで整合する画面は 0**。
- **L5 は 0**。自動テストは計70メソッドあり（2026-08-16 時点）、内訳は `MasterCascadeDbTests` 23 /
  `SummaryDbTests` 18 / `WriteEffectRunnerTests` 12 / `TestServer` 10 / `TestLogin` 7。
  在庫・引当の不変条件と更新の副作用手順はテストしたが、
  画面からサーバーまでの業務フローを通す E2E は依然として存在しない。

---

## 3. 領域別チェックリスト

凡例: CRUD ○=可 / 在庫=在庫集計への反映 / 掛=売掛買掛への反映

### 3.1 01Master マスタ（17画面）

| 画面 | View / ViewModel | 基底・対象 | CRUD | 在庫 | 掛 | Lv | 残課題 |
|---|---|---|:-:|:-:|:-:|:-:|---|
| 名称マスタ | `MasterMeishoMente` | `BaseCodeNameLightMente<MasterMeisho>` | ○ | – | – | L3 | V*列伝播はテスト済（23件） |
| 商品マスタ | `MasterShohinMente` | `<MasterShohin>` | ○ | – | – | L3 | SKU(色サイズ)は `DerivedShohinColSiz` 経由 |
| 得意先/店舗/倉庫 | `MasterTokuiMente` | `<MasterTokui>` | ○ | – | – | L3 | 倉庫は `TenType` で区別。請求先親子は未検証 |
| 仕入先マスタ | `MasterShiireMente` | `<MasterShiire>` | ○ | – | – | L3 | リードタイム列が無い（納品予定の前提） |
| 社員マスタ | `MasterShainMente` | `<MasterShain>` | ○ | – | – | L3 | |
| 環境設定 | `MasterConfigMente` | `BaseMente<MasterConfig>` | ○ | – | – | L2 | |
| システム管理 | `MasterSysKanriMente` | `BaseMente<MasterSysman>` | ○ | – | – | L2 | 締日・税率の格納先として要確認 |
| 上代一括変更 | `MasterJouDaiBulkChange` | `DerivedJodai` 生成 | ○ | – | – | L3 | 1187行。在庫評価額に効く |
| 得意先販促 | `TranTokuiPromotionMente` | `BaseMente<TranTokuiPromotion>` | ○ | – | – | L2 | |
| 店舗販促 | `TranShopPromotionMente` | `BaseMente<TranShopPromotion>` | ○ | – | – | L2 | |
| エンド顧客 | `MasterEndCustomerMente` | `<MasterEndCustomer>` | ○ | – | – | L2 | LCV領域。1.0優先度低 |
| バーコード印刷 | `MasterPrintBarcode` | `BaseMente<MasterShohin>` | – | – | – | L3 | |
| 社員証印刷 | `PrintMasterShainCard` | `BaseMente<MasterShain>` | – | – | – | L3 | |
| 外部CSV取込 | `ExternalCsvImport` | 714行 | ○ | – | – | L3 | CSV入力の中核 |
| 取込テンプレート | `ImportTemplateCreate` | 539行 | ○ | – | – | L3 | |
| **原価変更** | `GenkaChangeEntry` | 空クラス | – | – | – | **L0** | Phase 5「原価更新」と一体で設計要 |
| **商品格付変更** | `ProductRatingChange` | 空クラス | – | – | – | **L0** | |

### 3.2 03Hatchu 発注（12画面）

| 画面 | ViewModel | 基底・対象 | CRUD | 在庫 | 掛 | Lv | 残課題 |
|---|---|---|:-:|:-:|:-:|:-:|---|
| 発注入力 | `HachuInput` | `BaseTranInput<Tran13Hachu>` | ○ | – | – | L3 | 発注は在庫を動かさない（正） |
| 発注書 | `HachuForm` | `BaseReport` | – | – | – | L3 | |
| 発注残管理表 | `HachuZanKanriTable` | `BaseReport` | – | – | – | L3 | 残は `Tran03Shiire.RelateNo1` 突合で都度算出 |
| 仕入未受リスト | `PendingShiireList` | `BaseReport` | – | – | – | L3 | 同上の規約に依存 |
| 商品別発注表 | `ShohinHachuTable` | `BaseReport` | – | – | – | L3 | |
| 商品別発注集計表 | `ShohinHachuSummaryTable` | `BaseReport` | – | – | – | L3 | |
| 仕入先別発注表 | `SupplierHachuTable` | `BaseReport` | – | – | – | L3 | |
| **納品予定照会** | `DeliveryScheduleInquiry` | 空クラス | – | – | – | **L0** | 納品予定日を持つ列が無い（VMに調査記録あり） |
| **納品予定表** | `DeliveryScheduleTable` | 空クラス | – | – | – | **L0** | 同上 |
| 発注配分入力 | `HachuHaibunInput` | 発注Id単位で `TranHaibun` を作成・修正 | ○ | – | – | L2 | 初回配分（`Kubun=0`）として `RelateNo1` に発注Idを保存。配分確定→移動/出荷伝票生成は未実装 |
| 発注残完了設定 | `HachuZanCompletionSetting` | `BaseZanCompletionViewModel<Tran13Hachu>` | ○ | – | – | **L3** | 手動完了・解除は可能。完了済み伝票の編集時ワーニングは未実装 |

### 3.3 04Juchu 受注（15画面）

| 画面 | ViewModel | 基底・対象 | CRUD | Lv | 残課題 |
|---|---|---|:-:|:-:|---|
| 受注入力 | `JuchuInput` | `BaseTranInput<Tran12Jyuchu>` | ○ | L3 | |
| 受注残管理表 | `JuchuZanKanriTable` | `BaseReport` | – | L3 | 「残」は出荷実績との差のみ。引当数（`SummaryRealStock.ReserveQty`、2026-08-15 追加）は未利用 |
| 受注ベスト表 | `JuchuBestTable` | `BaseReport` | – | L3 | |
| 商品別受注表 | `ShouhinJuchuTable` | `BaseReport` | – | L3 | |
| 商品別受注集計表 | `ShouhinJuchuSummaryTable` | `BaseReport` | – | L3 | |
| 得意先別受注表 | `TokuiSakiJuchuTable` | `BaseReport` | – | L3 | |
| 得意先別売上予定表 | `TokuiSakiUriageYoteiTable` | `BaseReport` | – | L3 | |
| 担当別展示受注合計表 | `TantoTenjiJuchuGoukeiTable` | `BaseReport` | – | L3 | |
| **デザインリスト** | `DesignListTable` | 空クラス | – | **L0** | |
| **納品予定表** | `NouhinYoteiTable` | 空クラス | – | **L0** | 納品予定日列が無い |
| 受注残完了設定 | `JuchuZanCompletionSetting` | `BaseZanCompletionViewModel<Tran12Jyuchu>` | ○ | **L3** | 手動完了・解除は可能。完了済み伝票の編集時ワーニングは未実装 |
| **スワッチ作成** | `SwatchDataCreate` | 空クラス | – | **L0** | 1.0優先度低 |
| **スワッチ一括作成** | `SwatchDataBulkCreate` | 空クラス | – | **L0** | 同上 |
| **展示スワッチ** | `TenjiSwatch` | 空クラス | – | **L0** | 同上 |

### 3.4 05Shiire 仕入・買掛・支払（13画面、L0なし）

| 画面 | ViewModel | 基底・対象 | CRUD | 在庫 | 掛 | Lv | 残課題 |
|---|---|---|:-:|:-:|:-:|:-:|---|
| 仕入入力 | `ShiireInput` | `BaseTranInput<Tran03Shiire>` | ○ | ○ | × | **L4** | 買掛は保存時に更新されない |
| 返品入力 | `HenpinInput` | `BaseStockSheetInput<Tran03Shiire>` | ○ | ○ | × | **L4** | `CalcFlag=-1` で在庫減 |
| 支払入力 | `ShiharaiInput` | `BaseKinInput<Tran07Shiharai>` | ○ | – | × | L2 | 買掛残への即時反映なし |
| 支払消込 | `ShiharaiMatching` | `BaseMatching<Tran03Shiire,Tran07Shiharai>` | ○ | – | × | **L3** | 2026-08-14 に `Tran03Shiire.EndFlag` へ保存する方式へ差し替え。買掛残高へは意図的に非連動。実データが仕入2件/支払0件のため実運用検証は未 |
| 買掛残高表 | `KaikakeBalanceReport` | `SummaryKaiKake` 参照 | – | – | – | L3 | 再更新を回すまで値が出ない |
| 支払残高明細 | `ShiharaiBalanceDetail` | `BaseReport` | – | – | – | L3 | |
| 月別支払予定表 | `MonthlyShiharaiYoteiTable` | `BaseReport` | – | – | – | L3 | |
| 支払一覧表 | `ShiharaiListReport` | `SummaryKaiShi` 参照 | – | – | – | L3 | **`SummaryKaiShi` の生成処理が存在せず 0件** |
| 仕入先元帳 | `ShiireLedger` | Tran直読み | – | – | – | L3 | |
| 仕入伝票 | `ShiireSlipPrint` | `BaseReport` | – | – | – | L3 | |
| 仕入推移表 | `ShiireTrendReport` | `BaseReport` | – | – | – | L3 | |
| 品番別仕入チェックリスト | `HinbanShiireCheckList` | `BaseReport` | – | – | – | L3 | |
| ブランド別仕入金額表 | `BrandShiireKingakuTable` | `BaseReport` | – | – | – | L3 | |

### 3.5 06Uriage 売上・売掛・入金・請求（17画面）

| 画面 | ViewModel | 基底・対象 | CRUD | 在庫 | 掛 | Lv | 残課題 |
|---|---|---|:-:|:-:|:-:|:-:|---|
| 出荷売上入力 | `ShukkaUriageInput` | `BaseTranInput<Tran00Uriage>` | ○ | ○ | × | **L4** | `KakeDay`（掛計上日）列は分離済み |
| 店舗売上入力 | `ShopUriageInput` | `BaseTranInput<Tran01Tenuri>` | ○ | ○ | × | **L4** | |
| 入金入力 | `NyukinInput` | `BaseKinInput<Tran06Nyukin>` | ○ | – | × | L2 | 売掛残への即時反映なし |
| 入金消込 | `NyukinMatching` | `BaseMatching<Tran00Uriage,Tran06Nyukin>` | ○ | – | × | **L3** | 2026-08-14 に `Tran00Uriage.EndFlag` へ保存する方式へ差し替え。売掛残高へは意図的に非連動 |
| 売掛残高表 | `UrikakeBalanceReport` | `SummaryUriKake` 参照 | – | – | – | L3 | 再更新前提 |
| 請求残高明細 | `SeikyuBalanceDetail` | `SummaryUriSei` 参照 | – | – | – | L3 | **`SummaryUriSei` 未生成** |
| 請求一覧表 | `SeikyuListReport` | `SummaryUriSei` 参照 | – | – | – | L2 | **常に空。請求計算が無いため** |
| 得意先元帳 | `TokuiLedger` | Tran直読み | – | – | – | L3 | 集計に依存しないので値は出る |
| 月別入金予定表 | `MonthlyNyukinYoteiTable` | `BaseReport` | – | – | – | L3 | |
| 納品書 | `NouhinBookPrint` | `BaseReport` | – | – | – | L3 | |
| 納品書(カスタム) | `NouhinBookPrintCustom` | 上記継承 | – | – | – | L3 | |
| 納品書未発行チェック | `NouhinBookPendingCheckList` | `BaseReport` | – | – | – | L3 | |
| 売上チェックリスト | `UriageCheckList` | `BaseReport` | – | – | – | L3 | |
| 品番別売上チェック | `HinbanUriageCheckList` | `BaseReport` | – | – | – | L3 | |
| 現金種別売上表 | `UriageCashTypeReport` | `BaseQuery` | – | – | – | L3 | |
| 得意先推移表 | `TokuiTrendReport` | `BaseReport` | – | – | – | L3 | |
| **POS日次精算入力** | `PosDailySeisanInput` | 空クラス | – | – | – | **L0** | CVPOS互換は1.0優先度低 |

### 3.6 07Haibun 配分・引当・出荷（21画面 / うち20画面がL0）

| 画面 | ViewModel | Lv | 残課題 |
|---|---|:-:|---|
| 店舗配分入力 | `ShopHaibunInput`（885行） | L2 | `TranHaibun` を洗い替え保存する。後続ロジックは実装済みだが、配分確定・出荷処理画面は未実装 |
| 配分問合わせ | `HaibunQuery` | **L3** | 2026-08-18 実装。倉庫×色サイズで配分数(`TranHaibun` `EndFlag=0`)を展開・CSV出力 |
| 引当問合わせ | `HikiateQuery` | **L3** | 2026-08-18 実装。`SummaryRealStock.ReserveQty` を倉庫×色サイズで展開・CSV出力 |
| 有効在庫問合わせ | `YukoZaikoQuery` | **L3** | 2026-08-18 実装。有効在庫(`Su-ReserveQty`)を倉庫×色サイズで展開・CSV出力 |
| 受注配分入力 | `JuchuHaibunInput` | **L0** | |
| 得意先配分入力 | `TokuiHaibunInput` | **L0** | |
| 在庫品配分 | `ZaikoHinHaibun` | **L0** | |
| 配分メンテ | `HaibunMente` | **L0** | |
| 配分データメンテ | `HaibunDataMente` | **L0** | |
| 取置入力 | `ReservationInput` | **L0** | `TranHaibun` に配分・取置の部分設計あり。引当の範囲と有効在庫算式は未決定（下記4章参照） |
| 出荷処理入力 | `ShippingInput` | **L0** | |
| 出荷確定リスト | `ShippingConfirmList` | **L0** | |
| 出荷確定(商品別) | `ShippingConfirmShohin` | **L0** | |
| 出荷確定(得意先別) | `ShippingConfirmTokui` | **L0** | |
| 出荷確定明細書 | `ShippingConfirmDetailPrint` | **L0** | |
| 出荷リスト | `ShippingListReport` | **L0** | |
| 店舗出荷依頼 | `ShopShippingRequest` | **L0** | |
| 移動指示(商品別) | `IdoInstructionShohin` | **L0** | |
| 移動指示(SKU別) | `IdoInstructionSku` | **L0** | |
| 在庫自動補充メンテ | `ZaikoAutoHojunMente` | **L0** | |
| 自動発注補充除外設定 | `AutoHachuHojunExcludeSetting` | **L0** | |

### 3.7 08Zaiko 在庫・移動・棚卸（20画面）

| 画面 | ViewModel | 基底・対象 | CRUD | 在庫 | Lv | 残課題 |
|---|---|---|:-:|:-:|:-:|---|
| 移動入力(即時) | `IdoInputSoku` | `BaseIdoInput<Tran05Ido>` | ○ | ○ | **L4** | 移動元 -N / 移動先 +N |
| 移動出庫(積送) | `IdoInputOut` | `BaseIdoInput<Tran10IdoOut>` | ○ | ○ | **L4** | 移動元 -N / 積送中 +N |
| 移動受 | `IdoInputUke` | `BaseIdoInput<Tran11IdoIn>` | ○ | ○ | **L4** | 積送中 -N / 移動先 +N |
| 移動入力(棚) | `StockIdoInput` | `BaseStockSheetInput<Tran05Ido>` | ○ | ○ | **L4** | |
| 棚卸入力 | `StockInput` | `BasePlainLightMente<Tran60Tana>` | ○ | × | L2 | `Tran60Tana` は `ITranSoko` 非実装＝在庫に一切影響しない |
| 棚卸入力(一覧) | `StockInputList` | `BaseStockSheetInput<Tran60Tana>` | ○ | × | L2 | 同上 |
| 在庫照会 | `ZaikoQuery` | 651行 | – | – | L3 | 積送中は `SummaryStock.TransitQty` の月次積上げから算出 |
| 棚卸差異照会 | `StockDifferenceQuery` | `BaseQuery` | – | – | L3 | 差異は出せるが**反映する手段が無い** |
| 商品履歴照会 | `ShohinHistoryQuery` | `BaseQuery` | – | – | L3 | |
| 汎用在庫表 | `GeneralStockTable` | `SummaryRealStock` | – | – | L3 | |
| 在庫明細表 | `StockMeisaiTable` | `BaseReport` | – | – | L3 | |
| 倉庫別分類別在庫 | `SokoCategoryStockList` | `BaseReport` | – | – | L3 | |
| 倉庫別受払表 | `SokoInOutReport` | `BaseReport` | – | – | L3 | `AdjustQty` を読むが書き手が居ない |
| 倉庫別集計表 | `SokoSummaryReport` | `BaseReport` | – | – | L3 | 同上 |
| 商品別受払表 | `ShohinInOutReport` | `BaseReport` | – | – | L3 | 同上 |
| 棚卸チェックリスト | `StockCheckList` | `BaseReport` | – | – | L3 | |
| 品番別移動チェック | `HinbanIdoCheckList` | `BaseReport` | – | – | L3 | |
| 移動未受リスト | `IdoUnreceivedList` | `BaseReport` | – | – | L3 | 積送中の滞留検知 |
| 在庫日付一括メンテ | `StockDateBulkMente` | 422行 | ○ | – | L3 | |
| **在庫強制調整入力** | `StockForceInput` | 空クラス | – | – | **L0** | 調整の保存先が無い。VMに設計案A/Bを記録済み |

### 3.8 31Monthly 月次・締・原価（18画面 / うち17画面がL0）

| 画面 | ViewModel | Lv | 残課題 |
|---|---|:-:|---|
| 在庫・掛再更新 | `StockKakeUpdate`（167行） | L3 | 唯一実装済。`Msg051_SummaryRealStock` / `Msg052_SummaryUriKake` / `Msg053_SummaryKaiKake` をストリーミング実行 |
| **請求計算** | `BillingCalculation` | **L0** | `SummaryUriSei` の生成ロジックが `CvDomainLogic` に存在しない |
| **支払計算** | `PaymentCalculation` | **L0** | `SummaryKaiShi` 同上 |
| **締日更新** | `ShimebiUpdate` | **L0** | |
| 棚卸開始 | `StockTakeInitiation` | **L3** | `StocktakeDb.StartStocktake()` を実行。倉庫別棚卸日との連携は未実装 |
| 棚卸確定処理 | `StockTakeFinalization` | **L3** | `StocktakeDb.FixStocktake()` が調整伝票を作成。再確定可 |
| **総平均原価更新** | `TotalAverageCostUpdate` | **L0** | |
| **最終仕入原価更新** | `LastPurchaseCostRefresh` | **L0** | |
| **消化仕入更新** | `ConsumptionPurchaseUpdate` | **L0** | |
| **積送中クリア** | `InTransitClear` | **L0** | |
| **月間集計** | `MonthlyDataSummary` | **L0** | |
| **累計更新** | `StockRuikeiUpdate` | **L0** | |
| **税再計算** | `TaxRecalculation` | **L0** | |
| **諸掛更新** | `SundryChargesUpdate` | **L0** | |
| **残高登録** | `BalanceRegistration` | **L0** | 移行時の期首残高投入。Phase 8 の前提 |
| **データ整理** | `DataCleanupUpdate` | **L0** | |
| **仮処理** | `TemporaryProcessing` | **L0** | |
| **自動発注補充実行** | `AutoOrderReplenishExecute` | **L0** | |

---

## 4. 完成条件に対する横断的な欠落（最重要）

| 完成判定項目 | 保持場所 | 状態 |
|---|---|---|
| 実在庫 | `SummaryRealStock.Su` | **○** 6伝票種から即時更新。修正・削除も反転で打ち消す |
| 積送中在庫 | `SummaryStock.TransitQty` | **△** 月次テーブルにしか無く、現在値は月次行の積上げで算出 |
| 発注残 | `Tran13Hachu.EndFlag`＋都度算出 | **○** SKU単位の不足で自動完了を判定し、残完了設定で手動完了・解除できる。編集時ワーニングは未実装 |
| 受注残 | `Tran12Jyuchu.EndFlag`＋都度算出 | **○** 卸先・売仕店への出荷をSKU単位で突合する。編集時ワーニングは未実装 |
| 引当数 | `SummaryRealStock.ReserveQty` / `SummaryStock.ReserveQty` | **△** `EndFlag=0 AND Kubun!=0` を対象に、未確定は `Su`、確定済みは `JitsuSu` を集計する。照会3画面と配分確定・出荷処理画面は未実装 |
| 有効在庫 | 列なし（`Su - ReserveQty`） | **△** 算式は確定。受注残は引かない（2026-08-17）。表示・判定に使う画面は未実装 |
| 欠品数 | `TranHaibun.ShortSu` | **△** 2026-08-17 追加。倉庫から戻るデータで設定する経路（出荷処理入力）が未実装 |
| 売掛残 | `SummaryUriKake` | **△** 伝票保存時に更新されない。`StockKakeUpdate` の再更新でのみ生成 |
| 買掛残 | `SummaryKaiKake` | **△** 同上 |
| 請求残 | `SummaryUriSei` | **×** スキーマのみ。生成処理が皆無（実DBで0件） |
| 支払残 | `SummaryKaiShi` | **×** 同上（実DBで0件） |
| 原価 | `MasterShohin.TankaGenka` | **×** 最終仕入原価・総平均原価・評価替のいずれも未実装 |
| 月次集計値 | `SummaryStock` | **△** 在庫のみ。月間集計画面は L0 |

補足として、業務フロー上の断絶が3か所ある。

1. **配分 → 移動/出荷** — **2026-08-15 に一部解消**。`SummaryDb.CalcHaibun2Reserve()` が `TranHaibun` を読み、
   `EndFlag=0` の行を引当数へ集計するようになった（配分は在庫の可視性に効くようになった）。
   ただし**配分を確定して移動/出荷伝票を生成する処理は依然として無い**（`TranHaibun.RelateNo2` は常に0）。
   `EndFlag` を立てる画面も未実装のため、引当は登録すると解除されない。
2. **棚卸 → 在庫** — `Tran60Tana` は `ITranSoko` を実装していないため在庫を動かさない。
   差異照会はあるが、確定して在庫へ反映する `StockTakeFinalization` は L0。
3. ~~**消込 → 残高**~~ — **2026-08-14 解消**。消込仕様が確定し、`TranKesikomi` 新設案とFIFO充当は**不採用**となった。
   `Tran00Uriage` / `Tran03Shiire` へ `EndFlag` を追加し、伝票単位CheckBox方式へ差し替え済み
   （`2026-08-12_phase1_業務仕様決定ドラフト.md` 2.1 / 2.1.1 / 2.1.3）。`BaseMatchingViewModel` の
   `ApplyFifoAllocation()` / `Allocated` / `AutoMatch` / `ClearMatch` / `IsUnmatchedOnly` は削除した。
   ただし消込は**残高計算に影響しない**設計である（残高は伝票金額ベース、`EndFlag` は元帳の `*` 印字のためだけに存在する）。
   残る作業は元帳（`TokuiLedger` / `ShiireLedger`）への `*` 列追加（ドラフト 2.1.2-6、未着手）であり、
   これが入るまで消込結果は画面上でしか見えない。

`通常更新値 = Rebuild値` の検証範囲についても、現状 Rebuild 対象は在庫・売掛・買掛の3つのみで、
請求・支払・原価は Rebuild 側にも実装が無い。

### 参考: 実データの偏り

移行済みDB（`server-user163.db`）の件数。移行スクリプト `ConvertDb` が
どの業務まで通しているかが分かるため、テストシナリオ設計の参考になる。

| テーブル | 件数 | | テーブル | 件数 |
|---|---:|---|---|---:|
| `Tran01Tenuri` 店舗売上 | 3,432,479 | | `Tran13Hachu` 発注 | 1 |
| `Tran10IdoOut` 移動出庫 | 173,638 | | `Tran12Jyuchu` 受注 | 1 |
| `Tran11IdoIn` 移動受 | 170,930 | | `Tran03Shiire` 仕入 | 2 |
| `Tran00Uriage` 出荷売上 | 50,311 | | `Tran05Ido` 即時移動 | 1 |
| `Tran60Tana` 棚卸 | 47,764 | | `Tran07Shiharai` 支払 | 0 |
| `Tran06Nyukin` 入金 | 2,299 | | `TranHaibun` 配分 | 0 |
| `SummaryUriKake` 売掛 | 385 | | `SummaryUriSei` 請求 | 0 |
| `SummaryKaiKake` 買掛 | 1 | | `SummaryKaiShi` 支払 | 0 |

**発注・受注・仕入・支払・配分は実データがほぼ無い**。この5系統は移行対象外か未着手であり、
Phase 2〜4 のシナリオ検証にはテストデータの新規作成が必要になる。

---

## 5. 優先度付き残作業

`2026-08暫定スケジュールと課題.md` の Phase 割りに対応させた。

**A. 仕様確定が先（Phase 1、2026年8〜9月）** — 2026-08-14 時点の確定状況を各項に追記した。

| # | 項目 | 状態（2026-08-17 更新） |
|---|---|---|
| 1 | 引当モデル | **仕様確定（2026-08-17）／実装は一部未了**。`SummaryRealStock.ReserveQty` / `SummaryStock.ReserveQty`、有効在庫 = `Su - ReserveQty`。**引当対象は `Kubun != 0`（初回配分を除く全区分）**、**引当数は未確定なら `Su`、確定済み（`KakuteiDay` 有効）なら `JitsuSu`**、解除は `EndFlag=1`。受注残は有効在庫から引かない。2026-08-15 の実装は「全区分・常に `Su`」であり、`Kubun` の絞り込みと `Su`/`JitsuSu` の切替が**未実装**（比較資料 5.2.2 / 5.2.2c） |
| 2 | 消込結果の永続化 | **確定・実装済み（2026-08-14）**。`EndFlag` 方式。元帳の `*` 印字も 2026-08-16 に実装済み |
| 3 | 掛の更新タイミング | **確定・実装済み（2026-08-16）**。夜間の月次再集計で更新。区分別内訳・`IsPay` 絞り込み・後続月の繰越再計算まで実装 |
| 4 | 完了FLG | **確定・スキーマ実装済み（2026-08-17）**。`Tran13Hachu` / `Tran12Jyuchu` に `EndFlag` を追加（`26_08_17_01`）。**伝票単位／自動完了（明細単位で全SKU充足）＋手動での完了・解除／監査列なし**。完了後は削除・数量減でも自動で戻さずワーニングを出す（比較資料 4.2 / 4.3） |
| 5 | 在庫調整の記録方法 | **確定（2026-08-17）**。調整専用伝票 `Tran61Chosei` を新設。あわせて**棚卸確定は `Su` との差を `AdjustQty` へ設定**し、在庫数を `Su = InQty + OutQty + AdjustQty` へ変更する（比較資料 8.4 / 8.4.1） |
| 6 | 納品予定日 | **確定（2026-08-17）**。**伝票単位**で発注・受注のヘッダへ持つ。リードタイム自動計算は 2.0 以降。納期遅れは納品日と `EndFlag` で判定（比較資料 6.2） |
| 7 | 請求計算・支払計算の仕様 | **概ね確定（2026-08-17）**。集計キーは得意先／仕入先単位、請求日＝請求終了日＝締日、締日は第1段階1得意先1締日。**消費税は月ごとに 得意先×年月 で `Kingaku` に対して計算（四捨五入・外税基本）**。請求書番号（E10）とその他売上（E11）のみ保留（比較資料 7章） |
| 8 | 配分・出荷 | **確定（2026-08-17、新規）**。出荷指示確定が `KakuteiDay`（有効在庫割れはエラー）、出荷処理入力が伝票作成＋`RelateNo2`＋`EndFlag=1`。伝票は出荷先の `TenType` で分岐（1・3＝出荷売上／0・6＝移動）。ハンディは廃止。`TranHaibun.ShortSu`（欠品数）を追加済み。**配分照会3画面を1.0へ追加（130→133画面）**（比較資料 5章） |

上表1〜8は 2026-08-17 に旧CVnet資料（`refer/cvnet-knowlege/` 全点）と突き合わせて決着した。
各項の根拠と Given/When/Then は
[2026-08-17_旧cvnet比較_仕様決定判断材料.md](2026-08-17_旧cvnet比較_仕様決定判断材料.md) を参照する。

**A に残る未決は次の4件のみである。**

| # | 項目 | 内容 |
|---|---|---|
| A-9 | E10 請求書番号 | 請求書発行のたびに採番して再発行・差し替えを識別するか。請求書印刷を1.0に含めるかと連動 |
| A-10 | E11 その他売上 | 請求一覧表の算式に含まれるが CV10 は区分99を `Uriage` へ畳んでいる |
| A-11 | E7 親子締日チェック | `Id_Paysaki` 全件 `0` のため1.0不要と判断できるが、Phase 4 が「親子請求」を範囲に含む |
| A-12 | E8-g 締日が月末以外のときの税 | 第2段階（最大3締日）へ進む前の必須決定事項。第1段階の間は表面化しない |

**B. 実装（Phase 2〜5）**

7. 請求計算・支払計算（`SummaryUriSei` / `SummaryKaiShi` 生成）— 1.0必須で完全未着手。
8. 棚卸更新（差異 → 在庫反映）— 棚卸入力と差異照会は既にあるので、確定処理だけで閉じる。
9. 原価更新3種（最終仕入・総平均・評価替）。
10. 締日更新・月間集計・積送中クリア。
11. 配分確定 → 移動/出荷伝票生成（`TranHaibun` の消費側）。
12. 残高登録 — 移行試験（Phase 8）の前提。

**C. 検証（Phase 7）**

13. ~~在庫不変条件のテスト~~ — 完了。仕入+10、返品-2、即時移動、積送出庫/移動受、
    更新反転、消滅キー、前月値復帰、途中失敗rollback、非Tran列保持を `SummaryDbTests` 12件で確認。
14. ~~`通常更新値 = Rebuild値` の一致テスト~~ — 完了。Rebuild 2回の冪等性も確認。
15. 発注→仕入→在庫→受注→売上→入金 の通しシナリオ。テストデータは新規作成が必要。

---

## 6. 補足: メニュー定義について

`CvWpfclient/Models/MenuData.cs`（491行）には `AllowedRoles`（`EnumLoginRole`）によるロール絞り込みが
既に入っており、店舗・物流メニューはロール限定として定義されている。ただし
`2026-08暫定スケジュールと課題.md` 8章が求める `Capability` / `BusinessArea` / `Availability` /
`RequiredPermission` は未分離で、L0 の50画面もメニューから通常どおり開ける（開くと空画面が出る）。
`Availability`（実装済/Preview/Hidden）だけでも先に導入すると、UAT時の混乱を避けられる。
なお同ファイル468行付近に「権限分離は Permission 実装後に対応する」と既にコメントがある。

---

## 7. 2026-08-12 再確認で追加した実行順序

1. **P0-1 完了: 即時在庫更新を4フラグで判定する**
   - `5399328` の移動先 `GetCalcIdosaki()` 適用に加え、`SummaryStock` は4フラグのいずれかが非0なら更新する。
   - `SummaryRealStock` は実在庫フラグが非0の場合だけ更新し、積送中だけの移動先へ実在庫を加えない。
2. **P0-2 完了: Rebuild を冪等かつ原子的にする**
   - 対象範囲の旧キーと非Tran列4列を一時表へ退避し、対象月削除→6伝票再生成→非Tran列復元→
     旧・新キー和集合の `SummaryRealStock` 再構築を単一トランザクションで実行する。
   - 最後のTranが消えたキーは削除し、対象月より前の値があればその累計へ戻す。途中例外は全体rollbackする。
3. **P0-3 完了: 在庫不変条件を自動テスト化する**
   - `SummaryDbTests` 12/12、`TestServer` 全45/45成功。即時更新とRebuild一致、2回実行の冪等性、
     修正時の旧値反転、移動3種、仕入/返品、消滅キー、前月復帰、範囲外保持、rollback、非Tran列保持を確認。
   - 独立Testerと独立レビューを分け、最終レビューは承認。
4. **P1: L4 判定を再集計する**
   - P0 の残作業と回帰テスト完了後、「掛が非該当の移動画面」の扱いを明文化して L3/L4 を再分類する。
5. **P1: 仕様承認を個別に取得する**
   - 既存推奨案あり: 消込は `TranKesikomi` 新設、在庫調整は調整用 Tran 新設。
   - 部分設計あり: 引当/配分/取置、納品予定日。
   - 追加設計が必要: 掛の即時更新方式、発注・受注の完了FLGと監査情報。

### P0運用上の確認

- 月次Schedulerは前月・当月を別々に再集計する。前月成功後に当月が失敗した場合、各月呼出の原子性は保たれるが、
  対象キーの `SummaryRealStock` は一時的に前月末値になる。
- 通常の履歴DB書込み可能な経路では、失敗は `SysHistAutoexec.ReturnCode=9`、`Memo` の区分別 `NG` 詳細、
  およびサーバーログに記録される。システム管理者が自動実行履歴から確認して再実行等を判断する。
- `SysAutoExecHistoryView` は `ReturnCode != 0` の行について一覧の Id・タスク名をエラー色で表示する。
  履歴テーブル自体のINSERT/UPDATE失敗やプロセス強制終了は履歴行を保証できず、サーバーログ確認が必要。

### 実装コミット

- `531e71c` P0 在庫即時更新と再構築の整合性を修正
- `17c817c` 自動実行履歴のエラー表示を強調

---

## 8. 2026-08-14 進捗更新（HEAD `f27fedd`）

### 8.1 `bbad7ed` → `f27fedd` のコミット内容

| コミット | 内容 | 完成度への影響 |
|---|---|---|
| `db78ff9` | 機能完成度チェックリストを追加 | ドキュメントのみ |
| `d6dc08c` | ドキュメントを仕様作成用に再編成。Phase 1 業務仕様決定ドラフトを新設し、`spec.database.cvbase.md` / `spec.general.readme.md` / `checkSummaryRealStockDefference.sql` を削除 | ドキュメントのみ |
| `c8d6a76` | ドキュメントの更新 | ドキュメントのみ |
| `4ebd75d` | バージョンアップ 0.8.14（`CvServer` / `CvWpfclient` の `appsettings.json`） | 機能変更なし |
| `c8cc73d` | `ps-crlf.ps1` に UTF8 指定を追加 | ビルド補助のみ |
| `8972eab` | `EnumUri00` の各値へ `[Comment]` を追加（P売上/S売上/P返品/S返品/値引/その他）。併せて仕様ドラフトを更新 | **唯一の業務コード変更**。DDLのコメント生成に効くのみで挙動は不変 |
| `f27fedd` | 仕様ドラフト更新（消込仕様の確定） | ドキュメントのみ |

**結論: 画面の実装完成度は前回調査（130画面 / L0=49, L2=14, L3=59, L4=8暫定, L5=0）から変化していない。**
2章のサマリ表、3章の領域別チェックリスト、4章の横断的欠落表はいずれも据え置きとする。

### 8.2 Phase 1（仕様確定）の進捗

`Doc/spec/2026-08-12_phase1_業務仕様決定ドラフト.md` の確定範囲が広がった。

- **確定**: 消込の定義（伝票単位 `EndFlag`、元帳へ `*` 印字、部分消込・充当額を持たない）、入金消込／支払消込の
  検索条件・一覧項目・`消込実行` の挙動、掛更新は夜間バッチ、発注／受注の `EndFlag`、納品予定日は
  `TranHaibun.NouhinDay` を流用しない、在庫調整は調整専用伝票方式。
- **今回新たに論点化**: 消込の残論点7件（2.1.2）、掛集計 `CalcSummaryUriKake` / `CalcSummaryKaiKake` の
  更新ロジック7件（2.6）、請求計算・支払計算6件（2.7）、引当数の保持方式4件（2.8）、移行時の初期値（4.1）。

### 8.3 今回のコード確認で判明した、5章「B. 実装」に効く事実

いずれも本書 3章の Lv 判定は変えないが、着手時の作業量に直結する。

1. `CalcSummaryUriKake()` / `CalcSummaryKaiKake()` は `Henpin` / `Nebiki` / `Cash` / `Fee` / `Densai` /
   `Offset` / `Other` を **`0` 固定で INSERT** している。売掛残高表・買掛残高表の内訳欄は
   再更新を回しても常に0であり、「値が出ない」だけでなく「内訳が未実装」である。
2. 同2つのSQLは `Tran00Uriage.IsPay`（掛計上FLG）を条件に使っていない。掛計上しない売上も売掛へ入る。
3. 同2つのSQLは対象期間より**後**の月の `Balance` を再計算しない。夜間ジョブは前月・当月しか回さないため
   通常は表面化しないが、`StockKakeUpdate` 画面から過去月を単独指定すると以降の繰越が古いまま残る。
4. 入金・支払の明細 `TranKinMeisai` は `Jmeisai`（`SerializedColumn`, 4000桁のJSON）に入っており、
   SQLで `GROUP BY` できない。消込画面の区分別集計と、上記1の内訳の両方でJSON展開処理が必要になる。
5. `Tran06Nyukin` / `Tran07Shiharai` に入金日・支払日の専用列は無く、日付は `DenDay` のみである。
6. `SummaryRealStock` の列は `Id_Soko` / `Id_Shohin` / `Id_Col` / `Id_Siz` / `Su` だけで、`AdjustQty` は
   月次の `SummaryStock` にしかない。在庫調整・引当の列追加はどちらも `SummaryRealStock` の
   スキーマ変更を伴う（＝通常更新・範囲Rebuild・全件Rebuildの3経路を同時に変更する）。
7. `TranHaibun` には `KakuteiDay`（確定日、空なら未確定）・`JitsuSu`（実数量）・`RelateNo2`（生成した伝票Id）が
   既にあり、引当の確定・解除判定に使える。`EnumHaibun` は 0〜7 の8区分が定義済み。

### 8.4 消込機能の実装（2026-08-14、ドラフト 2.1.1 の実装）

ドラフト 2.1.1 の差し替えを実装した。詳細はドラフト 2.1.3 を参照する。

- **スキーマ**: `Tran00Uriage` / `Tran03Shiire` へ `EndFlag` を追加。`UpdateDb` の `26_08_14_01` で
  既存DBへ `ADD COLUMN EndFlag NUMBER not null default 0`（既存伝票は全て未消込）。
- **サーバー**: `PartialUpdateParam`（テーブル型＋更新列＋データ配列、`Vdu` はサーバー採番）を新設し、
  `HandlePartialUpdate()` で指定列のみを単一トランザクションで更新する。`Tran00Uriage` / `Tran03Shiire` は
  `ITranSoko` 実装のため既存 `UpdateParam` では1件ごとに在庫再集計が2回走るのを避けるのが目的。
  付随処理が必要な列は禁止列リストで拒否する。
- **画面**: FIFO自動充当を全廃し、請求先（必須）＋得意先（任意）＋掛計上日 from-to ＋支払日 from-to の条件で
  一覧取得し、伝票単位の消込Flgを `消込実行` で書き戻す。入金・支払は `Jmeisai` を `Id_Kin` 単位で集計して並べ、
  「伝票計 / 消込計 / 入金(支払)計」の3合計を比較できるようにした。
- **完成度への影響**: 入金消込・支払消込が L2 → **L3**。2章サマリは L2 が 14→12、L3 が 59→61 になった
  （動作画面数 81/130 は変わらない）。4章の業務フロー断絶3「消込 → 残高」を解消した。

意図的に実装しなかった点。

- 消込は残高へ影響しない。`SummaryUriKake` / `SummaryKaiKake` の集計SQLは変更していない（ドラフト 2.6 は別作業）。
- 元帳への `*` 列追加（2.1.2-6）は範囲外。これが入るまで消込結果は消込画面でしか見えない。
- 消込の監査列（誰がいつ）は持たない（2.1.2-5 の推奨どおり）。`Vdu` の更新のみ。

楽観排他は同日の追加実装で入れた。`PartialUpdateRow.ExpectedVdu`（一覧取得時点の `Vdu`）を
`UPDATE ... WHERE Id = @ AND Vdu = @` で照合し、1件でも競合すればトランザクション全体を戻して
`HandleUpdate()` と同じ `ConcurrentUpdate` を返す。画面は一覧を破棄して再取得を促し、成功時も
`Vdu` が変わるため自動で一覧を取り直す。

### 8.4.1 実機検証の結果（2026-08-14）

`CvServer` + `CvWpfclient` を実データ（`server-user163.db`）で起動し、消込2画面と楽観排他を確認した。
検証用に仕入24件／支払3件／入金3件を投入した（`ManualNo LIKE 'CLTEST%'` で識別。`Jmeisai='[]'` のため在庫集計へは影響しない）。

| 検証項目 | 結果 |
|---|---|
| `UpdateDb` の `26_08_14_01` 適用 | **OK**。両テーブルに `EndFlag` が追加され `SysUpdateDb.DbVersion=26081401` |
| 支払消込 一覧取得 | **OK**。仕入25件（既存1＋投入24）/ 支払3件。仕入計 1,091,200 / 支払計 1,055,880 |
| 入金消込 一覧取得 | **OK**。売上15件 / 入金3件。売上計 47,380 / 入金計 47,240（差140円を画面で比較できる） |
| `Jmeisai` の `Id_Kin` 別集計 | **OK**。1伝票2明細（手形入金＋振込手数料）も区分ごとに分解された |
| 消込実行（支払） | **OK**。「25件消込しました」→ `Tran03Shiire.EndFlag=1` が25件 |
| 消込実行（入金） | **OK**。「15件消込しました」→ 対象15件のみ `EndFlag=1`（残り50,296件は不変） |
| 成功後の一覧自動再取得 | **OK**。「未反映の変更 0 件」に戻り、連続実行しても楽観排他で弾かれない |
| 楽観排他の競合検知 | **OK**。25件中13件目の `Vdu` をDBで書き換えて実行 → サーバーログ `部分更新 競合検知 Tran03Shiire Id=13`、画面は「他端末で更新されたため消込しませんでした（1件も更新していません）」 |
| 競合時の全件rollback | **OK**。Id 1〜12 はUPDATE実行済みだったが `EndFlag` は25件すべて1のまま。部分適用なし |

検証中に見つけて修正した不具合。

1. **請求先の絞り込みが常に0件になる**（`BaseMatchingViewModel`）。`QueryListSqlParam.Parameters` が `string[]` のため
   Idをパラメータで渡すと SQLite が整数列と文字列を比較して一致しない（動的型のため `Id = '1'` は0件）。
   Idは `long` で数値以外を含み得ないので、サーバー側 `HandlePartialUpdate` の `Id IN (...)` と同じ理由でSQLへ直接埋め込む形に修正した。
   **静的解析とビルドでは検出できず、実機で一覧が空になって初めて判明した**。
2. **メニューの説明文が旧仕様のまま**（`MenuData.cs`）。入金消込・支払消込の `addInfo` が
   「FIFOで突合し未回収残･未充当を確認(保存は未対応)」だったので、EndFlag方式の説明へ差し替えた。

### 8.5 次の更新タイミング

Phase 1 の残論点（ドラフト 2.1.2-6 / 2.6 / 2.7）が承認され、スキーマ変更が入った時点で
2章のサマリ表と3章の該当行を更新する。2.8（引当）は 9.1 で確定・実装済みとなった。

---

## 9. 2026-08-16 進捗更新（HEAD `c9f755e`）

### 9.1 `f27fedd` → `c9f755e` のコミット内容

| コミット | 内容 | 完成度への影響 |
|---|---|---|
| `05e8263` | 引当数 `ReserveQty`（`SummaryRealStock` / `SummaryStock`）と `TranHaibun.EndFlag` を追加。`SummaryDb` に `CalcHaibun2Reserve()` / `CalcReserveQtyAll()` を新設し、CRUD6経路から呼ぶ | **4章の欠落2件を解消**。ただし読む画面が無いためLv判定は不変 |
| `c160243` | 取引区分enumを業務別に分離（`EnumJuchu` / `EnumHachu` を追加）。`Kubun` の整数値は維持 | 挙動不変。DB・JSON・帳票SQLの移行不要 |
| `da69e59` | テーブル定義の `Comment` 属性を全カラム（639件）へ拡充。`SysTableSpecViewModel` がカラムのコメントをDB定義書へ出すよう変更 | 業務挙動は不変。DB定義書の説明欄が埋まる |
| `c9f755e` | 更新経路の副作用（在庫・引当・派生展開・V*列伝播）のオーケストレーションを `WriteEffectRunner` へ集約。`HandlerDerived` を `CvDomainLogic/DerivedDb` へ移設 | 挙動不変のリファクタ。副次的に不具合2件を修正 |

**結論: 画面の実装完成度は前回から変化していない（130画面 / L0=49, L2=12, L3=61, L4=8暫定, L5=0）。**
2章のサマリ表と3章の領域別チェックリストは据え置きとする。変わったのは4章（横断的欠落）と5章A-1（引当モデル）である。

### 9.2 引当数の実装（ドラフト 2.8.1）

- **保持場所**: `SummaryRealStock.ReserveQty`（倉庫+SKU）と `SummaryStock.ReserveQty`（年月+倉庫+SKU）。別テーブルは作らない。
- **源泉**: `TranHaibun` のみ。`EndFlag=0`（振り分け後の入庫がまだ）の行の `Su` を集計する。
- **更新方式**: 差分の加減算ではなく**対象キーの引き直し**。呼び出し順やDB更新との前後関係に依存せず、
  通常更新値と全件Rebuild値が原理的に一致する（`CalcReserveQtyAll()` と一致することをテストで固定）。
- **有効在庫**: `Su - ReserveQty`。在庫実績が無いSKUへ配分するとマイナスで見える（意図どおり）。
- **未実装**: 引当数・有効在庫を表示する画面、`EndFlag` を立てる画面（配分確定）、`RelateNo2` への伝票Id書き戻し。
  したがって 07Haibun の17画面と受注残管理表は依然として着手待ちである。

### 9.3 更新経路の責務分担（2026-08-16）

本書の1章「判定に使った実装上の事実」で述べた「在庫反映は汎用CRUDハンドラ側で自動実行される」構造を、
副作用の**宣言**（`CvBase` のマーカーインターフェース）・**実行**（`CvDomainLogic`）・**起動順序**（`CvServer`）
の3層に整理した。判定基準そのものは変わらない。

- `ITranSoko` / `ITranIdo` / `ITranReserve` / `IDerivedOrigin` を実装する型の一覧は従来どおりで、
  L4 判定に使う「どの伝票が在庫を動かすか」の事実は不変（`Tran00Uriage` / `Tran01Tenuri` / `Tran03Shiire` /
  `Tran05Ido` / `Tran10IdoOut` / `Tran11IdoIn` の6テーブル）。
- 副次的に、`HandleDelete` / `HandleDeleteById` で例外時にトランザクションが開いたままになる不具合と、
  他端末が削除済みの行を更新・削除したときに `ConcurrentUpdate` ではなく汎用例外が返る不具合を修正した。
- `WriteEffectRunner` は `public` にしてテスト可能にした（新規12件）。従来は更新の手順そのものがテストできず、
  `MasterCascadeDbTests` が手順を手で再現していた。

### 9.4 未着手のまま残っている構造的な問題

`CvDomainLogic` / `CvServer` の境界調査で判明したもので、いずれも今回のスコープ外とした。

1. **業務単位が複数トランザクションに割れている**。`ShopHaibunInput` / `HachuHaibunInput` /
   `ShopBrandBudgetMaster` / `SalesStaffBudgetMaster` の4画面は「既存行を1件ずつ削除 → 一括登録」を
   N+1回のgRPC呼び出しで行う。CvServerは1呼び出し=1トランザクションなので、削除が成功した後に登録が
   失敗すると**データが消えたまま復旧しない**。引当再計算もN+1回走る。
2. **読み取り経路に層が無い**。クライアント100ファイルが `QueryListSqlParam` で生SQLを組み立て、
   サーバーは検証せず実行する。読み取りの業務知識（JOIN条件・区分の意味・有効在庫の式）はクライアント側にある。
   この方針は維持すると決定済み（更新はサーバー側で処理する）。

### 9.5 次の更新タイミング

07Haibun の配分確定（`EndFlag` を立てて移動/出荷伝票を生成する処理）が実装され、
引当を読む画面が出た時点で 2章のサマリ表と 3.6 を更新する。

---

## 10. 2026-08-17 進捗更新（HEAD `9833f24` ＋ 未コミット分）

### 10.1 `c9f755e` → `9833f24` のコミット内容

| コミット | 内容 | 完成度への影響 |
|---|---|---|
| `0df7357` | 入金・支払明細の旧区分コードを `KIN` 区分へ対応付け | 移行データの是正。`Jmeisai.Id_Kin` が全件 `KIN` マスタへ紐付いた |
| `9833f24` | 消込・掛集計の残論点を確定し実装。`TranKinHeader.DenDay` → `KakeDay` 改名、掛集計の区分別内訳・`IsPay` 絞り込み・後続月の繰越再計算、元帳の `*` 印字 | **4章の「内訳が未実装」「基準日の非対称」「後続月の繰越が古い」の3件を解消**。Lv判定は不変 |

### 10.2 旧CVnet資料との突き合わせ（2026-08-17）

`refer/cvnet-knowlege/`（業務フロー18点 / マニュアル14点 / DB定義書2点 / 帳票集PDF15点）と
`refer/cv10meeting/`（議事録5点）を全点読了し、Phase 1〜3 の業務仕様を確定した。
決定内容・根拠・Given/When/Then は
[2026-08-17_旧cvnet比較_仕様決定判断材料.md](2026-08-17_旧cvnet比較_仕様決定判断材料.md) を正本とする。

5章A の未確定7項目のうち**6項目が決着し、新たに「配分・出荷」が項目8として確定した**。
残る未決は E10 / E11 / E7 / E8-g の4件のみである。

### 10.3 スキーマ変更（`26_08_17_01`）

| テーブル | 列 | 用途 |
|---|---|---|
| `Tran13Hachu` | `EndFlag` | 発注残の完了 |
| `Tran12Jyuchu` | `EndFlag` | 受注残の完了 |
| `TranHaibun` | `ShortSu` | 配分の欠品数 |

ビルド成功、`TestServer` 72件 / `TestLogin` 7件すべて成功。実DBへ適用済み。

### 10.4 引当集計とメニュー構成の実装（2026-08-17）

1. **引当集計を新仕様へ修正**（`CvDomainLogic/SummaryDb.cs`）。
   `ReserveTargetWhere`（`EndFlag = 0 AND Kubun <> 0`）と
   `ReserveQtySumExpr`（未確定は `Su`、確定済みは `JitsuSu`）を新設し、
   `CalcHaibun2Reserve()` / `CalcReserveQtyAll()` の4か所で共用した。
   初回配分は引当対象外になり、欠品分は確定と同時に引当から外れる。
2. **配分照会3画面を新設**（配分問合わせ / 引当問合わせ / 有効在庫問合わせ）。
   旧CV.netの【配分】9〜11に相当し、CV10 には対応画面が無かった。
   これで「引当数・有効在庫を読む画面が0」という4章の状態に着手先ができた。
3. **メニュー構成を1.0スコープへ整理**（`CvWpfclient/Models/MenuData.cs`）。
   「発注配分リスト」「配分出荷リスト」を削除（配分の一覧は出荷指示一覧印刷へ集約）、
   「棚卸確定」→「棚卸確定処理」へ改名、確定した仕様を `addInfo` へ反映、
   1.0対象外（締日更新・自動補充・展示会スワッチ・絵型一覧表）は「1.1以降」と明示した。
   HHT連携は残した（決定 I6 は配分→出荷でハンディに依存しない意味であり、HHT機能の廃止ではない）。

検証: ビルド成功。`TestServer` 74件（新規2件）・`TestLogin` 7件すべて成功。

**対象画面数は 130 → 131 になった**（新設3 − 削除2）。
2章のサマリ表は L0 の増減を含めて次回の実装反映時にまとめて更新する。

### 10.5 業務ロジックの実装（2026-08-17）— 画面は未着手

10.4 に続き、本日の決定に対応する業務ロジックを実装した。**画面（WPF）はいずれも未着手である。**

| # | 実装 | 内容 |
|---|---|---|
| 1 | `CompletionDb`（新規） | 発注・受注の完了フラグを紐付く仕入・出荷から自動判定する。判定は明細単位で全SKU充足。完了は自動で戻さず、`FindCompleted()` を編集時ワーニングに使う。`WriteEffectRunner` から起動する |
| 2 | `ShippingDb`（新規） | 出荷指示確定（有効在庫割れは全件エラー）と出荷処理（仮想ヘッダ単位で伝票作成、`RelateNo2` と `EndFlag=1`）。伝票種別は出荷先の `TenType` で分岐する |
| 3 | `Tran61Chosei`（新規テーブル）＋ `StocktakeDb`（新規） | 在庫調整伝票を新設し、棚卸開始処理（帳簿在庫の凍結）と棚卸確定処理（差を調整伝票へ、再確定可）を実装した |
| 4 | 在庫恒等式の変更 | `SummaryStock` へ `AdjustQty` を集計し、Rebuild 対象に `Tran61Chosei` を追加。`Su = InQty + OutQty + AdjustQty` になった |

**調整を集計テーブルへ直接書かず伝票にしたのは「通常更新値 = Rebuild値」を守るため**である。
`AdjustQty` はこれまで非Tran列として Rebuild 時に復元していたが、伝票から導出する列になったので復元対象から外した。

スキーマ: `26_08_17_02` で `SummaryStock.BookQty`（帳簿在庫スナップショット）を追加。
`Tran61Chosei` は `DefineDataTable` が起動時に作成する。

検証: ビルド成功。`TestServer` 85件（新規11件）・`TestLogin` 7件すべて成功。

### 10.5.1 棚卸開始処理・棚卸確定処理の画面（2026-08-17 実装）

`StocktakeDb` を画面から実行できるようにした。**L0 → L3 へ進んだ最初の2画面である。**

- 通信は既存の集計バッチ（在庫・掛再更新）と同じストリーミング経路に載せた。
  `Msg054_StocktakeStart` / `Msg055_StocktakeFix` と `StocktakeParameter` を追加している。
- 2画面は入力項目がほぼ同じなので `BaseStocktakeViewModel` へ共通化した。
  棚卸年月・対象倉庫（複数選択。未選択は全倉庫）・進捗・実行・キャンセルが共通で、
  確定処理だけ「調整伝票日付」と「入力社員」を持つ。
- 確定処理はサーバー側で Serializable トランザクションに包み、途中で失敗したら全体を戻す。

検証: ビルド成功。`TestServer` 87件（新規2件）・`TestLogin` 7件すべて成功。
実DB（`DbVersion=26081701`、`SummaryStock` 349万行）に対し `26_08_17_02` の適用可否と
棚卸開始処理のSQL構文を、実スキーマの複製で確認した。

### 10.5.2 発注残完了設定・受注残完了設定（2026-08-17 実装）

残っていてもこれ以上入荷・出荷しないと決めた伝票をまとめて完了にする画面。
自動完了（明細単位で全SKU充足）に対する**例外処理**であり、誤って完了にしたものの解除もできる。

- 消込画面（`BaseMatchingViewModel`）と同型の2段構成（一覧取得 → 完了実行）にし、
  共通部分を `BaseZanCompletionViewModel<TDen>` へ置いた。
- 書き戻しは消込と同じ `PartialUpdateParam` で `EndFlag` 列だけを更新する。
  行単位の楽観排他つきで、1件でも競合すればサーバーが全件rollbackする。
- 一覧の**残数はSKU単位の不足を合計する**。単純な「伝票数量 − 実績数量」だと、
  あるSKUの超過が別のSKUの不足を隠すため、自動完了の判定と同じ見方に揃えた。
- 受注側が数える出荷は卸先(1)・売仕店(3)のみ。区分値は `TranCalcBase.ShukkaTenTypes` を
  サーバー（`CompletionDb`）と画面で共用し、判定が食い違わないようにした。

検証: ビルド成功。`TestServer` 89件（新規2件）・`TestLogin` 7件すべて成功。
`EndFlag` の部分更新が在庫・引当に副作用を起こさないこと、手動解除しても実績が充足していれば
次の書き込みで自動完了へ戻ることを固定した。

### 10.5.3 未着手（画面 4本）

| 画面 | 呼ぶロジック |
|---|---|
| 出荷指示確定(商品) / 出荷指示確定(得意先) | `ShippingDb.ConfirmShipping()` / `CancelConfirm()` |
| 出荷処理入力 | `ShippingDb.CreateShippingSlips()` |
| 配分問合わせ / 引当問合わせ / 有効在庫問合わせ | `SummaryRealStock` の照会（倉庫×SKUマトリクス＋CSV出力） |

加えて、完了済み伝票の**編集時ワーニング**を仕入入力・出荷売上入力へ組み込む必要がある。
また `Tran61Chosei` を使う**在庫強制調整入力**も空Viewのままである。

棚卸まわりの残課題として、棚卸開始処理は現状「棚卸年月末時点」の帳簿在庫を保存しており、
`Tran60TanaDate`（棚卸日一括メンテナンス）の倉庫別棚卸日を参照していない。
倉庫ごとに棚卸日が異なる運用へ広げる場合は日付別の集計が必要になる。

### 10.6 Mini-UAT（判断保留）

修正第1回 3.5 で User-Sales が提案した Phase 単位の Mini-UAT は、
議事録に「誰が・いつ・何をもって合格とするか」が定義されていないため 2026-08-17 時点で判断を保留した。
論点は比較資料 3.1.1 に整理してある（実施者 / 実施タイミング / 合格条件 / 記録方法の4点）。
決めるまでは Release Gate の条件に含めない。

### 10.7 次の更新タイミング

10.5 のいずれかが実装された時点で、2章のサマリ表と該当する3章の行を更新する。
その際に対象画面数を 131 へ改める。

---

## 11. 2026-08-18 進捗更新（I9 配分照会3画面）

### 11.1 実装内容

未適用課題 I9（10.5.3 の「配分問合わせ / 引当問合わせ / 有効在庫問合わせ」）を実装した。
**L0 → L3 へ進んだ 07Haibun の最初の照会画面である。** 詳細設計は
[2026-08-18_I9_配分照会3画面_詳細設計.md](2026-08-18_I9_配分照会3画面_詳細設計.md) を正とする。

- 新規: `ViewModels/07Haibun/BaseHaibunInquiryViewModel.cs`。`ZaikoQueryViewModel`（在庫問合せ）を
  一般化した共通基底。検索条件・商品一覧・倉庫×SKUマトリクスのドリルダウン・CSV出力・gRPC照会
  (`Msg101_Op_Query`) を持ち、展開する数量だけを抽象メソッド `LoadDrillRowsAsync` で差し替える。
- 3サブクラス（各20〜40行）で数量を切り替える。
  - 配分問合わせ … `TranHaibun`（`EndFlag=0`）の配分数。
  - 引当問合わせ … `SummaryRealStock.ReserveQty`（materialize済みの引当数を読むだけ）。
  - 有効在庫問合わせ … `SummaryRealStock.Su - ReserveQty`。
- 3View/コードビハインドを実装。一覧は 在庫／引当／有効在庫／配分数 を並べ、商品CDのダブルクリックで
  倉庫×色サイズのマトリクスタブを開き、CSV（`商品CD_数量名.csv`、UTF-8 BOM）へ出力できる。

サーバ・スキーマ・Msgの変更は無い（読み取りは既存経路、引当は集計テーブル済み）。

### 11.2 検証

- `dotnet build creativevision10.slnx`：成功（0 warnings / 0 errors）。
- `TestServer` 89件・`TestLogin` 7件すべて成功（クライアント閉じの変更のため件数不変）。
- 実データは `TranHaibun` が0件のため、配分問合わせのマトリクスは空になる。引当・有効在庫は既存在庫で表示される。

### 11.3 完成度への影響

07Haibun の L0 が 20→17、L3 が 0→3 になった（対象画面数131は不変）。
4章「引当数・有効在庫を読む画面が0」の状態に、読む画面が3本できた。
残りは出荷指示確定(商品/得意先)・出荷処理入力（`ShippingDb` をMsg接続）と、
完了済み伝票の編集時ワーニング・在庫強制調整入力（10.5.3）。
