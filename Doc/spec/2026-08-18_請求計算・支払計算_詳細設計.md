# 請求計算・支払計算 詳細設計（2026-08-18）

対象HEAD: `074d6c0`（Doc/spec 整理）
対象: `SummaryUriSei`（請求残）/ `SummaryKaiShi`（支払残）の生成、請求計算 `BillingCalculation` / 支払計算 `PaymentCalculation` 画面
根拠: 判断材料 7章（決定済 E1〜E9・E8a〜g）、2026-08-18 ユーザー決定（E10 / E11 / E7 / E8-g）、
チェックリスト 4章（`SummaryUriSei` / `SummaryKaiShi` は「スキーマのみ・生成処理皆無」）。

## 1. 目的とスコープ

`SummaryUriSei` / `SummaryKaiShi` を生成する**請求計算・支払計算**を実装し、請求一覧表・支払一覧表・
月別入金/支払予定表・請求書印刷の元データを作る。**第1段階＝1得意先1締日**（`Shime1` のみ、
移行実データ全件 `Shime1=99`＝末日）を対象にする。入金・支払予定日は `PayMonth`/`PayDay` で決定する。

本設計の実装スコープ:
- スキーマ追加（請求書番号・入金/支払予定日）。
- 請求計算・支払計算ロジック（`SummaryDb`）と gRPC ストリーミング接続（既存の掛再更新と同型）。
- 請求計算 `BillingCalculation` / 支払計算 `PaymentCalculation` の実行画面（31Monthly、現状 L0）。

**スコープ外（別課題）**: 請求書印刷（帳票 qfm）、請求一覧表・支払一覧表の帳票実データ突合、最大3締日（第2段階）、
締日更新（J1＝1.0対象外）、親子請求の合算出力（親のみ／子含む）。

## 2. 前提（既存資産）

- 掛月次集計 `SummaryDb.CalcSummaryUriKake(fromYYYYMM, toYYYYMM)` / `CalcSummaryKaiKake` が実装済みで、
  内訳（`Uriage`/`Henpin`/`Nebiki`/`Cash`/`Fee`/`Densai`/`Offset`/`Other`）と繰越 `Balance`・当月残を計算する。
  ストリーミングは `Msg052_SummaryUriKake` / `Msg053_SummaryKaiKake`（`StockKakeUpdate` 画面が呼ぶ）。
- スキーマ（`CvBase/BaseDbKake.cs`）:
  - `SummaryUriKake`（売掛月次）: `Id_Tokui`・`DenMonth`(yyyyMM)＋内訳＋`Balance`/`TotalIn`/`TotalSales`。
  - `SummaryUriSei`（請求残）: `Id_Tokui`・`DenDay`(請求日)・`DayFrom`・`DayTo`（請求期間）＋**掛と同じ内訳列**。
  - 買掛側 `SummaryKaiKake` / `SummaryKaiShi` は `Id_Shiire`・`TotalOut`/`TotalShiire` で同型。
- 締日: 得意先は `MasterTokui.Shime1`、仕入先は `MasterShiire.Shime1`（`EnumShime`、99=末日）を使用する。
  `PayMonth`/`PayDay` は締日ではなく、入金・支払予定日の算出にのみ使用する（`PayMonth=0`＝当月、`PayDay=0/99`＝末日）。
- 税率は 1.0 では `MasterSysMan` の `No=1` の `Tax` を使用する。丸めは四捨五入で統一する。1.1以降は `MasterShohin` の税率区分を使用する。

### 2.1 掛月次集計の確定ルール

`SummaryUriKake` は自社の締日を基準とする。対象年月の `Balance` は、同得意先の対象年月以前の `(TotalIn − TotalSales)` 合計であり、内訳と合計列は対象年月分のみを保持する。

- `TotalSales = Uriage − Henpin − Nebiki + Tax`、`TotalIn = Cash + Fee + Densai + Offset + Other`。
- 売上側は `Tran00Uriage` の税抜 `Total` を集計し、`Uriage` は `Kubun` 10〜19・40〜89、`Henpin` は20〜29、`Nebiki` は30〜39とする。これら3列に `CalcFlag` は用いない。
- `Tax` は `Tran00Uriage.Tax` の合計であり、20〜29は `CalcFlag` により負値とする。
- 入金側は `Tran06Nyukin.JDetail.Code_Kin` の01 / 02 / 03 / 04 / 05を `Cash` / `Fee` / `Densai` / `Offset` / `Other` へ順に集計する。

`SummaryKaiKake` も自社の締日を基準とし、対象年月の `Balance` は対象年月以前の `(TotalOut − TotalShiire)` 合計とする。`TotalShiire = Shiire − Henpin − Nebiki + Tax`、`TotalOut = Cash + Fee + Densai + Offset + Other` とし、仕入側の区分・税・支払種別はそれぞれ `Tran03Shiire` / `Tran07Shiharai.JDetail.Code_Kin` に対して売掛側と同じ区分規則を適用する。

## 3. スキーマ変更（`UpdateDb` 26_08_18_02 以降）

| テーブル | 追加列 | 用途・根拠 |
|---|---|---|
| `SummaryUriSei` | `SeikyuNo TEXT`（請求書番号） | E10。得意先Id＋請求日(yyyyMMdd)＋連番2桁でユニーク。再発行は連番で識別 |
| `SummaryUriSei` | `Renban NUMBER`（連番） | 再発行・差し替えの世代。既定1。`SeikyuNo` は `{Id_Tokui}-{DayTo}-{Renban:00}` |
| `SummaryUriSei` | `NyukinYoteiDay TEXT`（入金予定日 yyyyMMdd） | 決定 7.0。月別入金予定表の基準 |
| `SummaryKaiShi` | `ShiharaiYoteiDay TEXT`（支払予定日 yyyyMMdd） | 決定 7.0。月別支払予定表の基準 |

`Tran12Jyuchu` の `NouhinDay` と同じ要領（`ALTER TABLE ... ADD COLUMN ... DEFAULT ''`／`0`、既存0件のため影響小）。
新規DBは `DefineDataTable` が属性から作る。

## 4. 請求計算ロジック（`SummaryDb.CalcSummaryUriSei`）

旧CV.netの「締日ごとに回す月末処理」（判断材料 7.1）に倣う。パラメータは**締日・請求月・得意先範囲**。

### 4.1 対象と期間（E2/E3、決定済）

- 対象得意先 = `MasterTokui.Shime1 = 指定締日` かつ 得意先CD範囲内。
- 請求期間 `DayFrom`〜`DayTo`:
  - `DayTo`（請求終了日）＝請求月の締日（`Shime1=99` なら月末日）。
  - `DayFrom` ＝前回締日の翌日（前月締日＋1日）。
  - `DenDay`（請求日）＝`DayTo`（**請求日＝請求終了日＝締日**、決定 E3）。

### 4.2 再計算の単位（E5、決定済）

締日＋請求月＋得意先範囲で**対象期間を DELETE → 再作成**（掛集計と同じ）。冪等で「通常更新値＝Rebuild値」を保つ。

### 4.3 内訳・残高の集計

請求期間内（`Tran00Uriage.KakeDay` が `DayFrom`〜`DayTo`）の売上と、`Tran06Nyukin` の入金を得意先単位に集計する。金額列は税抜の `Total` を用い、`Uriage`/`Henpin`/`Nebiki` は `CalcFlag` を使用しない。

| 列 | 集計規則 |
|---|---|
| `Uriage` | `Tran00Uriage.Kubun` が 10〜19、40〜89 の `Total` 合計 |
| `Henpin` | `Tran00Uriage.Kubun` が 20〜29 の `Total` 合計 |
| `Nebiki` | `Tran00Uriage.Kubun` が 30〜39 の `Total` 合計 |
| `Tax` | `Tran00Uriage.Tax` の合計。区分20〜29は `CalcFlag` によりマイナスとして集計 |
| `Cash` / `Fee` / `Densai` / `Offset` / `Other` | `Tran06Nyukin.JDetail.Code_Kin` が順に 01 / 02 / 03 / 04 / 05 の金額合計 |

- `TotalSales = Uriage − Henpin − Nebiki + Tax`。
- `TotalIn = Cash + Fee + Densai + Offset + Other`。
- `Balance` は対象期間以前の同得意先 `SummaryUriSei` の `(TotalIn − TotalSales)` 合計に、対象期間の `(TotalIn − TotalSales)` を加えた累計残とする。1レコードの内訳・合計値は対象期間分のみ保持する。

### 4.4 消費税

- `Tax` は対象取引の `Tax` を集計する。返品（区分20〜29）は `CalcFlag` を用いて負値とする。
- 1.0で新たに税額を算出する必要がある場合は、`MasterSysMan` の `No=1` の `Tax` を税率に用い、外税・四捨五入で統一する。1.1以降は `MasterShohin` の税率区分を使用する。

### 4.5 請求書番号（E10、2026-08-18 決定）

- `Renban` 既定1、`SeikyuNo = {Id_Tokui}-{DayTo:yyyyMMdd}-{Renban:00}`。得意先Id＋請求日＋連番2桁でユニーク。
- 通常の再計算と Rebuild は既存の `Renban` / `SeikyuNo` を維持して DELETE→再作成し、冪等にする。明示的な再発行・差し替え時のみ `Renban` を＋1して新採番する。

### 4.6 入金予定日（7.0）

`NyukinYoteiDay`＝`MasterTokui.PayMonth`/`PayDay` から算出（`PayMonth=0`＝当月、`PayDay=0/99`＝末日。E2'）。月別入金予定表の基準。

## 5. 支払計算ロジック（`SummaryDb.CalcSummaryKaiShi`）

請求計算の仕入先版。集計元は `Tran03Shiire`（仕入）/`Tran07Shiharai`（支払）、締日は `MasterShiire.Shime1` を使用する。`PayMonth`/`PayDay` から `ShiharaiYoteiDay` を算出する。請求書番号は持たない（支払側は不要）。

支払期間内の `Tran03Shiire` / `Tran07Shiharai` を仕入先単位に集計する。`Shiire` は `Kubun` 10〜19・40〜89、`Henpin` は20〜29、`Nebiki` は30〜39の各 `Total` 合計（いずれも `CalcFlag` は使用しない）とする。`Tax` は取引の `Tax` を集計し、20〜29は `CalcFlag` により負値とする。`Cash` / `Fee` / `Densai` / `Offset` / `Other` は `Tran07Shiharai.JDetail.Code_Kin` の01 / 02 / 03 / 04 / 05を順に集計する。

- `TotalShiire = Shiire − Henpin − Nebiki + Tax`。
- `TotalOut = Cash + Fee + Densai + Offset + Other`。
- `Balance` は対象期間以前の同仕入先 `SummaryKaiShi` の `(TotalOut − TotalShiire)` 合計に、対象期間の `(TotalOut − TotalShiire)` を加えた累計残とする。1レコードの内訳・合計値は対象期間分のみ保持する。

## 6. E7 親子締日チェック（2026-08-18 決定）

請求計算の実行前に、対象得意先の `Shime1` と請求先（親、`Id_Paysaki`）の `Shime1` が食い違う得意先を検出する。
- 現状 `Id_Paysaki` 全件0のため検出0件。**不一致があれば警告**し、「マスタ変更および請求再計算が必要」である旨をメッセージ表示する（**ブロックはしない**）。

## 7. サーバ接続（新規 Msg、ストリーミング）

掛再更新（`Msg052/053`）・棚卸（`Msg054/055`）と同型。`CvFlag` に `Msg056_SummaryUriSei` / `Msg057_SummaryKaiShi` を追加し、
`QueryMsgStreamService` へ結線。パラメータは請求月・締日・得意先(仕入先)範囲を持つ新 `BillingParameter`。
`CalcSummaryUriSei` / `CalcSummaryKaiShi` を Serializable トランザクションでストリーミング実行する。

## 8. 画面（31Monthly）

- `BillingCalculation`（請求計算）: 締日（`MasterTokui.Shime1` の実データから動的リスト、判断材料 7.1）・請求月・得意先CD範囲・実行/キャンセル・進捗・
  親子締日チェックの警告表示。`BaseStocktakeViewModel` と同型のストリーミング実行（`Msg056`）。
- `PaymentCalculation`（支払計算）: 仕入先版（`Msg057`）。締日は `MasterShiire.Shime1` の実データから動的に作成する。

## 9. Rebuild 対象への追加（E6）

在庫・掛再更新（`StockKakeUpdate`）に請求残・支払残の再作成も乗せられるよう、`CalcSummaryUriSei`/`CalcSummaryKaiShi` を
Rebuild 経路からも呼べるようにする（当面は請求計算画面から明示実行で足りる。段階実装）。

## 10. 受入確認（Given/When/Then）

- G/W/T-1: 締日=末日・請求月・得意先範囲で請求計算 → 対象得意先ごとに `SummaryUriSei` が1行でき、`DenDay=DayTo=月末`、
  内訳・消費税（外税四捨五入）・繰越・`SeikyuNo`（`{Id_Tokui}-{yyyyMMdd}-01`）が入る。
- G/W/T-2: 同条件で再実行 → 対象期間 DELETE→再作成で冪等。再発行時は `Renban` が＋1。
- G/W/T-3: 売上は区分10〜19・40〜89、返品は20〜29、値引は30〜39へ集計され、`TotalSales = Uriage − Henpin − Nebiki + Tax` となる。
- G/W/T-4: 得意先と親の締日が異なる → 警告が出る（ブロックしない、E7）。
- G/W/T-5: 支払計算で `SummaryKaiShi` が仕入先ごとにでき、`MasterShiire.Shime1` で期間を判定し、`PayMonth=0/PayDay=0` は当月末の `ShiharaiYoteiDay` が入る。
- G/W/T-6: `Balance` は請求・支払とも対象期間までの `(TotalIn/TotalOut − TotalSales/TotalShiire)` 累計であり、行内の内訳値は対象期間分のみである。

## 11. テスト（TestServer）

`SummaryDbTests` に請求計算のテストを追加する。
- 売上・返品・値引・入金を投入 → `CalcSummaryUriSei` で内訳・売上合計・繰越・消費税（四捨五入外税）が期待値。
- 区分別売上・仕入、`Code_Kin` 別入金・支払、累計 `Balance` が期待値になる。
- DELETE→再作成の冪等性、通常再計算時の採番維持、明示的再発行時の `Renban` 採番。
- 支払計算（`CalcSummaryKaiShi`）の同等ケースと、`MasterShiire.Shime1` による期間判定、`PayMonth=0/PayDay=0`＝当月末。

## 12. 実装順（段階）

1. スキーマ追加（`SeikyuNo`/`Renban`/`NyukinYoteiDay`/`ShiharaiYoteiDay`）＋マイグレーション。
2. `SummaryDb.CalcSummaryUriSei`（掛集計を土台に締日・期間・請求書番号・確定税へ拡張）＋テスト。
3. `CalcSummaryKaiShi`（支払版）＋テスト。
4. `Msg056/057` 追加・`QueryMsgStreamService` 結線・`BillingParameter`。
5. `BillingCalculation` / `PaymentCalculation` 画面（`BaseStocktakeViewModel` 同型）＋親子締日チェック警告。
6. Rebuild 経路への組み込み（E6、任意）。

各段階を design→implement→log→commit で刻む。請求書印刷・帳票突合・最大3締日は後続。
