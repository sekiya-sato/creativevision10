実装前に決めるべき項目を、決めないと先へ進めない順に並べます。カッコ内は本書での採番です。

## 第1階層 — これが決まらないと §3 の設計が確定しない

**1. 諸掛の対応キーは何か（§3.7）**
実データで `ManualNo` の突合が0件だった以上、次のどれかを確定させる必要があります。判断材料は旧Oracle実データの調査（Step 1）です。

| 分岐 | 決まること |
|---|---|
| 旧CV.netでは運用されていた → `ConvertDbTran` の変換漏れ | §3はそのまま。変換側の不具合として別起票 |
| 旧CV.netでも未運用。別キーで対応付けていた | **§3全体を再設計**。対応キー・配賦単位・エラー条件がすべて変わる |
| 今後の運用で入力する新規キー | 仕入入力画面の必須化・重複チェック・運用手順が本書の範囲に増える |

3番目の分岐を引いた場合、諸掛更新は「実データが存在しない機能」を作ることになるため、10.1に含めるかどうかから再考の余地があります。

## 第2階層 — DB定義を凍結（Step 3）する前に決める

**2. `TranGenka` の名前と、評価替え登録の扱い（U-12）**
同名の空スタブが「原価変更伝票・評価区分・セールCD・OFF率」という別機能で予約済み。改名して旧構想を残すか、この名前を使って評価替え登録を10.1対象外と明記するか。*推奨: 改名（`TranGenkaRireki` 等）。旧構想は業務要件として実在する可能性があり、名前を潰すと検討が消える。*

**3. `SysCostMonthState` を作るか（U-13）**
既存の月次再計算は状態を永続化しない流儀。作らなければテーブルが1つ減り、状態と実データのズレも消えます。*推奨: 作らない。*

**4. 消化仕入の対応を専用テーブルにするか（U-10）**
`RelateNo1 = 元伝票Id` の既存規約で代用するか、明細単位対応と `SourceVdu` のために新設するか。*推奨: 新設（本書の現行案）。*

**5. 実行履歴の記録先（U-14）**
`SysHistAutoexec` はスケジューラ専用で対象月列もなく、手動実行を混ぜると既存の自動実行履歴画面の意味が変わります。列追加／別テーブル／状態テーブルで代用の三択。U-13と連動するので同時に決めてください。

**6. 10.1 の配布対象DBはSQLiteのみか**
既存テーブルへの列追加はSQLite専用経路しかありません。非SQLiteが対象なら `UpdateDb` の方言対応が前提作業として増えます。承認事項に入っていない見落としです。

**7. `Id_*` 命名からの逸脱を許容するか（U-11）**

## 第3階層 — 業務ルールの決定

**8. 原価0円商品の扱い（U-15）** — エラー1件で当月全件が止まる設計なので、初回稼働で詰まる可能性があります。Step 1 で該当件数を出してから判断。

**9. 生地付属仕入は生涯1回だけ配賦か（U-16）** — 当初案は多重配賦になるバグなので、本書では修正済みとして書きました。**追認が必要です。**

**10. 支払計算済み期間の消化仕入再実行** — 当初は「画面に表示」でしたが、確定済み支払額が変わるため「更新中断」に変更しました。**これも私の判断で書き換えたので追認が必要です。**

**11. 諸掛の按分方法1を残すか** — 方法2と差額調整後はほぼ同結果。旧CV.netでの利用実績を見て一本化の可否を判断。

**12. 総平均の `floor` を旧CV.net仕様の踏襲としてよいか** — 本書には「旧仕様踏襲」と書きましたが、旧仕様書での裏付けは未確認です。Step 1 で確認してください。

**13. `CostMethod=0`（固定原価）のときの諸掛更新** — 原価更新が実行不可なので、配賦結果を消費する処理が存在しません。走らせる意味があるか、実行不可にすべきか。**本書に未記載の論点です。**

**14. `PurchaseType` の値が 0 と 3 で 1・2 が欠番の理由** — 旧CV.net互換なら由来を注記、そうでなければ 0/1 に。

## スコープに関わる判断

**15. 確認/更新の二段階プレビューを新設するか**
31Monthly の既存画面（請求計算・支払計算）はすべて「警告→Yes/No→即更新」の単一フローで、一覧を伴う二段階の前例がありません。新設するなら4画面ぶんの実装量が増えます。既存流儀に合わせる選択肢もありますが、原価更新は影響範囲が広いので二段階の価値はあると考えます。

**16. プロセス間排他をどう実装するか**
「4処理の同時実行禁止」「Scheduler競合時の中断」は既存に機構がありません。DB上のロック行／`CvFlag` 単位の排他／アプリケーションロックのいずれか。Step 8 の実装量に直結します。

---

このうち **1・2・3・5・6** が最も影響が大きく、他は後から変えても手戻りが小さい部類です。1 は調査結果待ちですが、2・3・5・6 は今すぐ判断できます。