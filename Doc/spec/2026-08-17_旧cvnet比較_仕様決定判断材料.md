# 旧CVnet比較 — 優先確定仕様の判断材料（2026-08-17）

| 項目 | 内容 |
| --- | --- |
| 対象HEAD | `9833f24` |
| 仕様状態 | **DRAFT**（4章G0・G0'・G4、4.3、5.2 I1、7.2 E8・E9 は本日ユーザー決定済み） |
| 比較元（旧CV.net） | `refer/cvnet-knowlege/` — 業務フロー18点 / マニュアル14点 / DB定義書2点 / **帳票集PDF14点** |
| 比較元（会議） | `refer/cv10meeting/` — 第0回・第1回・修正第1回（7者）・新規機能3者会議×2 |
| 補助 | `C:\gitroot\cv\cvnet_pkg`（旧CV.netソース一式） |

---

## 0. 本書の位置づけ

修正第1回7者会議 **Decision 4** に従い、本書はAIが業務仕様を決定しない。
未確定事項は「判断材料不足 / 仕様未確定」として提示し、最終判断はユーザーに委ねる。
ユーザーが決定した項目は **決定済** と明記し、根拠と派生論点を併記する。

同 **Decision 7** は本日の検討対象と重なる8項目を「本会議では決定しない」としている。
本書はその8項目に旧CV.net資料の裏付けを与え、APPROVED へ送れる状態にすることを目的とする。

> Decision 7（未決定）: `SummaryUriKake` / `SummaryKaiKake` 詳細、請求計算・支払計算、
> 引当対象 `EnumHaibun`、引当解除タイミング、`SummaryRealStock.ReserveQty`、
> 調整数の現在値保持方式、納品予定日保持粒度、発注・受注 `EndFlag` の取消 / 監査

### 0.1 比較の軸

第0回会議 合意事項3 の5区分で整理する。**KEEP** 旧仕様を維持 / **CHANGE** 意図的に変更 /
**REPLACE** 別方式で同じ業務目的を達成 / **DROP** 廃止 / **LATER** 初回Release後へ延期。

DROP と LATER は開発側だけで判断せず、Sales・System・Architect を含めて理由と代替手段を記録する。
また cvnet-developer の要求（修正第1回 3.1）に従い、**旧CV.net資料の確認なしに DROP / LATER を決定しない**。
本書は全論点についてその確認を済ませてある。

### 0.2 旧の内部仕様は踏襲しない

第1回会議で cvnet-developer が述べたとおり、継承すべきは画面配置や内部構造ではなく業務能力である。
本書で旧の列名に言及するのは、その存在が**機能の存在を証明する**場合に限る。

---

## 1. 会議合意から来る制約

| 出典 | 制約 |
| --- | --- |
| 第0回 合意事項1 | 旧CV.netの完全コピーは完成条件ではない。機能カバー率**おおむね80%以上**、Release対象の必須業務シナリオは**100%完遂** |
| 第0回 代表シナリオ | 商品登録 → 発注 → 仕入 → 在庫 → 配分・移動 → 売上 → 売掛 → 入金 → 消込 → 締め |
| 第1回 3章 | 1.0必須機能は原則 **L5**（E2E検証済み）を Release Gate とする |
| 修正第1回 一致点5 | 「通常更新値 = Rebuild値」を重要品質原則とする |
| 修正第1回 一致点4 | **L0画面数を減らすこと自体を進捗KPIにしない** |
| 修正第1回 3.2 | 請求計算は画面を作るだけでは済まず、取引先・請求先・締日・計上期間・請求日・入金・繰越残の**業務モデルを先に確定**する |
| 第1回 3.2（Architect） | 単独フラグだけで業務状態を説明せず、**未処理・一部処理・完了・取消・再開**の状態遷移を仕様として定義する |
| 修正第1回 3.5（Sales） | 消込・発注残完了・受注残完了・**引当解除**・配分・出荷・棚卸確定・請求・支払は実ユーザー操作でMini-UATする |
| 修正第1回 Decision 3 | 可能な仕様は **Given / When / Then** へ変換する |

### 1.1 Phaseスケジュール（第1回5章 / 修正第1回7章）

| Phase | 時期 | 範囲 | 本書の該当論点 |
| --- | --- | --- | --- |
| Phase 1 | 8〜9月 | 基盤・仕様。引当、有効在庫、状態遷移、トランザクション、Rebuild設計 | **I1**（決定済）、全論点の仕様確定 |
| Phase 2 | 9〜10月 | 発注→仕入→在庫、発注残、返品、買掛発生、消化仕入 | **G群**（決定済）、**H群** |
| Phase 3 | 10月 | 受注→引当→配分→移動・出荷、受注残、**解除・取消時の復元** | **I2〜I7**、**G4**（決定済） |
| Phase 4 | 11月 | 売上→売掛・買掛→請求・入金・支払、元帳、**親子請求**、**税計算** | **E群** |
| Phase 5 | 11〜12月 | 棚卸、締日、在庫・掛再更新、最終仕入原価、総平均原価、評価替、月間集計 | **F群**、**J群** |
| Phase 6〜8 | 12月〜1月 | 帳票・権限、結合・障害、UAT・移行。1月下旬 Release 候補 | 本書対象外 |

**優先順位はこのPhase順に従う。** ただし修正第1回3.2により、請求計算は実装（Phase 4）に先立ち仕様確定を前倒しする。

### 1.2 新規機能3者会議の扱い

第1回・第2回の3者会議（Analyzer、Cockpit、Trace Explorer、Mobile 等10候補）は
いずれも **IDEA（DRAFT以前）** であり、1.0 P0 Release Gate に追加されていない。本書の対象外とする。

---

## 2. 資料の読み取り可否

| 資料 | 状態 |
| --- | --- |
| 業務フロー `.pptx` 18点 | **全文抽出済み** |
| マニュアル `.pdf` 14点 | Ghostscript（WSL2）で抽出可。本書では 11-月次更新 を使用 |
| DB定義書 `.pdf` 2点 | 全文抽出済み。機能の存在証明としてのみ使用 |
| **帳票集PDF 14点** | **全文抽出済み（2026-08-17 追加）**。各帳票の「3ページ目以降：出力内容の説明」に**算出式が明記**されており、本書の残・請求・原価の定義はここを一次根拠とする |
| 会議議事録 `.docx` 5点 | 全文抽出済み |
| 帳票集 `.xls` 16点 | **未読**。Excel未導入・変換ライブラリ無し。ただし帳票集PDF が同一構成のため、`帳票別計算式一覧.xls` を除き実質的に代替できている |

補足: 第0回・第1回議事録は旧CV.net一次資料として Google Drive を指定しているが、
当該フォルダは現在の接続アカウントから参照できない。本書は同等以上の内容を持つ
ローカル `refer/cvnet-knowlege/` を一次資料として使用した。
また第1回議事録は本文の開催日（8月13日）とファイル名の日付（2026-08-14）が食い違う。

### 2.1 用語集からの確認事項（帳票集PDF〔００〕）

旧の一覧取得用語のうち、本書に関係するもの。

| 用語 | 定義 |
| --- | --- |
| 請求 | 請求の対象の得意先（**子の請求先は表示されません**） |
| 支払 | 支払の対象の仕入先（子の支払先は表示されません） |
| 請求締 / 支払締 | **指定の締め日の請求先 / 支払先** |
| 倉庫 | 倉庫、自社倉庫扱いの得意先 |
| 移動倉庫 | 倉庫、売仕店、直営店、在庫管理を行う卸先 |
| 店舗 | 売仕店、直営店、在庫管理を行う卸先 |
| 得意先 | **卸先と売仕店** |
| 全得意先 | 卸先、売仕店、直営店 |

「得意先＝卸先と売仕店」という定義は、G4 の決定（出荷対象を `TenType=1`(卸先) と `3`(売仕店) とする）と一致する。

---

## 3. ガバナンス上の要確認事項

**引当（`ReserveQty`）は、APPROVED を経ずに実装されていた。**

| 日付 | 出来事 |
| --- | --- |
| 2026-08-15 | `05e8263` で `SummaryRealStock.ReserveQty` / `SummaryStock.ReserveQty` と `TranHaibun.EndFlag` を実装 |
| 2026-08-15 | 修正第1回7者会議 **Decision 7** が「引当対象 `EnumHaibun`」「引当解除タイミング」「`SummaryRealStock.ReserveQty`」を**未決定**と記録 |
| 2026-08-16 | チェックリスト 5章A-1 が引当モデルを「確定・実装済み」と記載 |
| **2026-08-17** | **ユーザーが I1 を決定（5.2節）。実装内容の変更が必要になった** |

第0回 合意事項4「重要業務仕様は APPROVED 前に Developer が独断で確定しない」との齟齬は、
本日の I1 決定をもって解消する。チェックリスト5章A-1 の記載は I1 決定に合わせて更新が必要である。

---

## 4. 発注・受注残の完了（Phase 2〜3 / Decision 7-8）

### 4.1 旧CV.netの業務機能

`業務フロー/02_受注・発注`、`帳票集PDF〔０３〕発注`、`〔０５〕受注` より。

- 受注・発注とも完了状態で残を管理し、**完納・全量配分した時点で自動的に「未完了→完了」へ切り替わる**。
  発注入力の説明に「完納されると未完了から完了に切り替わります」、受注に「受注分、全てを受注配分した時点で『完了』となります」。
- **受注残完了設定 / 発注残完了設定**は、その自動判定に対する**手動の一括上書き**である。
- 完了の効果は2つ。①受注残管理表の「残のみ」出力に現れない ②受注配分入力で検索されない。
- 旧は完了を**伝票明細の行単位**で管理していた（受注データの明細ごとの残管理の完了FLG）。
- 発注残・受注残の各帳票に **表示区分「残のみ / 全て」** がある。

#### 4.1.1 帳票が定義する残の算式（一次根拠）

**発注残管理表**（〔０３〕発注）

| 項目 | 定義 |
| --- | --- |
| 発注数 | 発注データの数量 |
| 納品数 | **仕入データの数量**。※ **発注伝票NOとの紐付けが必要** |
| 残数 | 発注数 − 納品数 |
| 残金額 | 発注金額 − 納品金額 |
| 納期予定 | **発注データの納品日** |

**受注残管理表**（〔０５〕受注）

| 項目 | 定義 |
| --- | --- |
| 受注数 | 商品色サイズごとの受注数 |
| 納品数 | 指定条件の受注データと紐づいた**出荷売上数量**。※ 出荷売上データの**受注Noに受注伝票Noが登録されていないものは対象外**。※ 出荷売上データは**出荷日の指定範囲内の数量のみ**を対象 |
| 残数 | 受注数 − 納品数 |
| 残金額 | 受注金額 − 納品金額 |
| 納期予定 | **受注データの納品日** |

別途「商品ごとに受注数・納品済数・受注残数を集計する簡易的な受注残管理表」もあり、そちらも `受注残数 = 受注数 − 納品済数` である。

### 4.2 論点と決定

| # | 論点 | 区分 | 内容と根拠 |
| --- | --- | --- | --- |
| **G0** | 完了の保持粒度 | **決定済 2026-08-17 / CHANGE** | **伝票単位とする。** 旧は明細行単位だったが、CV10 は明細を `Jmeisai`（JSON）に持ちSQLで絞れないため伝票単位へ変更する。1品番だけを完了にする運用は失われる |
| **G0'** | 完了の立て方 | **決定済 2026-08-17 / KEEP** | **完了は自動で立てるが、手動で立てる（解除する）ことも可能とする。** 旧の「自動完了＋残完了設定による強制上書き」を機能として維持する |
| **G4** | 受注残の定義 | **決定済 2026-08-17 / KEEP** | **受注残 = 受注 − 出荷。** 出荷は**受注伝票Idで紐付けられた `Tran00Uriage`** のうち **`TenType=1`(卸先) または `3`(売仕店)** を対象とする。集計対象の受注は**完了になっていないもの**に限る。**配分側の残は「引当数」で見る**（別の残概念を作らない）。旧の帳票定義（受注Noで紐付いた出荷売上数量）と一致する |
| **G2** | 完了の取消可否 | **決定済 2026-08-17 / KEEP** | 旧も未完⇔完了を何度でも変更できた。`PartialUpdateParam` を流用する。これが2件目の適用となるため、ドラフト5.5（禁止列リストを属性方式へ移すか）も同時に決着する |
| **G3** | 監査列 | **決定済 2026-08-17 / KEEP** | 旧も完了状態に監査列を持たない。追加しない |

#### 4.2.1 G0（伝票単位化）に伴う派生論点（決定済 2026-08-17）

| # | 派生論点 | 決定 |
| --- | --- | --- |
| **G0-a** | 「残のみ」の定義 | **完了を除外したうえで、明細残を出す。** 除外判定は伝票単位、残数の算出は明細単位で行う |
| **G0-b** | 部分完了伝票の扱い | **伝票ごと完了にしたものは、SKUに残があっても完了とみなす。** 旧は明細単位で残ありSKUが残ったが、CV10 では消える |
| **G0-c** | 自動完了の判定式 | **明細単位で全SKUが充足されたものを自動完了とする。** 伝票合計数量での比較は採らない |

G0-c により、1SKUでも未充足なら伝票は「一部処理」に留まる。
G0-b は運用上、残を残したまま締めたいときの手段（G0' の手動完了）として機能する。

### 4.3 状態遷移（決定済 2026-08-17）

第1回 3.2（Architect）の要求に対応する。

**発注**

| 状態 | 条件 | 遷移 |
| --- | --- | --- |
| 未処理 | `EndFlag=0`、仕入 0 | 発注伝票の登録 |
| 一部処理 | `EndFlag=0`、仕入 一部 | 関連伝票NO1で紐付いた仕入伝票の登録 |
| 完了（自動） | `EndFlag=0` の状態で、**仕入が全て発注数量を超えた → `EndFlag=1` へ変更** | 仕入伝票の登録・修正 |
| 完了（強制） | 発注残完了設定画面で `EndFlag` を更新 | 手動 |

**受注**

| 状態 | 条件 | 遷移 |
| --- | --- | --- |
| 未処理 | `EndFlag=0`、出荷 0 | 受注伝票の登録 |
| 一部処理 | `EndFlag=0`、出荷 一部 | 受注伝票Idで紐付いた出荷（`Tran00Uriage`、`TenType=1` or `3`）の登録 |
| 完了（自動） | `EndFlag=0` の状態で、**出荷が全て受注数量を超えた → `EndFlag=1` へ変更** | 出荷伝票の登録・修正 |
| 完了（強制） | 受注残完了設定画面で `EndFlag` を更新 | 手動 |

**この `EndFlag` は `Tran13Hachu` / `Tran12Jyuchu` へ新設する列である**（4.4）。
発注残・受注残の完了判定は、必ずこの新設 `EndFlag` を参照する。

「未処理／一部処理」は `EndFlag=0` の内部区分であり、独立した永続値を持たない。
帳票上は残数の有無で区別される。自動完了の判定は G0-c により**明細単位で全SKU充足**を条件とする。

派生論点として、**仕入・出荷の削除や数量減で `EndFlag=1` から戻すか**が未決である。
Phase 3 の範囲に「解除・取消時の復元」が明記されているため、そこで決着させる。

### 4.4 スキーマ変更（決定済 2026-08-17）

本日の決定で必要になる列を整理する。`CvBase/UpdateDb.cs` の `versions` へ **1行**追加し、
複数の `ALTER` を `;` 区切りで記述する（`26_08_14_01` / `26_08_15_01` と同じ書式）。

| テーブル | 列 | 用途 | 状態 |
| --- | --- | --- | --- |
| `Tran13Hachu` | `EndFlag` | 発注残の完了（4.3） | **新設** |
| `Tran12Jyuchu` | `EndFlag` | 受注残の完了（4.3） | **新設** |
| `TranHaibun` | `ShortSu` | 欠品数（5.1.2） | **新設** |
| `TranHaibun` | `EndFlag` | 配分の完了 | **既存**。`26_08_15_01` で追加済みのため ALTER 不要 |

```
new (26_08_17_01,
 "ALTER TABLE Tran13Hachu ADD COLUMN EndFlag NUMBER not null default 0;"
+"ALTER TABLE Tran12Jyuchu ADD COLUMN EndFlag NUMBER not null default 0;"
+"ALTER TABLE TranHaibun ADD COLUMN ShortSu NUMBER not null default 0;",
 "発注・受注の完了フラグと配分の欠品数を追加 既存伝票は未完了(0)・欠品0"),
```

既存データへの影響は無い。移行実データは発注1件・受注1件・配分0件である。

---

## 5. 配分・引当（Phase 1〜3 / Decision 7-3・7-4・7-5）

### 5.1 旧CV.netの業務機能

`業務フロー/03_配分・出荷`、`帳票集PDF〔０７〕配分` より。配分は4段階。

1. **配分入力** — 受注配分入力／店舗配分入力／在庫品配分／店舗出荷依頼の4系統。いずれも「配分可能数（有効在庫数）」を基準に入力する。
2. **出荷指示確定**（商品別／得意先別の2画面） — ここで確定したものが倉庫への指示になる。
3. **出荷指示明細書印刷** — ピッキングリスト。**配分伝票NO単位で「1出庫元 ⇒ 1出荷先」**にまとまる。
   なお帳票仕様に「配分データの**完了FLG及び確定FLGは参照しておりません**」と明記がある。
4. **伝票作成** — ハンディ読取→取込、**または「出荷処理入力」メニュー**から。ここで初めて在庫が動く伝票が生成される。
   生成先は出荷先の店種区分で分岐し、**卸先 → 出荷売上伝票 / 倉庫・売仕店・直営店 → 移動伝票**。

#### 5.1.1 配分データが持つ数量と完了判定（帳票集PDF〔０７〕）

**出荷指示一覧印刷**の出力項目定義より。

| 数量 | 定義 |
| --- | --- |
| 予定数量 | 配分入力での配分数 |
| 確定数量 | 配分数に対して**出荷した数量** |
| 欠品数量 | 配分数に対しての**欠品数** |

同帳票の検索条件「出荷依頼倉庫完了」が、完了の判定式をそのまま示している。

> **0未完了 ： 指示数 > 出荷数 + 欠品数**
> **1完了済 ： 指示数 <= 出荷数 + 欠品数**

つまり旧では、配分は**出荷されるか欠品として確定するかのどちらかで解消**され、その両方が指示数に届いて初めて完了になる。
検索条件にはこのほか「CV出荷確定登録（0未確定 / 1確定済み）」があり、**確定と完了は独立した2軸**である。

#### 5.1.2 CV10における配分の数量と状態（決定済 2026-08-17）

旧の3数量に対応させ、`TranHaibun` へ **`ShortSu`（欠品数）を新設**する（4.4）。

| CV10 列 | 対応 | 設定者 |
| --- | --- | --- |
| `Su` | 予定数量（指示数） | **ユーザーが配分入力で設定** |
| `JitsuSu` | 確定数量（出荷した数量） | **倉庫から戻されるデータで設定** |
| `ShortSu` | 欠品数量 | **倉庫から戻されるデータで設定**（新設） |
| `KakuteiDay` | 確定日 | 確定処理 |
| `EndFlag` | 完了 | 完了処理（既存列） |

**状態の定義**

1. ユーザーが `Su` を入力する。
2. そのデータが倉庫へ送信される。
3. 倉庫側から戻されるデータにより `JitsuSu` と `ShortSu` が設定される。**`Su = JitsuSu + ShortSu`** が成立する。
4. その状態であり、かつ **`KakuteiDay` に有効な日付が設定された**データを**確定**とみなす。
5. **完了は `EndFlag = 1`。**

旧の完了判定「指示数 ≤ 出荷数 + 欠品数」は、CV10 では `Su = JitsuSu + ShortSu` の成立として表現される。
これにより、出荷できなかった分は欠品として解消でき、`EndFlag` の手動更新だけに頼る必要がなくなる。

### 5.2 引当対象（決定済 2026-08-17）— 重要な仕様変更

#### 5.2.1 旧CV.netの定義

旧資料は4スライドで同一の定義を繰り返している。

> 有効在庫数 … 実在庫から、指示した分を差し引いた数。「実在庫 − 配分入力（**初回配分は除く**）」

さらに受注配分入力・店舗配分入力の注意書きに、

> 区分「初回配分」「在庫配分」で分けることができます。注意：発注入力を元に配分入力ができます。
> **初回配分は引き当てがかからない（有効在庫数が減らない）ので、在庫のある商品は必ず在庫配分を選択してください。**

補強材料として、第1回会議7章で cvnet-developer は
**「入荷前の初回配分」**を、汎用ERPでは追加設計が必要になるアパレル固有業務の代表例として列挙している。

#### 5.2.2 決定内容（決定済 2026-08-17）

**引当対象は `EnumHaibun = 0`（初回配分）**以外**のすべてとする。**
すなわち引当判定は **`Kubun != 0`** の一点で行い、`0:初回配分` のみが引当対象外となる。

区分の意味づけは **KEEP**（旧の「初回配分は引き当てがかからない」を維持）、
引当判定のロジックは **CHANGE**（2026-08-15 実装の「全区分を引当対象」から変更）である。
`TranHaibun` は実データ0件のため、移行コストは発生しない。

#### 5.2.3 区分ごとの扱い

現行 `EnumHaibun`（`CvBase/BaseDbHaibun.cs`）の8区分に対する判定は次のとおり確定する。

| 値 | 名称 | 引当 |
| --- | --- | --- |
| 0 | 初回配分 | **対象外**（入荷前の振り分けであり現物を押さえない） |
| 1 | 在庫配分 | 対象 |
| 2 | 受注配分 | 対象 |
| 3 | 得意先別配分 | 対象 |
| 4 | 店舗出荷依頼 | 対象 |
| 5 | 在庫品配分 | 対象 |
| 6 | 取置 | 対象 |
| 7 | 移動指示 | 対象 |

区分値を2つへ集約する必要はなく、**8区分を維持したまま判定式を `Kubun != 0` にすればよい**。
これにより「どの画面から登録された配分か」も区分から判別でき続ける。

コード上で `EnumHaibun` の値を参照しているのは `HachuHaibunInputViewModel`（`Hatsukai`）と
`ShopHaibunInputViewModel`（`Hatsukai` / `Zaiko`）の2ファイルのみで、いずれも 0 と 1 しか使っていない。
変更が必要なのは `SummaryDb.CalcHaibun2Reserve()` / `CalcReserveQtyAll()` の集計条件である
（現行は `EndFlag=0` のみで絞っており、`Kubun != 0` の条件が無い）。

なお旧の店舗出荷依頼は「本部で出荷確定をするまでは倉庫の有効在庫数は減りません」とされていた。
本決定では `4:店舗出荷依頼` も引当対象とするため、この点は旧に対する **CHANGE** となる。

### 5.3 残る論点

| # | 論点 | 区分候補 | 判断材料 |
| --- | --- | --- | --- |
| **I2** | **引当解除のタイミング**（Decision 7-4） | 要判断 | 旧の完了判定は **指示数 ≤ 出荷数 + 欠品数**（5.1.1）。CV10 は `EndFlag=1` のみで、**欠品数の概念が無い**。欠品を持たないなら「出荷しない残りをどう解消するか」の手段（手動完了・一括完了）が必須になる。Phase 3 の「解除・取消時の復元」に直結 |
| **I3** | 配分確定の主体と段階 | **KEEP** | 旧は「出荷指示確定」→「伝票作成」の2段階で、**確定と完了は独立2軸**（5.1.1）。CV10 は `KakuteiDay` と `EndFlag` で構造上表現できる。どの画面がどちらを立てるかが未定 |
| **I4** | 生成する伝票の種類 | **決定済 2026-08-17 / KEEP** | 出荷先の `MasterTokui.TenType` で分岐する。**出荷売上（`Tran00Uriage`）= `TenType` 1(卸先) または 3(売仕店)**、**移動 = `TenType` 0(倉庫) または 6(直営店)**。G4 の出荷判定と同一基準になる |
| **I5** | 出荷指示の単位 | **決定済 2026-08-17 / REPLACE** | `TranHaibun` は明細行単位のままとし、**`DenDay` + `Id_Soko` + `Kubun` + `RelateNo1` を単位としてまとめる（仮想のヘッダ）**。旧の配分伝票NO（1出庫元⇒1出荷先）に相当する括りを、物理的なヘッダ行を作らずキーの組で表現する |
| **I6** | ハンディ無し運用 | **決定済 2026-08-17 / DROP** | **ハンディは無し。** 旧の主経路（ハンディ読取→取込）は採らず、「出荷処理入力」相当の画面（`ShippingInput`）に一本化する。倉庫から戻る `JitsuSu` / `ShortSu` の設定経路がハンディの代替になる（5.1.2） |
| **I7** | 未出荷指示の検出と一括完了 | **KEEP 推奨** | 旧は「確定済かつ未完了」で滞留を検出し、配分データメンテで一括完了できた。**CV10 は現在 `EndFlag` を立てる画面が1つも無く、登録した引当が永久に解除されない**。Sales が Mini-UAT 対象に「引当解除」を挙げていることとも符合する |
| **I8** | 自動補充2方式 | **LATER 候補** | 売上基準（売上順／ランク順／均等配分、端数はランク順に1枚ずつ加算）と在庫基準（基準在庫を下回ったら補充）。CV10 は3画面ともL0。B09 として独立領域が立っており1.0スコープ外が妥当 |

---

## 6. 納品予定日（Phase 2〜3 / Decision 7-7）

### 6.1 旧CV.netの業務機能

- 旧は**全取引伝票が納品日を標準で持ち**、発注入力の項目説明も「②納品日 … 納品予定日」。
  配分データも納品日を持ち、出荷指示確定の検索軸を「指示日」と「納品日」で切り替えられた。
- **発注残管理表・受注残管理表の「納期予定」列は、発注データ／受注データの納品日をそのまま出力する**（4.1.1）。
  つまり納品予定日は残管理帳票の必須項目である。
- 両帳票とも**検索条件が「納品日 / 発注日（受注日）」の切替**になっており、既定値は納品日側である。
- 納品予定を使う専用機能は3つ。**納品予定照会**（月単位カレンダー、予定日を色分けしSKU別入荷予定数へ遷移）、
  **納品予定表**（発注・受注の双方にあり、仕入先単位では「納期遅れ」を表示）、**MDマップ**。
- 受注発注連携更新で発注伝票を自動作成する際も、納品日が設定項目に含まれる。
- リードタイムからの自動計算を示す記述は資料内に無い。手入力である。

### 6.2 論点

| # | 論点 | 区分候補 | 判断材料 |
| --- | --- | --- | --- |
| **H1** | 保持先と粒度（Decision 7-7） | **決定済 2026-08-17 / CHANGE** | **伝票単位で揃える。** G0（完了は伝票単位）と一貫させる。旧は明細側にも納品日を持っていたが、1伝票内で商品ごとに納期を変える運用は失われる。残管理表の「納期予定」は伝票の納品日をSKU行に展開して表示する |
| H2 | リードタイムの基準 | **KEEP** | 旧も手入力のみ。マスタ由来の自動計算は 2.0 以降 |
| **H3** | 納品予定照会のUI形式 | **REPLACE** | 旧は月単位カレンダー。列を追加すれば一覧型でも業務は閉じるため、1.0は一覧型で可 |
| **H4** | 納期遅れの判定 | **決定済 2026-08-17 / KEEP** | **納品日と完了フラグを参照して判定する。** 納品日を過ぎており、かつ `EndFlag=0`（未完了）のものを納期遅れとする。完了した伝票は納品日を過ぎていても対象外 |
| **H5** | MDマップ | **LATER 候補** | 絵型（画像）管理を伴い、業務の閉じ方に影響しない |

---

## 7. 請求計算・支払計算（Phase 4 / Decision 7-2）

修正第1回 3.2 のとおり、実装は Phase 4 だが**業務モデルの確定は Phase 1 で先行させる**。

### 7.0 基本方針（決定済 2026-08-17）

**旧仕様のデータの持ち方に合わせて、同じように持つ。**

`SummaryUriSei` / `SummaryKaiShi` は旧の請求残高・支払残高（`HC$MANAGE_KAKESKY` / `HC$MANAGE_KAKESIH`）の
構成に倣う。CV10 側は既に 得意先Id＋請求日 をユニークキーとし、請求開始日・請求終了日を持つ形で定義済みであり、
方針と整合している。

旧にあって CV10 に無い保持項目は次のとおりで、本方針に従えば追加対象となる。

| 旧の項目 | CV10 | 扱い |
| --- | --- | --- |
| 入金予定日 / 支払予定日 | 無し | **追加する。** 請求一覧表・支払一覧表の出力基準「入金予定月 / 支払予定日」に必要（7.1.1） |
| 請求書NO | 無し | E10 として**判断保留** |
| その他売上 | 無し（区分99を `Uriage` へ畳んでいる） | E11 として**判断保留** |

### 7.1 旧CV.netの業務機能

`業務フロー/05_掛管理`、`マニュアル/11-月次更新`、`帳票集PDF〔０６〕売上` より。

- **請求計算は「締日ごとに回す月末処理」**。実行条件は ①締日を一覧から選択 ②請求月 ③得意先CD FROM-TO。
  支払計算も仕入先で同一構造。請求計算を回して初めて請求一覧表・請求書印刷・月別入金予定表が出力可能になる。
- **請求一覧表の締日リストは「得意先マスタに登録されている得意先の締日より抽出」される。**
  締日は固定リストではなく、マスタの実データから動的に作られる。
- **親子関係チェック**という専用機能がある。得意先マスタの締日と請求先（親）の締日が食い違っていないかを事前検査する。
- 請求残高は「請求開始日／請求終了日／請求日／入金予定日／当月請求額」で構成される。
  導入時の初期投入画面の説明に「請求終了日 … 請求日と同じ日付を入力します」とあり、
  **請求日 = 請求終了日 = 締日**が確定できる。
- 消費税計算方法は **3値**（`0:請求単位` / `1:伝票単位` / `2:明細単位`）で、得意先ごとに設定できた。
  `0:請求単位` のときは請求書に「請求時一括」と表示し、独立処理（売掛消費税計算）で税額を計算していた。

#### 7.1.1 請求一覧表の算式（帳票集PDF〔０６〕売上 — 一次根拠）

| 項目 | 定義 |
| --- | --- |
| 日付 | 請求データの請求日または入金予定日 |
| 締日 | 得意先マスタの締日 |
| 前月残 / 現金 / 振込手数料 / 手形 / 相殺 / その他 | 請求データの各合計 |
| **繰越金額** | **前月残 −（現金 + 振込手数料 + 手形 + 相殺 + その他）** |
| 売上 / 返品 / 値引 | 請求データの各合計 |
| **売上合計** | **売上 − 返品 − 値引 +（その他売上の合計）+ 消費税** |
| **純売上** | **売上合計 − 消費税** |
| 当月残 | 繰越金額 + 売上合計（サンプル値で検算済み） |

出力基準は **請求集計月**（請求日基準）と **入金予定月**（支払予定日基準）の2種、
出力区分は **親のみ**（請求先のみ）と **子含む**（請求先および同一請求先の得意先）の2種。

注意点として、旧は**返品・値引を正値で保持して減算**している。
CV10 のドラフトD2は `CalcFlag` を掛けた符号付きで保持し `Henpin` / `Nebiki` を負値で入れる方式を採り、実装済みである。
合計は一致するため機能等価だが、帳票の表示時に符号を反転する必要がある。

また算式に **「その他売上」** が含まれる。CV10 には該当列が無く、ドラフトD2 は区分 `99`（その他）を `Uriage` に含めている。
合計は一致するが、**請求一覧表の「売上」列の値が旧と変わる**点は CHANGE として認識しておく必要がある。

### 7.2 論点と決定

| # | 論点 | 区分 | 内容と根拠 |
| --- | --- | --- | --- |
| E1 | 集計キー（得意先 or 請求先） | **KEEP** | 旧も残高は得意先単位で持ち、請求先での合算は帳票の出力区分「親のみ／子含む」で切り替えた。ドラフトD1と一致 |
| E2 | 締日の取得元 | **決定済 2026-08-17** | 第1段階は**1得意先1締日**（`Shime1` のみ）、次段階で最大3締日へ拡張。仕入先も同様。旧も3組持てたが移行実データは `Shime2` / `Shime3` 未使用 |
| **E2'** | 仕入先の `PayMonth=0` / `PayDay=0` の解釈 | **決定済 2026-08-17** | **`PayMonth=0` は「当月」の意味とする。** **`PayDay=0` はイレギュラーだが `99`（末日）と同様に扱う**（月・日に 0 は存在しないため）。移行実データの仕入先501件は全件 `PayMonth=0` / `PayDay=0` であり、この解釈により「当月末払い」となる |
| E3 | 請求日・期間の算出規則 | **KEEP** | 旧の請求残高設定が「請求終了日＝請求日」と明記。ドラフト推奨（`DayTo`＝締日、`DenDay`＝締日当日、`DayFrom`＝前回締日の翌日、`99`＝月末）と一致 |
| E4 | 消込との関係 | **KEEP** | 旧も消込は元帳の `*` 印字のためだけで、残高は伝票金額ベース |
| E5 | 再計算の単位 | **KEEP** | 旧は締日＋請求月＋得意先範囲で再実行。対象期間 DELETE → 再作成でよい |
| E6 | Rebuild 対象への追加 | **KEEP** | 旧も「売掛再更新」「買掛再更新」「在庫再更新」「在庫・掛一括再更新」を独立メニューで持つ |
| **E7** | 親子締日チェック | **LATER 候補** | `Id_Paysaki` 全件 `0` で検査対象が存在しない。ただし **Phase 4 は「親子請求」を範囲に含む**ため整合を確認する。LATER とするなら「親子運用開始前に追加」と明記 |
| **E8** | 消費税の計算単位 | **決定済 2026-08-17 / CHANGE** | **消費税は月ごとに計算する。** 旧の3方式（請求単位／伝票単位／明細単位）を得意先ごとに切り替える運用は廃止する。取引先マスタへ消費税計算方法を追加しない。**ドラフトD6（`Tax` は伝票ヘッダの単純合算）はこの決定で置き換わる**（7.3節） |
| **E9** | 入金率・支払率による予定額按分 | **決定済 2026-08-17 / DROP** | **実装しない。** 取引先マスタへ入金率・支払率を追加しない。月別入金／支払予定表は予定額をそのまま表示する |
| **E10** | 請求書番号 | **判断保留 2026-08-17** | 旧は請求書発行のたびに番号を採番し、請求残高側と伝票側の双方から辿れるようにして再発行・差し替えを識別した。CV10 に該当概念が無い |
| **E11** | 「その他売上」列 | **判断保留 2026-08-17** | 請求一覧表の算式に含まれる（7.1.1）。CV10 は区分99を `Uriage` へ畳んでいるため「売上」列の値が旧と変わる |

### 7.3 E8「消費税は月ごとに計算する」の詳細

E8 の決定により、ドラフト D6（税は伝票ヘッダ `Tax` の単純合算、税再計算は1.0対象外）は置き換わる。

| # | 論点 | 状態 | 内容 |
| --- | --- | --- | --- |
| E8-a | 計算対象 | 要判断 | 「月ごと」の単位は 得意先（仕入先）× 年月 でよいか。`SummaryUriKake` / `SummaryKaiKake` の集計キーと一致させるのが自然 |
| E8-b | 課税標準 | 要判断 | 月次の何に税率を掛けるか。`Uriage + Henpin + Nebiki`（符号付き合計）が候補。旧の算式でいう「売上 − 返品 − 値引 + その他売上」に相当する |
| **E8-c** | 端数処理 | **決定済 2026-08-17** | **四捨五入で統一する。** 旧は取引先マスタに消費税端数の設定を持っていたが、取引先ごとの切替は行わない |
| **E8-d** | 税率改定をまたぐ月 | **決定済 2026-08-17** | 施行日が月中の場合、一括計算では処理できない。**これはイレギュラーとして扱い、消費税を人手で調整する。** 数年〜10年に一度の頻度であり、専用の仕組みは持たない |
| **E8-e** | 内税・外税 | **決定済 2026-08-17 / CHANGE** | **内税は廃止する**（残す場合も見かけ上の表示のみ）。**外税を基本とする。** 旧は伝票ヘッダに内税消費税と外税消費税を別々に持っていた |
| **E8-f** | 伝票側 `Tax` の扱い | **決定済 2026-08-17** | **伝票ごとの `Tax` はあくまで目安**であり、確定するのは請求計算での消費税である。計算範囲が違えば税額は変わるため、**正式に採用されるのは請求書の税額**とする。伝票側 `Tax` は保持を続ける（伝票印刷・元帳表示に必要） |
| **E8-f'** | 目安と確定額の差の吸収 | **決定済 2026-08-17** | 伝票側の税額合計と請求計算の税額との差分は、**`Tran06Nyukin` / `Tran07Shiharai` の `Kubun`（相殺）で ± を調整する** |
| **E8-g** | 請求側（`SummaryUriSei`）の税 | 要判断 | **締日が月末以外のとき「月」と「請求期間」がずれる。** 第1段階は1得意先1締日かつ移行実データ全件 `Shime1=99`（末日）のため当面一致するが、締日を月末以外に設定した時点で月次税額と請求書の税額が食い違う。**第2段階（最大3締日）へ進む前の必須決定事項**。なお E8-f により「正式な税額は請求書側」と決まったため、`SummaryUriKake` 側の月次税額は目安という位置づけになる |

---

## 8. 棚卸・在庫調整・締日（Phase 5 / Decision 7-6）

### 8.1 旧CV.netの棚卸業務機能

`業務フロー/01`、`マニュアル/11-月次更新` 3〜4章、`帳票集PDF〔０８〕在庫管理` より。棚卸は**7段階**である。

1. **棚卸準備** — 棚卸日設定／棚卸日一括メンテナンスで倉庫ごとの対象日を設定。
2. **棚卸入力** — ハンディ読取→取込（実地棚卸数）。
3. **帳票印刷** — 棚卸明細表／棚卸差異表／棚卸チェックリスト。
   差異表は **帳簿在庫・実在庫・差異の3ブロック**をそれぞれ数量・上代合計・原価合計で出力し、**差異が無い場合は出力しない**。
4. **棚卸開始処理** — 「①で設定した日付時点の在庫を保存」。棚卸中に帳簿在庫が動かないようスナップショットを取り、
   棚卸明細表の「帳簿在庫数」欄へ反映する。得意先ごとに「する／しない」を選べる一覧形式。**自動実行に組み込める**。
5. **差異調査・伝票修正**。
6. **棚卸開始処理を再実行** — 修正後の在庫を再保存。
7. **棚卸確定処理（棚卸更新）** — 「①設定日付時点在庫を棚卸数にする」。**帳簿在庫を実地棚卸数で置き換える**。
   「棚卸確定処理後に過去の伝票を訂正した場合は、在庫が変動しますので、この画面に再度確定の表示がでます。再確定をして下さい」
   とあり、**再確定が正常な運用手順**として想定されている。

在庫強制調整は旧も独立した伝票入力メニューとして存在し、倉庫・売仕店・直営店を対象に商品マスタと紐づく。

### 8.2 旧CV.netの締日制御

`業務フロー/05` スライド20〜22。旧は伝票の遡及修正を**2層**で制御していた。

- **締日更新** — 設定日を含めそれ以前の**すべての伝票**の修正・削除・追加を禁止。請求計算・支払計算も対象。
  何度でも変更でき、前回締日・最終更新日・最終更新者を表示する。ただし**外部連携経由の登録は締日以前でも通る**。
- **システム管理設定** — 「修正有効日数」（過去何日前まで登録可）と「先付有効日数」（未来何日先まで登録可）。
  **締日更新の設定が優先**される。

ハンディ側にも対応するエラー（締日以前）があり、締日制御が全入力経路に効いていた。

### 8.3 参考: 総平均原価の算式（帳票集PDF〔１２〕月次更新）

Phase 5 の原価更新に直接使える一次根拠が取れたため記録する。

> **総平均原価 =（前月在庫 × 原価 + 当月仕入金額 + TQ）÷（前月在庫 + 当月仕入数）**

更新前に「総平均原価計算チェックリスト」で更新後の値を確認でき、
**更新結果確認（更新できる商品のみ）／エラー確認（エラーで更新できない商品のみ）／更新** の3操作に分かれている。
`TQ` の意味は帳票上で定義されておらず**判断材料不足**（諸掛の可能性がある。`業務フロー/06_諸掛・総平均原価更新` の精読が必要）。

### 8.4 論点

| # | 論点 | 区分候補 | 判断材料 |
| --- | --- | --- | --- |
| **F0** | 棚卸確定の反映方式 | **決定済 2026-08-17 / REPLACE** | **棚卸確定が行われると、`SummaryStock` の在庫数 `Su` との差を `AdjustQty`（調整数）に設定する。** 旧の「置換」と同じ結果を、差分を調整数として記録する形で実現する。あわせて**在庫数の構成を `Su = InQty + OutQty + AdjustQty`（当月棚卸数を含む）へ変更する（仕様変更）** |
| **F0'** | **棚卸開始処理（帳簿在庫スナップショット）を1.0に入れるか** | **KEEP 推奨** | 旧は棚卸中の帳簿在庫を凍結する専用処理を持ち、差異表の「帳簿在庫数」はこのスナップショット由来。CV10 `StockTakeInitiation` はL0。これが無いと作業中に伝票が入るたび差異が動き、差異照会の値が確定しない。実データ `Tran60Tana` 47,764件は旧運用が回っていた証拠 |
| **F0''** | **再確定を前提にするか** | **KEEP** | 旧は確定後に過去伝票を修正すると再確定を促す設計。1回きりの不可逆処理にすると運用が旧より狭くなる |
| F1〜F4 | 調整数の保持先・テーブル・理由・取消 | **KEEP** | 内部仕様。ドラフト推奨で問題ない。ただし F0 の決定により、`AdjustQty` は棚卸確定の書き手を得たことになる |
| **J1** | 締日更新を1.0に入れるか | **決定済 2026-08-17 / LATER** | **1.0では入れない。** `ShimebiUpdate`（L0）は 1.0 スコープ外とする。代替は J2 の有効日数によるワーニングである |
| **J2** | 修正有効日数・先付有効日数 | **決定済 2026-08-17 / KEEP** | **`MasterSysKanriMente` を参照する。** 判定に引っかかった場合は**ワーニング**を出す（一括で組み込む予定） |
| **J3** | 外部連携の締日例外を踏襲するか | **決定済 2026-08-17 / KEEP** | **外部連携は締日更新・有効日数の影響を無視する（例外）。** 旧と同じ扱いとする |

#### 8.4.1 F0 に伴う在庫恒等式の変更

`SummaryStock` は `Su`（在庫数）のほか `InQty`（入庫数）/ `OutQty`（出庫数）/ `AdjustQty`（調整数）/
`ActualQty`（棚卸数）/ `StocktakeDdate`（棚卸日）/ `CumulativeSu`（当月までの累計数量）を持つ。

F0 の決定により、棚卸確定時に `AdjustQty = 当月棚卸数 − 確定前の Su` を設定し、
在庫数は `Su = InQty + OutQty + AdjustQty` を満たす構成へ変更する。

これは修正第1回 一致点5「通常更新値 = Rebuild値」の検証対象式が置き換わることを意味する。
実装時には、①`AdjustQty` を Rebuild 側でも同じ手順で再現できること
②前月繰越（`CumulativeSu`）との関係を明示すること の2点を仕様として固定する必要がある。
`AdjustQty` はこれまで書き手が存在せず読み手（受払表3帳票）だけだったため、既存帳票の値が変わる点にも注意する。

---

## 9. 決定状況と次の決定順序

### 9.1 本日（2026-08-17）決定済み

| # | 決定 | 区分 |
| --- | --- | --- |
| E2 | 締日は第1段階 1得意先1締日、次段階で最大3締日（仕入先も同様） | CHANGE（段階導入） |
| E2' | `PayMonth=0` は当月、`PayDay=0` は `99`（末日）と同様に扱う | KEEP |
| 7.0 | 請求・支払計算は旧仕様のデータの持ち方に合わせる。入金予定日 / 支払予定日を追加 | KEEP |
| E8 | 消費税は月ごとに計算する | CHANGE |
| E8-c | 端数処理は四捨五入で統一 | CHANGE |
| E8-d | 税率改定が月途中の場合はイレギュラーとして人手で調整 | CHANGE |
| E8-e | 内税は廃止（表示のみ）、外税を基本とする | CHANGE |
| E8-f / f' | 伝票ごとの `Tax` は目安、確定は請求書の税額。差分は入金・支払の相殺区分で調整 | CHANGE |
| E9 | 入金率・支払率による按分は実装しない | DROP |
| G0 | 発注・受注の完了は**伝票単位** | CHANGE |
| G0' | 完了は自動で立てるが、手動で立てる（解除する）ことも可能 | KEEP |
| G0-a | 「残のみ」は完了除外後に明細残を出す | KEEP |
| G0-b | 伝票ごと完了にしたものはSKUに残があっても完了とみなす | CHANGE |
| G0-c | 明細単位で全SKUが充足されたものを自動完了とする | KEEP |
| G2 / G3 | 完了の取消は可、監査列は持たない | KEEP |
| G4 | 受注残 = 受注 − 受注伝票Idで紐付いた出荷（`Tran00Uriage`、`TenType=1` or `3`）。対象は完了でない受注。配分側の残は引当数で見る | KEEP |
| 4.3 | 発注・受注の状態遷移と、新設 `EndFlag` を完了判定に使うこと | KEEP |
| 4.4 | `Tran13Hachu.EndFlag` / `Tran12Jyuchu.EndFlag` / `TranHaibun.ShortSu` を追加 | — |
| I1 | 引当対象は `EnumHaibun = 0`（初回配分）以外のすべて（判定は `Kubun != 0`） | KEEP（挙動）/ CHANGE（判定式） |
| 5.1.2 | 配分の確定は `Su = JitsuSu + ShortSu` かつ `KakuteiDay` に有効日付。完了は `EndFlag=1` | KEEP |
| I4 | 出荷売上 = `TenType` 1 or 3、移動 = `TenType` 0 or 6 | KEEP |
| I5 | `TranHaibun` を `DenDay` + `Id_Soko` + `Kubun` + `RelateNo1` 単位でまとめる（仮想ヘッダ） | REPLACE |
| I6 | ハンディは無し | DROP |
| H1 | 納品予定日は伝票単位で揃える | CHANGE |
| H4 | 納期遅れは納品日と完了フラグで判定する | KEEP |
| F0 | 棚卸確定は `Su` との差を `AdjustQty` に設定。`Su = InQty + OutQty + AdjustQty` へ変更 | REPLACE |
| J1 | 締日更新は1.0では入れない | LATER |
| J2 | 修正有効日数・先付有効日数は `MasterSysKanriMente` を参照しワーニングを出す | KEEP |
| J3 | 外部連携は締日更新・有効日数の影響を無視する | KEEP |

### 9.2 実装済み（2026-08-17）

4.4 のスキーマ変更は先行実装した。

| 対象 | 内容 |
| --- | --- |
| `CvBase/BaseDb2Trans.cs` | `Tran13Hachu.EndFlag` / `Tran12Jyuchu.EndFlag` を追加（`EnumYesNo` の `EnEndFlag` 付き） |
| `CvBase/BaseDbHaibun.cs` | `TranHaibun.ShortSu` を追加。`EndFlag` と `EnumHaibun` のコメントを I1 の判定（`Kubun != 0`）へ更新 |
| `CvBase/UpdateDb.cs` | `26_08_17_01` を追加（3つの `ALTER` を `;` 区切りで1行） |

検証結果。

- ソリューション全体のビルド成功。`TestServer` 72件 / `TestLogin` 7件すべて成功。
- 実DB（`server-user163.db`、`DbVersion=26081601`）の実スキーマを読み取り、`Tran13Hachu` / `Tran12Jyuchu` に
  `EndFlag` が無いこと、`TranHaibun` に `EndFlag` が既存で `ShortSu` が無いことを確認した。
- 同スキーマを複製した空DBへ `SubInsertRecordAsync` と同じ分割（`;` 区切り、空要素除去）で適用し、3文とも成功。
- 重複適用は `duplicate column name` で失敗するが、`UpdateDb` はエラーをログに残して処理を継続する設計のため、
  二重実行してもDBは壊れない。
- 新規DB作成時は `versions` の SQL を実行せず最新バージョンだけを記録する経路のため、テーブル定義側への追加が必須である。
  3テーブルとも `DefineDataTable.cs` の生成対象に含まれていることを確認した。

### 9.3 次に決めるべき順序

| 順 | 決定項目 | Phase | 理由 |
| --- | --- | --- | --- |
| 1 | **I2 / I3 / I7 配分の確定・解除・滞留検出** | Phase 3 | `ShortSu` を追加したことで解消手段の骨格はできた。残るは「どの画面が `KakuteiDay` と `EndFlag` を立てるか」と滞留の一括完了 |
| 2 | **E8-a / E8-b 月次税の計算対象と課税標準** | Phase 4 | E8 の決定を実装可能にするために必須 |
| 3 | **E10 / E11**（請求書番号、その他売上） | Phase 4 | 現在保留。請求書印刷を1.0に含めるかと連動する |
| 4 | **E7 親子締日チェック** | Phase 4 | Phase 4 が「親子請求」を範囲に含むため、LATER の可否をここで確定する |
| 5 | **F0' 棚卸開始処理を1.0に入れるか** | Phase 5 | F0 が決まったため、スナップショットの要否だけが残っている |
| 6 | **E8-g 締日が月末以外のときの税** | 第2段階前 | 1得意先1締日かつ全件末日締めの間は表面化しない |

### 9.4 LATER

| 機能 | 状態 | 理由 | 代替手段 |
| --- | --- | --- | --- |
| **J1 締日更新** | **決定済 2026-08-17** | 1.0では入れない | J2 の有効日数によるワーニング |
| I8 自動補充2方式（B09） | 候補 | 3アルゴリズム＋基準在庫管理で作り込みが大きい。CV10は3画面ともL0 | 手動の店舗配分入力 |
| H5 MDマップ | 候補 | 絵型（画像）管理を伴い、業務の閉じ方に影響しない | 納品予定表 |
| E7 親子締日チェック | 候補 | `Id_Paysaki` 全件 `0` で検査対象が存在しない | 親子運用開始前に追加する前提を明記 |
| 展示会スワッチ関連 | 候補 | チェックリストで既に「1.0優先度低」 | — |

「候補」はいずれも Sales・System・Architect の確認を要する。

### 9.5 DROP 決定済み

| 機能 | 決定日 | 理由 | 代替手段 |
| --- | --- | --- | --- |
| I6 ハンディ運用 | 2026-08-17 | 旧の主経路だが1.0では採らない | 「出荷処理入力」相当の画面に一本化。倉庫から戻る `JitsuSu` / `ShortSu` が代替 |
| E9 入金率・支払率による予定額按分 | 2026-08-17 | 按分を行わない方針 | 予定額をそのまま表示 |
| E8 得意先別の消費税計算方法（請求単位／伝票単位／明細単位の切替） | 2026-08-17 | 消費税は月ごとに計算する方式へ統一 | システム共通の月次税計算（7.3節） |
| E8-e 内税 | 2026-08-17 | 外税を基本とする | 残す場合も見かけ上の表示のみ |
| G0 明細行単位の完了管理 | 2026-08-17 | 明細が `Jmeisai`（JSON）でSQLから絞れない | 伝票単位の完了（4.2） |

---

## 10. Given / When / Then

修正第1回 Decision 3 に対応する。決定済み項目はそのままテストケースに変換できる。

### I1 引当対象（決定済み）

```
Given 倉庫Aの商品Xが 実在庫10、引当数0
When  初回配分（Kubun=0）で商品Xを3枚配分する
Then  実在庫10、引当数0、有効在庫10 のまま変化しない

Given 倉庫Aの商品Xが 実在庫10、引当数0
When  在庫配分（Kubun=1）で商品Xを3枚配分する
Then  実在庫10、引当数3、有効在庫7 となる

Given 倉庫Aの商品Xが 実在庫10、引当数0
When  取置（Kubun=6）で商品Xを2枚配分する
Then  実在庫10、引当数2、有効在庫8 となる（0以外はすべて引当対象）

Given 上記の在庫配分が登録済み
When  当該配分を解除する（EndFlag=1）
Then  実在庫10、引当数0、有効在庫10 へ戻る

Given 実在庫0の商品Yに初回配分5枚が登録済み
When  有効在庫を参照する
Then  0 となる（マイナスにならない）
```

### 5.1.2 配分の確定と完了（決定済み）

```
Given 配分行が Su=10、JitsuSu=0、ShortSu=0、KakuteiDay=空
When  倉庫へ送信し、戻りデータで JitsuSu=8、ShortSu=2 が設定される
Then  Su = JitsuSu + ShortSu が成立する（10 = 8 + 2）
And   KakuteiDay が空のため、まだ確定ではない

When  KakuteiDay に有効な日付が設定される
Then  当該行は確定とみなされる

When  EndFlag=1 が設定される
Then  当該行は完了となり、引当数から外れる
```

### G0' / 4.3 発注の状態遷移（決定済み）

```
Given 発注伝票Pが 数量10、仕入0、EndFlag=0
When  関連伝票NO1にPを指定した仕入伝票を数量4で登録する
Then  PのEndFlagは0のまま、発注残管理表に残6として現れる

When  さらに数量6の仕入伝票を登録する
Then  PのEndFlagが1へ自動変更され、発注残管理表の「残のみ」に現れない

Given 発注伝票Qが 数量10、仕入4、EndFlag=0
When  発注残完了設定でQを完了にする
Then  QのEndFlagが1となり、残6を保持したまま「残のみ」に現れない

When  同画面でQの完了を解除する
Then  QのEndFlagが0へ戻り、残6として再び現れる
```

### G4 / 4.3 受注の状態遷移（決定済み）

```
Given 受注伝票Rが 数量10、出荷0、EndFlag=0
When  RのIdで紐付いた出荷売上（Tran00Uriage、TenType=1）を数量4で登録する
Then  RのEndFlagは0のまま、受注残管理表に残6として現れる

Given 受注伝票Rが 数量10、出荷4
When  RのIdで紐付かない出荷売上を数量6で登録する
Then  Rの残は6のまま変化しない（紐付けの無い出荷は対象外）

Given 受注伝票Rが 数量10、出荷4
When  RのIdで紐付いた出荷売上を TenType=6（直営店）で数量6登録する
Then  Rの残は6のまま変化しない（対象は TenType=1 または 3）

When  RのIdで紐付いた出荷売上（TenType=3）を数量6で登録する
Then  RのEndFlagが1へ自動変更され、「残のみ」に現れない

Given EndFlag=1 の受注伝票S
When  受注残管理表を出力する
Then  Sは集計対象に含まれない
```

### F0 棚卸確定（決定済み）

```
Given 倉庫Aの商品Xが SummaryStock で Su=10、AdjustQty=0、当月棚卸数(ActualQty)=8
When  棚卸確定処理を実行する
Then  AdjustQty が -2 になる（当月棚卸数 − 確定前の Su）
And   Su が 8 になり、Su = InQty + OutQty + AdjustQty を満たす
And   通常更新値 = Rebuild値 の原則が維持される

Given 棚卸確定処理の実行後
When  棚卸対象日以前の伝票を修正する
Then  当該倉庫が再確定対象として再表示される
```

---

## 11. 未取得の判断材料

1. **`帳票集/帳票別計算式一覧.xls`** — 帳票集PDF で各帳票の算式は取得できたため、優先度は下がった。
   ただし帳票横断の一覧としては未読。`.xls`（旧BIFF形式）を読む手段が環境に無い。
2. **`マニュアル/13-配分.pdf`（14.6MB）** — 配分の画面単位の詳細仕様。**I1-a〜c と I2〜I7 を詰める段で必要**。
   Ghostscript で抽出可能。
3. **`業務フロー/06_諸掛・総平均原価更新`** — 8.3 の `TQ` の定義。Phase 5 の原価更新で必要。
4. **旧システムでの仕入先支払サイトの実運用値** — E2' の直接の根拠。
5. **消費税端数処理の旧設定値** — E8-c の判断に必要。旧は取引先マスタに消費税端数を持っていた。
6. **Google Drive の旧CV.net資料** — 議事録が一次資料として指定しているが現在の接続アカウントから参照できない。
   `refer/cvnet-knowlege/` との差分の有無は未確認。
