# 本部売上入力（ShukkaUriage）消費税セットと画面表示 詳細設計（2026-09-02）

`CvWpfclient.Views._06Uriage.ShukkaUriageInputView` / `ShukkaUriageInputViewModel` について、
明細 `Id_Tax` のセット漏れとヘッダ `Tax1/2/3` 周辺の不整合を修正し、
消費税を画面へ表示できるようにする。

前提となる仕様は `Doc/spec/2026-09-01_消費税計算単位・端数処理_全体設計.md`（3.1〜3.7）を正とする。
本書はその仕様を**本部売上入力画面へ正しく適用し直す**ための修正設計であり、税計算の仕様そのものは変更しない。

**対象は本部売上（`ShukkaUriageInputViewModel` / `ShukkaUriageInputView.xaml`）のみ。**
同じ不具合を持つ他の入力画面（店舗売上・商品仕入・生地付属仕入・受注・発注）は後続作業とする（9章）。

---

## 1. 調査結果（現状の不具合）

### 1.1 明細 `Id_Tax` のセット漏れ（収束しない確定バグ）

明細の `Id_Tax` は基底 `BaseTranInputViewModel` の次の2経路でしか解決されない。

| 経路 | 契機 |
|------|------|
| `RecalcAllMeisaiTaxAsync()` | 伝票を開いたとき・`DenDay` 変更時（全行） |
| `RecalcMeisaiTaxAsync(m, …)` | `OnMeisaiPropertyChanged` が `Kingaku` / `Id_Shohin` の変更を検知したとき（1行） |

`OnMeisaiPropertyChanged` は `EditMeisai` へ追加する**前**に購読を張らないと発火しないため、
「値を全部詰めてから購読 → `Add`」という順で行を作る次の2箇所は、どちらも経路に乗らない。

| # | 箇所 | 内容 |
|---|------|------|
| **A** | `ApplyBarcodeMeisai`（`ShukkaUriageInputViewModel.cs:262`） | `InputBarcodeRow.ToMeisai`（`InputBarcodeViewModel.cs:259`）が `Id_Tax` を設定しない。`Kingaku`・`Id_Shohin` は購読前に確定するため再解決も走らず、`Id_Tax = 0` のまま残る |
| **B** | `ApplyShohinColSizMeisai`（同 `:299`） | 色サイズ展開で作る新規行も `Id_Tax` 未設定。`FillMeisaiFromColSizRow` が `Su`/`Kingaku` を購読前に書くため再解決も走らない |

`TaxCalculator.Apply` は `Id_Tax` が 1-3 以外の明細を課税対象額の集計から除外する（`TaxCalculator.cs:118-126`）。
したがってバーコード入力・色サイズ展開で作られた行は
**`TaxableAmount1/2/3` にも `Tax1/2/3` にも一切計上されない。**

実運用は全件が請求単位（`TaxCalcUnit=0`）で伝票の `Tax1/2/3` は元々 0 のため
画面上は異常が見えないが、`TaxableAmount1/2/3` が欠けるので
**請求計算（`SummaryDb.CalcSummaryUriSei`）が算出する消費税が過少になる。**
これが本件で最も影響の大きい不具合である。

### 1.2 ハンドラ二重購読（B に併発）

`ApplyShohinColSizMeisai` は、先頭の展開結果を既存行 `firstTarget` へ流し込んだあと

```csharp
FillMeisaiFromColSizRow(firstTarget, firstResult);
firstTarget.PropertyChanged += OnMeisaiPropertyChanged;   // ← 既に購読済み
```

としている。`firstTarget` は `SelectedMeisai`（＝ `EditMeisai` の要素）であり、
`ApplyMeisaiFromCurrentEdit` / `AddMeisai` の時点で既に購読済みである。
ここで購読が二重になり、以後この行を触るたびに `UpdateTotals()` と
`RecalcMeisaiTaxAsync` が 2 回ずつ走る。結果は同じだが無駄であり、
色サイズ展開を繰り返すと購読が 3 重・4 重に増える。

### 1.3 ヘッダ `Tax1/2/3` と関連項目

| # | 箇所 | 内容 |
|---|------|------|
| **C** | `DoSelectTokui`（`:389`） | 得意先を選び直して `TaxCalcUnit`/`TaxRounding` を差し替えても `UpdateHeaderTotals()` を呼ばない。`OnCurrentEditPropertyChanged` の監視対象にも入っていない。→ **端数処理・計算単位を変えても税額に反映されない** |
| **D** | `OnCurrentEditPropertyChanged`（`:157`） | `Tax1/2/3` の変更を監視して `UpdateHeaderTotals()` を呼ぶが、そのメソッド自身が `Tax1/2/3` を書く。**自己再入**であり、同値で `PropertyChanged` が止まることだけに依存している。監視対象として意味がなく、危うい |
| **E** | `OnCurrentEditChangedCore`（`:148`） | `RecalcAllMeisaiTaxAsync()`（税率キャッシュの充填を含む）の完了前に `UpdateHeaderTotals()` を同期実行する。税率キャッシュが空の状態で一度 `Tax1/2/3 = 0` を書いてから正しい値へ上書きされる。最終的には収束するため**実害はない**が、意図が読めない |

`TaxCalculator.Apply` の呼び方・`TaxableAmount1/2/3` への代入・
`Total = |KingakuTotal| + TaxTotal` は `ShiireInputViewModel` と同一であり**正しい**。

### 1.4 画面表示

| # | 内容 |
|---|------|
| **F** | `ShukkaUriageInputView.xaml` に**消費税・総合計の表示が無い**。伝票サマリーカードは 合計数量／合計金額／上代合計／下代合計 のみ。仕入・発注・受注が持つ `TaxTotal` 読み取り専用プロパティも本 VM には無い |
| **G** | 明細グリッドに**金額（`Kingaku`）列すら無い**（仕入入力にはある）。税率・明細税額も無いため、1.1 の `Id_Tax` 欠落に画面から気付けない |
| **H** | 一覧タブの `LightweightSelectColumns`（`:52`）が `Total` / `Tax1/2/3` を含まないため、一覧へ税・総合計の列を足すならここも直す必要がある |

---

## 2. 修正方針

### 2.1 `Id_Tax` セットの原則

**「`EditMeisai` へ行を足したら、必ず `RecalcAllMeisaiTaxAsync()` を await する」** を本画面の規約とする。

行ごとに `RecalcMeisaiTaxAsync` を呼ぶのではなく全行版を使う理由は、

- 商品マスタの参照結果は `shohinTaxIdCache` に載るため、全行走査してもマスタアクセスは増えない
- 税率キャッシュ（`EnsureTaxRateCacheAsync`）の充填も同時に済む
- 追加した行だけでなく「数量が加算された既存行」も同じ1回で正しくなる（`ApplyBarcodeMeisai` は既存行の `Su` を加算する枝を持つ）

`RecalcAllMeisaiTaxAsync()` は内部で `UpdateTotals()` を呼ぶため、
既存の末尾 `UpdateTotals()` は置き換えになる（二重呼び出しにしない）。

これに伴い `DoInputBarcode` / `DoInputShohinColSiz` の 2 コマンドを `async Task` 化する。
`[RelayCommand]` は `async Task` メソッドをそのまま非同期コマンドにできる。

### 2.2 二重購読の除去

`ApplyShohinColSizMeisai` の `firstTarget.PropertyChanged += OnMeisaiPropertyChanged;` を削除する。
`firstTarget` は `EditMeisai` の既存要素であり必ず購読済みである。

### 2.3 ヘッダ再計算の契機を整理する

`OnCurrentEditPropertyChanged` の監視対象を次へ改める。

| 監視するプロパティ | 動作 | 理由 |
|--------------------|------|------|
| `Kubun` | `UpdateHeaderTotals()` | 現状踏襲（明細区分の正規化ポリシーがヘッダ区分に従うため） |
| `TaxCalcUnit` / `TaxRounding` | `UpdateHeaderTotals()` | **追加。** 得意先差し替えで税額を引き直す（C の修正） |
| `DenDay` | `RecalcAllMeisaiTaxAsync()` | 現状踏襲（適用税率が変わる） |
| ~~`Tax1` / `Tax2` / `Tax3`~~ | **監視をやめる** | `UpdateHeaderTotals()` の出力であり入力ではない。自己再入を断つ（D の修正） |

あわせて `DoSelectTokui` の末尾で `UpdateHeaderTotals()` を明示的に1回呼ぶ。
プロパティ監視だけに頼ると、差し替え後の `TaxCalcUnit`/`TaxRounding` が
たまたま同値だったときに走らないため。

`DoSelectShohin` も同様に、末尾で当該行の税区分を明示的に引き直す
（同じ商品を選び直すと `Id_Shohin` が同値になり `PropertyChanged` が発火しないため）。

E（`OnCurrentEditChangedCore` の一時的な 0 書き）は、
同期メソッドから await できない構造上の制約であり、`ShiireInputViewModel` ほか
既存の入力 VM 全てと同じ挙動である。**修正せず、意図を説明するコメントのみ追加する。**

---

## 3. 画面表示の設計

### 3.1 表示方針（請求単位のときの見せ方）

実運用は全件が請求単位（`TaxCalcUnit=0`）であり、伝票の `Tax1/2/3` は**常に 0** である。
これは仕様どおりであって異常ではない（全体設計 10.2-2「伝票を見たら消費税が 0 だ、は正常」）。

**保存値どおり 0 を表示し、なぜ 0 なのかを注記し、根拠となる課税対象額の内訳を併記する。**
画面独自に見込み税額を計算して見せることはしない。請求計算が締請求期間で1回だけ丸めた確定値と
必ず端数で食い違い、「画面と請求書で消費税が違う」という問い合わせを生むためである。

### 3.2 伝票サマリーカード（詳細タブ右）

現状の 合計数量／合計金額／上代合計／下代合計 の下へ次を追加する。

```
  合計数量            1,234
  合計金額        1,000,000
  上代合計        1,500,000
  下代合計          800,000
 ─────────────────────────
  消費税                  0        ← TaxTotal
  請求単位のため、消費税は請求計算で確定します     ← 請求単位のときだけ表示
    10% 対象         900,000        ← 課税対象額の税区分別内訳
     8% 対象         100,000
 ─────────────────────────
  総合計          1,000,000        ← CurrentEdit.Total
```

- **消費税**: `TaxTotal`（= `Tax1+Tax2+Tax3`）。伝票単位の得意先では実額が出る
- **注記**: `TaxCalcUnit == Billing` のときだけ表示する。伝票単位のときは非表示
- **内訳**: 課税対象額が 0 でない税区分だけを行として出す。ラベルは適用税率から作る（`10% 対象`）。
  伝票単位のときは税額も併記する（`10% 対象 900,000 → 90,000`）
- **総合計**: `CurrentEdit.Total`。請求単位では税抜合計と同額になる

「下代合計」が現状は太字＝最終行の扱いだが、最終行は「総合計」へ譲り、下代合計は通常表示へ戻す。

### 3.3 明細グリッド

`下代単価` 列の後ろへ3列を追加する。

| 列 | バインド | 編集 | 書式 |
|----|----------|------|------|
| 金額 | `Kingaku` | 可（`TwoWay`, `UpdateSourceTrigger=PropertyChanged`） | `N0` 右寄せ |
| 税率 | `TaxRate` | 不可 | `{0}%` 右寄せ |
| 消費税 | `Tax` | 不可 | `N0` 右寄せ |

- 金額は仕入入力（`ShiireInputView.xaml:676`）と同じく編集可にする。
  `Su`/`Tanka` の変更で自動計算されるが、手入力での上書きも従来どおり効く
  （`OnMeisaiPropertyChanged` が `Kingaku` 変更を検知して税を引き直す）
- 税率は `Id_Tax` から解決された結果そのものなので、`Id_Tax` の欠落があれば `0%` として画面に出る。
  1.1 のような不具合を利用者・保守者が発見できるようにするのが主目的
- 明細 `Tax` は請求単位では 0 になる（仕様）。伝票単位のときに按分値が出る

税区分（`Id_Tax`）そのものは列に出さない。業務利用者には税率で足りる。

### 3.4 一覧グリッド

`合計金額` の後ろへ **消費税**（`Tax`）と **総合計**（`Total`）の2列を追加する。

一覧グリッドの行の DataContext は ViewModel ではなく `Tran00Uriage` である。
`DataGridTextColumn` は Visual Tree に載らず `RelativeSource` で ViewModel を掴めないため、
ViewModel 側に表示用プロパティを置いても届かない。またそれは `CurrentEdit` 由来の単一値であり、
一覧の全行に同じ数字が並んでしまう。したがって**モデル側に派生プロパティ `Tax` を置く**（4.3）。

あわせて `LightweightSelectColumns` へ `Total,Tax1,Tax2,Tax3,TaxableAmount1,TaxableAmount2,TaxableAmount3,TaxCalcUnit,TaxRounding` を追加する。
税列は一覧表示には使わないが、**一覧から詳細タブへ遷移したときの `CurrentEdit` の初期値**になるため、
欠けていると詳細を開いた瞬間に 0 が表示されてから正しい値へ差し替わる（ちらつき）。

---

## 4. 実装内容

### 4.1 `ShukkaUriageInputViewModel.cs`

| # | 変更 | 対応する不具合 |
|---|------|----------------|
| 1 | `LightweightSelectColumns` へ `Total` と税関連6列＋`TaxCalcUnit`/`TaxRounding` を追加 | H |
| 2 | `OnCurrentEditPropertyChanged` の監視対象を `Kubun` / `TaxCalcUnit` / `TaxRounding` / `DenDay` へ改める（`Tax1/2/3` を外す） | C, D |
| 3 | `UpdateHeaderTotals()` の末尾で表示用プロパティの変更通知を出す（`RaiseTaxDisplayChanged()`） | F |
| 4 | 読み取り専用プロパティ `TaxTotal` を追加（他の入力 VM と同名・同定義） | F |
| 5 | 表示用プロパティ `IsBillingUnitTax`（注記の表示切替）と `TaxBreakdown`（税区分別内訳）を追加 | F |
| 6 | `DoInputBarcode` を `async Task` 化し、`ApplyBarcodeMeisai` の末尾 `UpdateTotals()` を `await RecalcAllMeisaiTaxAsync()` へ | A |
| 7 | `DoInputShohinColSiz` を `async Task` 化し、`ApplyShohinColSizMeisai` の末尾 `UpdateTotals()` を `await RecalcAllMeisaiTaxAsync()` へ | B |
| 8 | `ApplyShohinColSizMeisai` の `firstTarget.PropertyChanged += …` を削除 | 1.2 |
| 9 | `DoSelectTokui` の末尾で `UpdateHeaderTotals()` を明示呼び出し | C |
| 10 | `DoSelectShohin` の末尾で当該行の税区分を明示的に引き直す | 2.3 |
| 11 | `OnCurrentEditChangedCore` へ、同期 `UpdateHeaderTotals()` が暫定値である旨のコメントを追加 | E |

内訳表示用の型（VM 内の入れ子 record）:

```csharp
/// <summary>伝票サマリーの課税対象額内訳1行（税区分ごと）。</summary>
public sealed record TaxBreakdownRow(long IdTax, int RatePercent, long TaxableAmount, long Tax) {
    /// <summary>"10% 対象"。税率0(未解決・非課税)は "税率未設定" とし、異常に気付けるようにする</summary>
    public string Label => RatePercent > 0 ? $"{RatePercent}% 対象" : "税率未設定";
}
```

`TaxBreakdown` は `UpdateHeaderTotals()` のたびに作り直す `IReadOnlyList<TaxBreakdownRow>` とし、
`TaxableAmount` が 0 の区分は含めない。

### 4.2 `ShukkaUriageInputView.xaml`

| # | 変更 |
|---|------|
| 1 | 一覧グリッドへ `消費税`（`Tax`）と `総合計`（`Total`）列を追加 |
| 2 | 明細グリッドの `下代単価` の後ろへ `金額` / `税率` / `消費税` の3列を追加 |
| 3 | 伝票サマリーカードの `RowDefinitions` を 5 行から 9 行へ拡張し、`Separator` / `消費税` / 注記 / 内訳 `ItemsControl` / `総合計` を追加。`下代合計` の太字を解除 |

税率の `{0}%` 表示は `StringFormat` で足りるため、コンバータは追加しない。
注記の表示切替は既存の `BooleanToVisibilityConverter` を使う（`ShiireInputView.xaml` 等で既に参照済みのリソースを確認して合わせる）。

### 4.3 `CvBase/BaseDb2Trans.cs`（モデルの派生プロパティ）

`Tax1/2/3` を持つ伝票クラスへ、一覧グリッド表示用の派生プロパティを追加する。

```csharp
[ResultColumn]
[JsonIgnore]
public long Tax => Tax1 + Tax2 + Tax3;
```

- **DB は変更しない。** `[ResultColumn]` により DDL 生成（`ExDatabase.GetSqlColumns`）と
  部分更新の列マッピング（`HandlerClass`）の双方から除外されるため、実列は増えない
- `[JsonIgnore]` で gRPC 転送の JSON にも載せない。クライアント側は転送済みの `Tax1/2/3` から再計算する
- `[ObservableProperty]` は付けない。setter 付きの実体を生成するため式本体と併記できず、
  そもそも派生値に setter は不要だから。`Tax1/2/3` の変更で `PropertyChanged` は出ないが、
  一覧グリッドは読み込み済みの行を読み取り専用で表示するだけなので問題にならない

対象クラス: `Tran00Uriage`（本作業で使用）、`Tran03Shiire` / `Tran02Material` /
`Tran12Jyuchu` / `Tran13Hachu`（9章の4画面で既に壊れている `{Binding Tax}` の修復。些少な変更のため同時に行う）。
`Tran01Tenuri`（店舗売上）は一覧に消費税列が無いため今回は追加しない。

---

## 5. 影響範囲

- 変更ファイルは `ShukkaUriageInputViewModel.cs` / `ShukkaUriageInputView.xaml` / `CvBase/BaseDb2Trans.cs` の 3 件
- 共通基底（`BaseTranInputViewModel`）・`TaxCalculator`・`InputBarcodeViewModel` は**変更しない**
  - `InputBarcodeRow.ToMeisai` に `Id_Tax` を持たせる案もあるが、`InputBarcodeViewModel` は
    商品マスタの `Id_Tax` を保持していないうえ、他の入力画面と共有しているため、
    呼び出し側で `RecalcAllMeisaiTaxAsync()` を通す 2.1 の方針で統一する
- 保存されるデータの意味は変わらない。**欠けていた `TaxableAmount1/2/3` が正しく入るようになる**
- 既存伝票のうち、バーコード入力・色サイズ展開で作られた行を含むものは
  `TaxableAmount1/2/3` が過少のまま保存されている可能性がある。
  これは全体設計 8章 手順4「システム > その他処理 > 伝票税額再更新」で一括修復できる（6.1）

---

## 6. 既存データへの影響と復旧

### 6.1 既存伝票の `TaxableAmount` 欠落

1.1 の不具合により、バーコード入力・色サイズ展開を使って作成された既存伝票は
`TaxableAmount1/2/3` が実際の課税対象額より小さい（該当行ぶんが欠けている）可能性がある。

`TranTaxRebuildDb`（「システム > その他処理 > 伝票税額再更新」）は
明細の `Id_Shohin` から `Id_Tax` を引き直してヘッダを作り直すため、
**この不具合で欠けた課税対象額もあわせて修復される。**
コード修正の適用後、期首日以降の伝票に対して一括再計算 → 請求計算のやり直しを行うこと
（全体設計 8章 手順4→5→6）。

### 6.2 影響範囲の事前確認

修正前に、影響を受けた伝票の件数を次で確認できる。
明細に `Id_Tax` が 1-3 でない行を含む `Tran00Uriage` を数える。

```sql
SELECT COUNT(*) FROM Tran00Uriage t
WHERE EXISTS (
  SELECT 1 FROM json_each(t.Jmeisai) m
  WHERE COALESCE(json_extract(m.value, '$.Id_Tax'), 0) NOT BETWEEN 1 AND 3
);
```

---

## 7. テスト観点

| # | 観点 | 期待 |
|---|------|------|
| 1 | バーコード入力で明細を追加する | 追加行の `税率` 列に伝票日付時点の税率が出る。`TaxableAmount1/2/3` が全行の金額合計と一致する |
| 2 | バーコード入力で既存行の数量が加算される枝 | 加算後の金額で課税対象額が再集計される |
| 3 | 色サイズ展開で明細を追加する | 展開行すべてに税率が出る。先頭行（既存行への流し込み）も含めて課税対象額が一致する |
| 4 | 色サイズ展開を同じ行に対して2回続けて行う | `UpdateTotals` が二重に走らない（購読が増えない）。金額・税額が正しい |
| 5 | 得意先を、端数処理が異なる得意先へ選び直す | 伝票単位の得意先どうしで、税額が新しい端数処理で計算し直される |
| 6 | 得意先を、請求単位 ⇄ 伝票単位で選び直す | 請求単位へ変えると `Tax1/2/3` が 0 になり注記が出る。伝票単位へ変えると実額が出て注記が消える |
| 7 | 伝票日付を税率改定日をまたいで変更する | 明細の税率・ヘッダの税額が新税率で引き直される |
| 8 | 同じ商品を選び直す（`Id_Shohin` が同値） | 税区分の引き直しが走る（明示呼び出しの確認） |
| 9 | 請求単位の伝票を開く | 消費税 0・注記あり・課税対象額の内訳が税率別に出る。総合計＝合計金額 |
| 10 | 伝票単位の伝票を開く | 消費税に実額・注記なし・内訳に税額併記。総合計＝合計金額＋消費税 |
| 11 | 明細の金額を手入力で上書きする | 課税対象額・税額が追随する |
| 12 | 一覧タブ | 総合計列が出る。行を選んで詳細タブへ移ってもちらつかない |
| 13 | 保存 → 再読込 | `TaxableAmount1/2/3`・`Tax1/2/3`・`Total` が保存前と一致する |

`Tests/TestServer` に本画面（`CvWpfclient`）を対象とする自動テストは無い（参照関係の都合）。
上記は画面での手動確認とする。`dotnet build creativevision10.slnx` が 0 エラーであることは必須。

> **テストの実行方法**: この環境では `dotnet test` が 0 件になる。
> `Tests/TestServer/bin/Debug/net10.0/TestServer.exe` を直接実行する。

---

## 8. 実装状況

| フェーズ | 内容 | 状態 |
|----------|------|------|
| 0 | 本書（調査・詳細設計） | 完了 |
| 1 | `ShukkaUriageInputViewModel.cs` の修正（4.1） | 完了 |
| 2 | `ShukkaUriageInputView.xaml` の修正（4.2） | 完了 |
| 3 | `BaseDb2Trans.cs` へ派生プロパティ `Tax` を追加（4.3） | 完了 |
| 4 | ビルド確認 | 完了（`dotnet build creativevision10.slnx` → 0 エラー 0 警告） |
| 5 | 自動テスト | 完了（`TestServer.exe` → 264 件全件成功。書き込み経路の `TranTaxRebuildTests` / `SummaryDbTests` / `WriteEffectRunnerTests` を含む） |
| 6 | 画面での手動確認（7章） | 完了（実機確認OK） |

---

## 9. 後続作業（2026-09-02 適用完了）

同じ構造の不具合を持つ入力画面へ、本部売上と**同じ方針（2.1〜2.3）を適用した**。
View（XAML）は変更していない。3章の画面表示（消費税・課税対象額内訳の追加）は本部売上のみの対応であり、
他画面へ広げるかは別途判断する。

### 9.1 適用した修正

| # | 修正 | 対応する不具合 | 適用画面 |
|---|------|----------------|----------|
| 1 | `OnCurrentEditPropertyChanged` の監視対象から `Tax1/2/3` を外し、`TaxCalcUnit`（持つ画面のみ）/ `TaxRounding` を加える | D, C | 5画面すべて |
| 2 | `OnPropertyChanged(nameof(TaxTotal))` を監視分岐から `UpdateHeaderTotals()` 末尾へ移す。全再計算経路で通知が出る | F | 店舗売上を除く4画面（店舗売上は `TaxTotal` を持たない） |
| 3 | `OnCurrentEditChangedCore` の同期 `UpdateHeaderTotals()` が暫定値である旨のコメントを追加 | E | 5画面すべて |
| 4 | 取引先選択コマンドの末尾で `UpdateHeaderTotals()` を明示呼び出し | C | 5画面すべて |
| 5 | `DoInputBarcode` / `ApplyBarcodeMeisai` を `async Task` 化し、末尾 `UpdateTotals()` を `await RecalcAllMeisaiTaxAsync()` へ | A | 店舗売上・商品仕入・受注・発注 |
| 6 | `DoInputShohinColSiz` / `ApplyShohinColSizMeisai` を同様に `async Task` 化 | B | 同上 |
| 7 | `ApplyShohinColSizMeisai` の `firstTarget.PropertyChanged += …` を削除 | 1.2 | 同上 |
| 8 | 明細の商品／生地・付属を選び直したとき、末尾で当該行の税区分を明示的に引き直す | 2.3 | 5画面すべて |

### 9.2 画面ごとの差異

| 画面 | ファイル | 備考 |
|------|----------|------|
| 店舗売上 | `06Uriage/ShopUriageInputViewModel.cs` | `Tran01Tenuri` は `TaxCalcUnit` を持たない（現金売のため常に伝票単位）。取引先選択は `DoSelectTenpo`。`TaxTotal` プロパティを持たないため #2 は対象外 |
| 商品仕入 | `05Shiire/ShiireInputViewModel.cs` | `Tran03Shiire` は `TaxCalcUnit` を持つ。取引先選択は `DoSelectShiire` |
| 生地・付属仕入 | `05Shiire/MaterialInputViewModel.cs` | バーコード入力・色サイズ展開が無いため #5〜#7 は対象外。明細は `Tran99MaterialMeisai` で税ヘルパーは当 VM のローカル実装。#8 は `DoSelectMaterial`（`Id_Material` 同値で `PropertyChanged` が出ない） |
| 受注 | `04Juchu/JuchuInputViewModel.cs` | `Tran12Jyuchu` は `TaxCalcUnit` を持たない（常に伝票単位）。取引先選択は `DoSelectTokui` |
| 発注 | `03Hatchu/HachuInputViewModel.cs` | `Tran13Hachu` は `TaxCalcUnit` を持たない。`DoSelectShiire` は仕入先が引けないときの早期 return を持つため、**両方の経路**で `UpdateHeaderTotals()` を呼ぶ |

`Tran01Tenuri` への派生プロパティ `Tax` は**追加しない**（4.3 の判断どおり。店舗売上の一覧グリッドに消費税列が無く、
追加する必要が生じていない）。

### 9.3 修正不要と確認した箇所

明細グリッドの `{Binding Tax}`（`MaterialInputView.xaml:569`）は `Tran99MaterialMeisai.Tax` であり、
この列は分割していないため**正常**。修正不要。

**一覧グリッドの `{Binding Tax}` 4 画面は、本書 4.3 で解消済み。**
`Tran*.Tax` は `Tax1/2/3` へ分割済みで存在せず、これらの列は常に空欄になっていた。
全体設計 5章が「XAMLバインド4件」として挙げたのは詳細タブの `CurrentEdit.Tax` であり、
**一覧グリッド側は取りこぼしていた。**
4.3 の派生プロパティ追加により View 無修正で表示されるようになった。

### 9.4 既存データの修復

本節 #5〜#7（バーコード入力・色サイズ展開の `Id_Tax` セット漏れ）は、本部売上と同様に
既存伝票の `TaxableAmount1/2/3` を過少にしている可能性がある。
6章と同じく「システム > その他処理 > 伝票税額再更新」（`TranTaxRebuildDb`）で
期首日以降を一括再計算し、請求・支払計算をやり直すこと（全体設計 8章 手順4→5→6）。
対象は商品仕入・受注・発注・店舗売上の伝票である。

#### 実施結果（2026-09-02、`CvServer/server-user163.db`）

1. 「システム > その他処理 > 伝票税額再更新」を実行済み（画面から実施）
2. 続けて請求・支払の全締日・全期間再計算バッチを実行

```
dotnet run --project "Doc/spec/tools/seikyushiharai_recalc" -- run

請求計算(CalcSummaryUriSei) 対象: 178 件   締日=20 / 締日=99  201905〜202609
支払計算(CalcSummaryKaiShi) 対象:  89 件   締日=99            201905〜202609
成功: 267 件  失敗: 0 件  合計挿入行数: 1089395
```

検証:

- `Balance = TotalSales - TotalIn`（`SummaryUriSei` 250,713 行）不一致 0 件
- `Balance = TotalShiire - TotalOut`（`SummaryKaiShi` 44,678 行）不一致 0 件
- `TestServer.exe` 264 件全件成功

3. 続けて売掛残・買掛残を全期間で再計算

```
dotnet run --project "Doc/spec/tools/seikyushiharai_recalc" -- kake-run

売掛残(CalcSummaryUriKake) / 買掛残(CalcSummaryKaiKake) 対象期間: 201304〜202609
成功: 2 件  失敗: 0 件  合計挿入行数: 42180
```

売掛残・買掛残は締日×月のループを持たず日付範囲を1回で受け取るため、
バッチへ `kake-plan` / `kake-run` モードを追加して対応した（`Doc/spec/tools/seikyushiharai_recalc/README.md`）。

追加の検証:

- 行数は実行前と同一（`SummaryUriKake` 18,841行 / `SummaryKaiKake` 2,249行）。
  この実行時点では戻り値が行数の2倍だったが(DELETE+INSERTの影響行数を返していたため、重複挿入ではない)、
  後日 `CalcSummaryUriKake`/`CalcSummaryKaiKake` の戻り値を挿入件数のみへ変更した(下記参照)
- `Balance = TotalSales - TotalIn`（`SummaryUriKake`）不一致 0 件
- `Balance = TotalShiire - TotalOut`（`SummaryKaiKake`）不一致 0 件

実行前に DB のバックアップを2世代取得済み（リポジトリ外 `_dbbackup/`。
`server-user163.db.20260902-pre-recalc` = 請求・支払再計算前、
`server-user163.db.20260902-pre-kake` = 売掛残・買掛残再計算前）。

### 9.5 実装状況

| フェーズ | 内容 | 状態 |
|----------|------|------|
| 1 | 5画面の ViewModel 修正（9.1） | 完了 |
| 2 | ビルド確認 | 完了（`dotnet build creativevision10.slnx` → 0 エラー 0 警告） |
| 3 | 自動テスト | 完了（`TestServer.exe` → 264 件全件成功） |
| 4 | 画面での手動確認（7章に準ずる） | 完了（5画面とも実機確認OK） |
| 5 | 既存データの修復（9.4） | 完了（伝票税額再更新 + 請求・支払 + 売掛残・買掛残の全期間再計算。9.4 実施結果を参照） |

### 9.6 CalcSummaryUriKake/CalcSummaryKaiKake の戻り値を挿入件数のみへ変更

9.4 実行時、戻り値が実際のテーブル行数の約2倍になっていた（DELETE+INSERTの影響行数を
合算していたため）。呼び出し側が実件数と誤認しないよう、削除件数を戻り値へ加算しないよう修正した。

対象: `CvDomainLogic/SummaryDb.cs` の `CalcSummaryUriKake` / `CalcSummaryKaiKake`。
挙動が同じ `CalcSummaryUriSei` / `CalcSummaryKaiShi` は対象外（呼び出し側で挙動を前提にしていないため、
本件のスコープを Kake の2メソッドに限定した）。

`dotnet build creativevision10.slnx` → 0エラー0警告、`TestServer.exe` → 264件全件成功で確認済み。

修正後に同じ範囲(201304〜202609)で再実行し、戻り値がテーブル行数と一致することを確認した
(売掛残 18,841行 / 買掛残 2,249行。`Balance` 不一致0件は変化なし。詳細は
`Doc/spec/tools/seikyushiharai_recalc/README.md`「CalcSummaryUriKake/CalcSummaryKaiKake 戻り値の修正」を参照)。

## 10. 関連ドキュメント

| ドキュメント | 関係 |
|--------------|------|
| `Doc/spec/2026-09-01_消費税計算単位・端数処理_全体設計.md` | **前提。税計算の仕様はこちらが正。**本書はその適用漏れの修正 |
| `Doc/spec/2026-08-25_明細別消費税計算_詳細設計.md` | 明細へ `Id_Tax`/`TaxRate`/`Tax` を持たせた回 |
| `Doc/spec/2026-09-02_月次請求書_適格請求書対応_詳細設計.md` | 課税対象額・税率別内訳の出力先 |

