# CV10 機能完成度チェックリスト（2026-09-02 更新）⭐️

状態: **常設ドキュメント（進捗インデックス、随時更新）**。個別設計書の完了記録はDoc/spec/archive/へ集約し、本書は現在地・期別計画・仕様決定・UAT Gateの要約のみを保持する。

調査基準日: 2026-09-02
調査対象HEAD: `91c3870`
前回確認（2026-08-28、`7114eec`）以降の主な反映:
- `3b897a9` Summary 4テーブルを対象期間の集計値へ統一し、帳票の繰越は表示専用の`PreviousBalance`として算出する方式へ移行。全締日・全期間の再計算と実データでの帳票恒等式・冪等性確認まで完了した。
- `33e945b` 複数締日（最大3締日）の第2段階を実装完了。請求・支払計算の期間算出を取引先ごとの直前締日基準へ変え、画面候補・保存前バリデーション・再計算検査・期首残高CSV（最終締日で1行）・solution外ツールまで対応した。帳票8本は3観点（`Shime1`の誤用、1取引先1月1行の前提、`PreviousBalance`の相関）で確認し全て該当なし。`TestServer.exe` 324件全件成功。実データでの最終確認も完了（受入条件#8は実DBのコピーで全列突合し差分0件、実機での画面・帳票確認も実施済み）。**複数締日対応は完了。**
- `331bed4` POS売上・返品・取消へ共通消費税計算を適用し、標準・軽減・非課税、端数処理、再送・取消を関連テストで確認した。D-11のPOS/HHT/物流を10.0で運用する決定を反映する。
- `159a7f9` 月次請求書を適格請求書対応にし、登録番号、軽減税率記号、税率別内訳を実DBの混在税率請求書PDFで突合した。
- `b4aecc2` / `5993e3f` 納品書印刷を適格返還請求書（R2）対応の66列帳票へ再設計し、実行時エラー5点を修正した。返品・値引は題名を「適格返還請求書」に切り替え、10%／8%／非課税の返還対価と税額を区分表示する。請求単位伝票は保存税額が0のため印刷時に再計算する。実データ3伝票と実機PDFまで確認済み。**これで消費税計算単位・端数処理 全体設計 9章の残課題 R1〜R5 はすべて決着した。**
- `92e11c3` VM駆動UATハーネス Phase 0/1（C-01〜C-10）は全ケースPASS。D-09の最終承認も完了済みである。
- （2026-09-03）移行リハーサル1回目: 実機でConvertDbマスター変換処理を実行し、Tran系13カテゴリ全件をcv-oracle/cv-sqlite突合で検証OK。詳細は[実データ突合結果](archive/2026-09-03_ConvertDbマスター変換_Tran系実データ突合_結果.md)。

累積の到達点（完了済みの詳細は各設計書・`Doc/aicoding_log.md`へ集約）
メニュー基準: `CvWpfclient/Models/MenuData.cs`

---

## 1. 本書の目的・判定基準

### 1.1 目的

- 現行の大メニューを基準に、実装済み領域と残作業を整理する。
- 残作業を **10.0実装予定 / 10.1予定 / 10.2以降** に分ける。
- 実装済み機能の詳細説明は繰り返さず、Release Gate、必要な仕様決定、最小UATの不足を中心にする。

### 1.2 正本の優先順位

内容が食い違う場合は、次の順に確認する。

1. 最新コミットの実コード・DB定義・テスト。
2. `CvWpfclient/Models/MenuData.cs` の現行大メニューと到達経路。
3. `Doc/spec/` フォルダの各詳細設計。
4. `refer/cvnet-knowlege/` のマニュアル、業務フロー、帳票集、DB定義。

### 1.3 状態・期別の定義

| 表記 | 判定 |
|---|---|
| 実装済 | 実コードに業務処理があり、ビルドまたは関連テストの記録がある |
| 要UAT | 実装済みだが、実ユーザーの業務シナリオとして合格記録が無い |
| 10.0実装予定 | 10.0の必須業務シナリオ、移行、請求・支払、月次運用を成立させるためRelease前に必要 |
| 10.1予定 | 10.0に代替手段があり、初回Release後に業務範囲・利便性を拡張するもの |
| 10.2以降 | 外部契約、画像・高度分析基盤、別製品との責務分担など、先に要件化が必要なもの |
| 仕様決定 | 実装前に業務責任者が選択し、Given/When/Thenまたはデータ契約へ落とす必要があるもの |

完成度は画面数ではなく、次の条件で判定する。

- 10.0必須シナリオが画面からサーバー、DB、帳票まで完遂する。
- 通常更新値とRebuild値が一致する。
- 取消・再実行・楽観排他・異常終了後の復旧手順が確認されている。
- 移行データと新規テストデータの両方で金額・数量・税・残高を検算できる。
- UATの実施者、証跡、合格基準が決まっている。

---

## 2. サマリ

### 2.1 現行メニューの規模

`MenuData.cs` には **16大メニュー、214の表示参照、重複を除く196 View** が定義されている。このため、2026-08-12版の「8フォルダ・132画面」と単純比較しない。現行の大メニュー名は `顧客管理` であり、旧称のLoyal Customer（LCV）を併記する。

| 区分 | 大メニュー | 表示参照数 | 現在地 |
|---|---|---:|---|
| ロール別 | 店舗業務 / 倉庫業務 | 18 | 既存機能へのショートカット。 |
| 基幹入力 | マスター / 予算 / 発注 / 受注・展示会 / 仕入 / 配分・出荷 / 売上 / 在庫管理 | 103 | 代表業務経路は概ね実装済み。残りは代替入力、周辺帳票、POS。生地・付属マスタ/仕入を新規追加。 |
| 掛・分析 | 掛管理 / 分析 | 47 | 請求・支払計算、再発行、複数締日対応の中核、期間集計化済みの帳票・月次予定表まで実装済み。残るのは複数締日の周辺ツール・帳票確認と業務UAT。 |
| 更新・連携 | 月次・更新処理 / 外部連携 | 28 | 棚卸・在庫掛・請求/支払Rebuild、全締日・全期間再計算、税額再計算は実装済み。原価・外部連携の一部は未実装。 |
| 顧客・管理 | 顧客管理（LCV） / システム管理 | 18 | システム管理は実装済み。顧客管理（LCV）は未実装 |

### 2.2 10.0完成までの結論

商品登録から発注、仕入、在庫、配分、出荷、売上、入金・支払、消込、請求・支払計算、Rebuildまでの主要部品は揃った。期間集計化・税計算単位/端数処理・POS税計算・複数締日の中核実装を反映し、D-09（Mini-UAT）およびD-11（POS/HHT/物流の10.0運用）は完了済みである。2026-09-02時点で残る主要なRelease Gateは次の3点である（複数締日対応は同日完了）。

1. **移行リハーサル1回目完了（2026-09-03）**。実機でマスター変換処理を実行し、Tran系13カテゴリ全件（件数・金額・数量）をcv-oracle/cv-sqlite全件突合で検証OK（差異は区分99の金額付け替え・在庫調整の符号化仕様のみで説明可能）。詳細は[実データ突合結果](archive/2026-09-03_ConvertDbマスター変換_Tran系実データ突合_結果.md)。区分18→`Tran61Chosei`の変換ロジック（`CnvTran61Chosei`）は実装済み（2026-08-26）。区分2（1,728件）の変換ロジック実装済み、取り込み済み。Rate列は**決着済み（2026-09-02）**。`Rate` は掛率として使う方式で確定し、実際の `Rate` 列は変更していない。旧値が残るのは実データに影響しない過去データのみのため、**洗い替えは実施せずそのままとする**（`Doc/spec/archive/2026-08-24_Rate列_掛率と税率の分離課題.md` 6章 #1）。**残: 同一手順2回目の再変換・差分突合、マスター系（`CnvMaster*`）・Tax/Total/IsPay個別項目の突合。**
2. **UAT-02（受注・配分・出荷）/ UAT-03（移動）/ UAT-04（棚卸）の通しシナリオ未実施**。部品は実装済み。UAT-01は修正後の再UATが残る。
3. **引当・HHT・夜間再集計の実データ確認**。締日20境界3ヶ月（C-01）、末日締め（C-02）、親子締日ワーニング（C-03）、Rebuild締日変更ブロック（C-07）、明細別税（C-04）、実行中キャンセル（C-10）は実運用相当DBで確認済み。なお支払側の境界パターン（仕入先005〜007）は合成データによる補完であり、実スケール検証は残る。
4. ~~**複数締日の残作業**~~ **完了（2026-09-02）**。期首残高CSV・solution外ツール・帳票8本の確認・実データ確認（受入条件#8の差分0件）・実機確認まで完了した。

このほか、D-09（Mini-UAT結果レポートのユーザー最終承認）は完了済みである。D-11も2026-09-02にPOS/HHT/物流を10.0で運用すると決定し、POS消費税（R4）を実装済みである。

### 2.3 期別ロードマップ

| 期 | 必須成果 | 完了条件 |
|---|---|---|
| **10.0** | SQLiteを対象とした請求・支払計算、最大3締日の実データ最終確認、請求/支払帳票突合、必要な月次・残高登録、移行リハーサル、最小UAT（原価4項目はD-01により10.1へ移管、公開状態/権限はD-10により対象外） | 代表シナリオ100%完遂。数量・在庫・掛・請求・支払・税の期待値とRebuild値が一致 |
| **10.1** | LCV、締日更新、原価4項目（最終仕入原価/総平均原価/諸掛/消化仕入、D-01）、権限判定・メニュー公開状態（D-10）、配分の別入力経路、自動補充、展示会スワッチ/絵型（メニュー削除済み、実装時に再追加）、納品・出荷帳票 | 10.0の代替運用を解消し、LCVを含む対象業務のUATに合格 |
| **10.2以降** | CPA（メニュー削除済み、再導入は新規実装）、物流の外部接続、勤怠、輸入、画像/MDマップ、高度分析、PostgreSQL/MariaDB実DB互換 | 外部契約・データ所有者・責務境界・移行要件を承認後に個別Release |

### 2.4 10.0の優先順位

| 優先 | 作業 | 依存 |
|---:|---|---|
| 済 | **WPF Mini-UAT**（請求/支払計算、E7、明細別税、Rebuild、20日締め境界、冪等性、旧締日残データ再計算）はPhase 0/1で全PASS（2026-08-28）。D-09の最終承認も完了済み | 結果レポート `Doc/test/UatVm_Phase01_20260828_テスト結果レポート.md` |
| P0 | **移行リハーサル**（実DBコピーで全件再変換、Rate列の再変換、同一手順2回の突合）。1回目のTran系全件突合は完了（2026-09-03、検証OK）。残: 2回目の再変換・差分突合、マスター系・Tax/Total/IsPayの個別突合 |  |
| P0 | **UAT-02/03/04の通しシナリオ**とUAT-01再テスト（VM駆動ハーネスの拡張が有望） |  |
| P1 | 引当・HHT・夜間再集計の実データ確認。`summaryreconcile`シードの`Jmeisai`欠落修正（202607を含む在庫Rebuildの前提） | 実DBコピー |
| 済 | 在庫/原価の期首残の投入元 → D-08で決定（期首前日の棚卸入力データ。売掛/買掛/請求/支払は `BalanceRegistrationView` で完了） | 実DBバックアップ |
| 済 | D-04親子請求出力（10.0は単独請求のみ）、D-05適格請求書要件（税率別内訳）を決定し `7114eec` で実装済み。2026-09-02に登録番号印字・軽減税率の`*`印・税率別内訳の印字位置を実装し、**実DBの税率混在請求書（得意先100590 / 2022-04-30）でPDF突合済み** | `Doc/spec/archive/2026-09-02_月次請求書_適格請求書対応_詳細設計.md` |
| P1 | D-07排他/順序（キャンセル仕様は決定済み）、必要な性能測定 | P0と並行可。D-11は2026-09-02決定・R4実装済み |
| 済 | 複数締日（最大3締日）の第2段階を実装・実データ確認・実機確認まで完了（2026-09-02） | `Doc/spec/archive/2026-09-02_複数締日対応_詳細設計.md` |
| 別Release | PostgreSQL/MariaDBの実DB接続・CRUD・3DB差分検証。SQLite 10.0のRelease判定とは分離 | 実DB環境、`changes()`対応方針 |

---

## 3. 領域別チェックリスト（大メニュー別）

### 3.1 ■ 店舗業務（8参照）

| 現在地 | 10.0実装予定 | 10.1予定 | 10.2以降 / 決定 |
|---|---|---|---|
| 店舗売上、在庫問合せ、棚卸、原価無し帳票は実装済み | 店舗売上→在庫→売上速報、棚卸入力→確定のロール別UAT。店舗ユーザーから原価列へ到達できないことを確認 | POS日別精算入力とLCVの顧客/ポイント利用。日次締め、過不足金、金種、取消、ポイント返品/失効を決定後に実装 | 外部CRM/MA |

### 3.2 ■ 倉庫業務（10参照）

| 現在地 | 10.0実装予定 | 10.1予定 | 10.2以降 / 決定 |
|---|---|---|---|
| 在庫、移動、棚卸、出荷処理、有効在庫照会の中核は実装済み。`店舗出荷依頼` と出荷指示明細書は未実装 | 配分確定→出荷処理→引当解除、積送出庫→移動受、欠品/強制完了の倉庫ロールUAT。 | 店舗出荷依頼、出荷指示明細書、納入一覧表。ピッキング、検品、欠品確定の担当境界を決める | HHT/物流連携。外部倉庫との送受信契約、再送、重複受信、障害時運用を先に決定 |

### 3.3 ■ マスター（16参照）

| 現在地 | 10.0実装予定 | 10.1予定 | 10.2以降 / 決定 |
|---|---|---|---|
| 基本設定、社員（担当区分・権限プロファイルを含む）、取引先、商品、イベント、CSV取込、上代変更、帳票、親子締日チェックのワーニング(E7。計4画面で非ブロック警告)、商品別消費税区分、**生地・付属マスタ（`MasterMaterial`、`Id_Tax`消費税区分・資材区分`KIJ`対応、旧DB変換`CnvMasterMaterial`）は実装済み** | 税、締日、支払条件、請求先親子、倉庫/店舗種別、ロールの必須値・禁止組合せを検証。生地・付属マスタの資材区分マッピング（旧HC$MASTER_SHKIJI区分CD→KIJ）を実データで確認 | リードタイム、在庫調整理由、絵型/画像管理、LCV顧客・ポイントに必要なマスター拡張 | 外部物流、輸入 |

### 3.4 ■ 予算（8参照）

| 現在地 | 10.0実装予定 | 10.1予定 | 10.2以降 / 決定 |
|---|---|---|---|
| 店舗・販売員予算の入力/帳票は実装済み | 10.0必須経路には新規実装を追加せず、主要帳票のスモーク確認と原価/売上集計値の整合確認 | 予算登録、実績対比、前年/累計の業務UAT。年度途中改定・複写・端数配賦を仕様化 | 予測、シミュレーション、AI計画は別要件 |

### 3.5 ■ 発注（11参照）

| 現在地 | 10.0実装予定 | 10.1予定 | 10.2以降 / 決定 |
|---|---|---|---|
| 発注、初回配分、残管理、完了/解除、納品予定照会、発注帳票、納品予定表PDF(`DeliveryScheduleTable`)、残管理表の納品予定日/納期遅れ列は実装済み | 発注→部分仕入→完納、手動完了/解除、完了後編集警告、納期遅れのUAT。仕入との数量・関連伝票を検算 | リードタイム自動算出、自動発注/補充 | 輸入発注、外部仕入先ポータル/EDIは外部契約後 |

### 3.6 ■ 受注・展示会（10参照）

| 現在地 | 10.0実装予定 | 10.1予定 | 10.2以降 / 決定 |
|---|---|---|---|
| 受注、残管理、完了/解除、納品予定照会、主要帳票は実装済み。 | 受注→配分→部分出荷→完納、手動完了/解除、完了後編集警告のUAT | 絵型一覧。画像保管、版管理、印刷品質を決定 | MDマップ、高度な展示会計画は画像/商品企画基盤と一体で要件化 |

### 3.7 ■ 仕入（7参照）

| 現在地 | 10.0実装予定 | 10.1予定 | 10.2以降 / 決定 |
|---|---|---|---|
| 仕入、返品、チェック/推移/伝票帳票、**生地・付属入力（`Tran02Material`、区分:仕入/仕入返品/値引/その他）は実装済み。買掛集計（`SummaryDb`）へ`Tran03Shiire`と合算済み。区分99（その他）は仕入ではなく消費税へ全額計上する専用ルール** | 発注参照仕入、返品、在庫反映、買掛生成、支払計算への接続をUAT。税・原価の境界値を検算。生地・付属仕入の買掛合算・区分99消費税計上ロジックをWPF実操作で確認 | 諸掛配賦、輸入原価、仕入先支払サイト拡張 | 輸入書類・通関・外部会計連携は別Release |

### 3.8 ■ 配分・出荷（21参照）

| 現在地 | 10.0実装予定 | 10.1予定 | 10.2以降 / 決定 |
|---|---|---|---|
| 店舗配分、受注配分入力、配分/引当/有効在庫照会、商品/得意先基準の出荷確定、出荷処理、滞留・欠品例外は実装済み。その他の代替入力と帳票は未実装。 | 店舗配分→確定→出荷→伝票作成→引当解除をMini-UAT。確定取消、全量欠品、在庫割れ、競合、再読込を確認 | 在庫品配分、得意先別配分、店舗出荷依頼、取置、移動指示、配分データメンテ、出荷帳票、自動補充2方式 | 外部倉庫の送受信とリアルタイム配分。10.1機能の利用実績を見て優先順位を再決定 |

### 3.9 ■ 売上（8参照）

| 現在地 | 10.0実装予定 | 10.1予定 | 10.2以降 / 決定 |
|---|---|---|---|
| 出荷売上、店舗売上、売上/金種チェック、納品書（適格返還請求書対応済み）、明細別消費税は実装済み。POS日別精算は仕様待ち | 出荷伝票から売上・在庫・売掛へつながること、返品/値引、標準/軽減/非課税混在、納品書発行済み、再発行をWPFでUAT | POS日別精算。保存テーブル、店舗日締め、金種差額、再締め、POS取込責任を決定 | EC/オムニチャネル、外部決済照合は別契約 |

### 3.10 ■ 在庫管理（22参照）

| 現在地 | 10.0実装予定 | 10.1予定 | 10.2以降 / 決定 |
|---|---|---|---|
| 在庫照会、移動、積送、強制調整/取消、調整理由区分(CHR)＋実績表PDF、棚卸入力/差異/帳票、自社締日基準の計上月は実装済み。棚卸開始・確定とRebuildも接続済み | 即時更新=Rebuild、20日締めの境界、積送中、棚卸開始→入力→差異→確定→再確定、強制調整取消・調整理由選択をMini-UAT。移行在庫との総数・金額照合 | 倉庫別棚卸日、受払表へのドリル遷移 | RFID/リアルタイム倉庫、複数拠点同期は外部連携と一体化 |

### 3.11 ■ 掛管理（請求・支払）（18参照）

| 現在地 | 10.0実装予定 | 10.1予定 | 10.2以降 / 決定 |
|---|---|---|---|
| 入出金、消込、元帳、掛残、請求/支払関連帳票（請求台帳・支払台帳〔いずれも発行控え〕を含む）、請求/支払計算画面、再発行、Rebuild時締日変更ブロック、請求書の税率別内訳（D-05。標準10%/軽減8%/非課税、不一致時は印刷中止）、最大3締日（計算・画面・保存時検査・期首残高CSV・solution外ツール）を実装済み。売掛/買掛再集計は自社締日基準 | 6帳票の数値突合とWPF実操作Mini-UAT（E7警告、20日締め境界を含むC-01〜C-09）は実運用相当DBで完了（2026-08-28）。複数締日は自動テスト324件全件成功・帳票8本確認済みで、実データでの最終確認（受入条件#8の差分0件確認と実機確認）を残す | 締日更新、親子請求の出力方式拡張 | 月末以外の税差額運用、電子請求、会計/銀行/EDI連携 |

### 3.12 ■ 分析（29参照）

| 現在地 | 10.0実装予定 | 10.1予定 | 10.2以降 / 決定 |
|---|---|---|---|
| 売上分析、店舗配布版、卸/販売員/経営分析の多くは実装済み。セット売上分析は仕様待ち。 | 10.0では代表帳票だけスモークし、入力・残高・月次のRelease Gateを優先。 | セット定義とセット売上分析、既存分析帳票の全件UAT、予算/在庫累計との突合 | オンラインモニタ、高度CSV/ABC/行動分析。分析基盤と性能要件を先に決定 |

### 3.13 ■ 月次・更新処理（15参照）

| 現在地 | 10.0実装予定 | 10.1予定 | 10.2以降 / 決定 |
|---|---|---|---|
| 棚卸開始/確定、在庫・掛再更新、請求/支払Rebuild、期首残高投入、自社締日基準の計上月、全締日・全期間再計算、期間集計化された残高と`PreviousBalance`表示は実装済み。明細別消費税と伝票税額再更新も実装済み。原価は未実装（D-01により10.1送り、10.0は`MasterShohin.TankaGenka`流用と決定済み） | D-01/D-06/D-08は決定済み（原価4項目は10.1、在庫/原価期首残は期首前日の棚卸入力データ）。残る10.0作業は引当・HHT・夜間再集計の実データ確認、複数締日の期首残高CSV・周辺ツール確認、D-07の処理順・ロック確定。自社締日境界は締日20/末日を実運用相当DBで確認済み（C-01/C-02） | 締日更新、原価変更登録、評価替、消費税再計算、積送中クリア。10.0の代替運用を解消 | 自動発注実行、一時処理。管理者専用機能は具体的な運用要求が出てから実装。データ整理は対象データ確定後に検討 |

### 3.14 ■ 外部連携（13参照）

| 現在地 | 10.0実装予定 | 10.1予定 | 10.2以降 / 決定 |
|---|---|---|---|
| HHTマスター出力・手動受信・データ更新・エラーデータ修正入力は実装済み。自社締日基準のHHT後再集計も実装済み。未更新データ印刷/削除と帳票3画面、物流4画面は未実装 | 10.0の配分→出荷はHHT非依存。HHT更新と自社締日境界の実DB画面通し確認を残す | 採用するHHT/POS機種・ファイル仕様が確定した範囲だけ実装 | 物流連携、再送/重複排除/監視、外部倉庫SLA。契約・接続先確定後 |

### 3.15 ■ 顧客管理（Loyal Customer / LCV）（9参照）

| 現在地 | 10.0実装予定 | 10.1予定 | 10.2以降 / 決定 |
|---|---|---|---|
| 9画面すべて未実装。現行メニュー名は `顧客管理`。01Masterの顧客マスタとは別に機能境界が未整理 | 10.0には含めずHidden/Previewを明示 | **LCV初回Release**。顧客マスタ/カルテ、ポイント3種、店舗/商品別設定、ポイント集計、RFMを実装。顧客統合、同意、個人情報、残高・失効・返品、移行、権限を先に決定しUATを行う | 外部CRM/MA、複数ブランド横断、高度顧客分析、リアルタイム配信 |

### 3.16 ■ システム管理（9参照）

| 現在地 | 10.0実装予定 | 10.1予定 | 10.2以降 / 決定 |
|---|---|---|---|
| ログイン、履歴、Scheduler、汎用メンテ、変換・保守ツールは実装済み。 | 権限とメニュー公開状態、Scheduler失敗通知、バックアップ/復旧、移行実行権限、監査ログ、運用手順をRelease Gate化 | 機能単位の権限、承認ワークフロー、運用ダッシュボード | 複数テナント、集中監視、外部ID連携は別要件 |

---

## 4. 10.0実装計画

### 4.1 月次・原価・移行の10.0候補

自社締日基準の計上月は `d9c815b` で実装済み。20日締め境界3ヶ月（C-01）と末日締め（C-02）は2026-08-28に実運用相当DBで確認済み。残る確認は、引当、HHT、夜間再集計を実DBコピーで突合することである。なお原価4項目（最終仕入原価/総平均原価/諸掛更新/消化仕入）はD-01（2026-08-28）により10.1へ移管することが決定済みで、10.0では`MasterShohin.TankaGenka`を原価として流用する（新規実装不要）。下表の原価4行は10.1着手時の実装前条件として残す。

| 項目 | 10.0で必要な理由 | 実装前条件 |
|---|---|---|
| 残高登録（**2026-08-21 実装済み**） | 期首売掛/買掛/請求/支払の移行に必要 | 専用画面 `BalanceRegistrationView`（①期首情報→②テンプレートCSV出力→③読込と登録）。`OpeningBalanceDb` が対象日付＋取引先の洗い替えを1トランザクションで行い再取込可。再計算は `MasterSysman.FiscalStartDate` 以降のみ（期首以前の行を凍結）。詳細は [詳細設計](archive/2026-08-21_残高登録処理_詳細設計.md)。**在庫/原価の期首残は未対応** |
| 最終仕入原価 | 仕入後の原価・在庫金額に必要 | 対象仕入区分、返品/値引、適用日 |
| 総平均原価 | 月末在庫評価と粗利に必要 | 旧算式の`TQ`、ゼロ在庫、負在庫、再計算 |
| 諸掛更新 | 輸入・付随費用を原価へ含める場合に必要 | 配賦基準、端数、取消、外貨 |
| 消化仕入 | 旧CV.net業務を継続する場合に必要 | 対象会社/店舗、算式、会計との責務 |

### 4.2 10.0実装の推奨順

1. D-09の実施体制と証跡様式を確定する。
2. 実DBコピーで自社締日境界、移行変換、請求・支払・在庫の数値を突合する。
3. WPFで請求/支払計算、E7、明細別税、Rebuild、20日締め境界をMini-UATする。
4. `BalanceRegistrationView` で期首残高を登録し、移行リハーサルを実施する。在庫/原価の期首残の投入元を決める。
5. D-01/D-04/D-05/D-07を決定し、原価・税率別請求・月次処理順のRelease範囲を固定する。

---

## 5. 必要な仕様決定

| ID | 決定事項 | 選択肢・論点 | 決定期限 |
|---|---|---|---|
| D-01 | 10.0の月次・原価範囲 | **2026-08-28 決定**：最終仕入原価・総平均原価・諸掛更新・消化仕入の原価4項目は**10.1**とする。10.0は`MasterShohin.TankaGenka`（原価単価）をそのまま原価として採用する（既存コードに`Gedai`へ転記する経路あり、例`CvServer/Services/PointOfSaleService.cs:160`。新規実装不要） | 決定済み |
| D-02 | 請求・支払をRebuildへ含めるか（決定・実装済み） | 請求/支払Rebuildを含め、通常更新値=Rebuild値を10.0原則とする。締日変更検出時は全要求を送信しない | 実装済み。実DB/UATで確認 |
| D-03 | 通常再計算と再発行の区別（決定・実装済み） | 通常再計算/Rebuildは番号維持で冪等、明示的再発行だけ`Renban+1`とする | 実装済み。帳票/UATで確認 |
| D-04 | 請求書の親子出力 | **2026-08-28 決定**：10.0は親子出力なし（単独請求のみ） | 決定済み |
| D-05 | 適格請求書要件 | **2026-08-28 決定・実装**：税率別内訳（標準10%/軽減8%/非課税）は請求書印刷時に`Tran00Uriage.Jmeisai`の保存済み`TaxRate`/`Tax`から集計する。`SummaryUriSei`への列追加は行わない。明細集計と請求集計が一致しない、不正JSON、未対応税率の場合は印刷を中止する。詳細は[詳細設計](archive/2026-08-28_D05_請求書税率別内訳_詳細設計.md) | 実DBの税率混在請求書でPDF突合 |
| D-06 | 総平均原価の`TQ` | **2026-08-28 決定**：`TQ`はcv10では無視する（旧cvnetでは調整金額の意味だったが、cv10の総平均原価計算では扱わない） | 決定済み |
| D-07 | 月次処理の順序とロック | 請求/支払、在庫累計、原価、税、締めの順番。処理中入力、部分失敗、再実行。**2026-08-28 追加決定**：集計系Rebuild（在庫等）のキャンセルは実際にはサーバー処理を中断しない仕様を許容する（クラッシュ・DB破損なし） | 順序・ロックは10.0 Release判定前、キャンセル仕様は決定済み |
| D-08 | 期首残高と移行 | **2026-08-21 決定・実装**：期首売掛/買掛/請求/支払残は専用画面 `BalanceRegistrationView` で登録する（汎用CSV取込では再取込のuk1違反・期首前チェック・繰越の引き継ぎ方の差異・部分失敗を担保できないため）。基準日は `MasterSysman.FiscalStartDate` で、再計算は期首以降のみ。詳細は [詳細設計](archive/2026-08-21_残高登録処理_詳細設計.md)。**2026-08-28 決定**：在庫/原価の期首残は、期首日前日時点の棚卸入力データを採用する（例：期首2026/04/01なら2026/03/31棚卸データ。専用画面・外部CSVは不要、既存の棚卸機能に乗せる） | 完了 |
| D-09 | Mini-UAT責任者 | **2026-08-28 決定：合格とみなす**。実施はAIが担当し、[VM駆動UATハーネス](archive/2026-08-27_Mini-UAT自動化計画_VM駆動ハーネス.md)によりPhase 0/1（Mini-UAT相当）を完遂。[結果レポート](../test/UatVm_Phase01_20260828_テスト結果レポート.md)をもってユーザーが最終承認 | 完了 |
| D-10 | メニュー公開状態 | **2026-08-28 決定**：実効権限・メニュー公開状態は10.0リリース対象外とする。権限プロファイルのデータ基盤はあるが、判定・直接起動防止は将来Releaseで扱う | 10.0対象外 |
| D-11 | POS/HHT/物流の10.0扱い | **2026-09-02 決定**：POS/HHT/物流を10.0で運用する。HHT・物流は共通消費税計算を実装済み。POSはR4として `PointOfSaleService` の売上・返品・取消へ税計算を実装した。実POS端末との結合テストはPOS側プロジェクトの責務でありcv10対象外 | 決定・実装済み |
| D-12 | 倉庫別棚卸日 | 月末一律を10.0仕様とするか、倉庫別日付を10.1で追加するか | 在庫UAT前 |
| D-13 | 在庫調整理由 | **2026-08-19 決定・実装**：区分マスター（`MasterMeisho` Kubun=`CHR`、10入庫/20紛失/21盗難/22破損/23検品ミス/29その他）、強制調整入力で**必須選択**。コード帯で増減方向（10〜19=＋/20〜29=−）。取消は実績照会から。権限は共通ロールに従う | 完了 |
| D-14 | 個人情報・LCV責務 | CV10の顧客マスタとLCVの統合、同意、削除、ポイント会計、返品・失効、移行 | 10.1設計着手前 |
| D-15 | 旧CV.net機能の対象外承認 | 勤怠、輸入、CPA、外部物流をCV10で継承するか別製品へ委ねるか。LCVは10.1実装対象として別途D-14で確定。なおCPAは2026-08-19にメニュー・空画面一式を削除済み（継承判断そのものは未決のまま） | 10.1計画確定前 |
| D-16 | 自社締日基準の計上月 | `MasterSysman.ShimeBi` を基準に、日が締日を超えたら翌月、99は当月とする。対象は在庫 `DenDay`、売掛/買掛 `KakeDay`、引当、HHT、夜間再集計 | **実装済み**。実DBコピー・WPF境界UAT・締日変更後の再計算運用を確認 |

決定結果は「採用案、理由、代替、影響、Given/When/Then、決定者、決定日」を残す。

---

## 6. 最小UATに向けての不足項目

### 6.1 UAT基盤の不足

| 不足 | 必要な対応 |
|---|---|
| 実施体制 | D-09で決定済み（2026-08-28）: 実施はAI（VM駆動UATハーネス）、ユーザーが最終承認 |
| テストデータ | 発注・受注・仕入・支払・配分の正常/部分/取消/例外データを新規作成。移行データだけに依存しない |
| 期待値 | 数量、在庫、引当、残、税（標準/軽減/非課税）、原価、繰越、予定日を表計算等で事前算出し、DB値と帳票値を比較する |
| 初期化 | シナリオごとのDBスナップショット、復元、再実行手順を用意する |
| 証跡 | 実施者、日時、入力、画面、出力PDF/CSV、DB検算、結果、課題IDを1件単位で保存する |
| 障害時運用 | Scheduler/ストリーム/帳票失敗、クライアント切断、二重実行、競合時の確認・再実行手順を用意する |
| 性能 | 店舗売上約343万件、移動約34万件を含む移行DBで月次・帳票・照会の時間上限を決める |
| 権限 | 標準/店舗/倉庫/管理者で見えるメニュー、実行可能操作、原価表示を確認する（D-10により10.0は対象外、10.1で実施） |

### 6.2 最小UATシナリオ

| UAT | Given / When | 必須確認 | 現在地 |
|---|---|---|---|
| UAT-01 発注・仕入 | 商品/仕入先/倉庫を登録し、発注10→仕入4→仕入6 | 発注残6→0、自動完了、在庫+10、買掛、返品、手動完了解除 | **2026-08-21通し実施でPASS**（`DerivedShohinColSiz`列順不整合を検出し修正済み）。**残: 修正後の新規商品登録からの再UAT**。結果詳細：`Doc/test/UAT01_20260821_テスト結果レポート.md` |
| UAT-02 受注・配分・出荷 | 受注10、在庫8、配分、確定、実出荷6/欠品2 | 有効在庫、在庫割れ、欠品、売上/移動伝票、引当解除、受注残 | 受注配分入力は2026-08-21実装済み（コミット1721f029）。通しUAT未実施 |
| UAT-03 移動 | 即時移動、積送出庫、移動受、未受取消 | 出庫元/入庫先/積送中、履歴、Rebuild一致 | 部品実装済・通し未実施 |
| UAT-04 棚卸 | 開始→入力→差異→確定→過去伝票修正→再確定 | 帳簿在庫固定、調整伝票、在庫一致、再確定 | 部品実装済・通し未実施 |
| UAT-05 売掛・請求 | 売上/返品/値引/その他(区分99)/入金→掛再更新→請求計算→請求書 | 内訳（その他=E11分離集計含む）、標準/軽減/非課税の税、繰越、請求番号(E10)、予定日、消込`*`、親子締日ワーニング(E7)、帳票一致 | 開発DBのテストデータで通し・数値突合（手計算一致）・冪等性・Rebuild時締日変更ブロック・E7実データ発火・帳票PDF目視まで完了。2026-08-28に実運用相当DBでVM駆動Mini-UAT（C-01〜C-08）がPASS（締日20境界3ヶ月・末日締め・E7親子警告・冪等性/再発行・締日変更ブロック・明細別税・入力不正）。**残: D-09レポートのユーザー最終承認** |
| UAT-06 買掛・支払 | 仕入/返品/支払→掛再更新→支払計算→支払明細。生地・付属仕入（`Tran02Material`。仕入/仕入返品/値引/その他）を含む | 内訳、標準/軽減/非課税の税、繰越、予定日、消込`*`、帳票一致、生地・付属仕入の買掛合算と区分99（その他=消費税へ全額計上）の検算 | 開発DBのテストデータで通し・数値突合（手計算一致）・境界パターン（支払超過/全額相殺/複数明細決済、仕入先005〜007）・帳票PDF目視まで完了。2026-08-28にC-09で生地・付属仕入（`Tran02Material`）の買掛合算と区分99の消費税全額計上を画面経由で検証済み。**残: 支払側の実スケール検証（実運用の仕入/支払元データ不足、005〜007は合成データ）、D-09レポートのユーザー最終承認** |
| UAT-07 月次・原価 | 自社締日20の7/20・7/21・8/20・8/21、末締め、前月残、当月仕入/売上/移動、月次更新 | 在庫・売掛・買掛の計上月、通常更新=再作成、引当、HHT、夜間再集計、原価、粗利、再実行、遡及修正 | `ClosingMonthCalculator` と集計経路のテストは完了。2026-08-28にC-01（締日20境界3ヶ月）・C-02（末日締め）・C-07（Rebuild締日変更ブロック）を実運用相当DBで確認済み。原価範囲はD-01で決定（原価4項目は10.1）。**残: 引当・HHT・夜間再集計の実データ確認、`summaryreconcile`シードの`Jmeisai`欠落修正（202607を含む在庫Rebuildの前提）** |
| UAT-08 移行 | 専用画面 `BalanceRegistrationView` で期首売掛/買掛/請求/支払残を登録し、代表取引を追加。移行再変換後に前月・当月をRebuild/自動実行 | 旧残高+新規取引、期首前の行が凍結され上書きされない、繰越が期首残を起点に積み上がる、Rate/Tax/Total/IsPay、旧処理区分18、総数/総額、帳票、rollback | 専用画面・再計算ガードは実装済み。**実機でマスター変換処理を実行し、Tran系13カテゴリ全件の件数・金額・数量突合まで完了（2026-09-03、検証OK。[結果](archive/2026-09-03_ConvertDbマスター変換_Tran系実データ突合_結果.md)）**。残: 同一手順2回目の再変換・差分突合、マスター系・Tax/Total/IsPay個別項目の突合、期首残高登録以降の通しUAT |
| UAT-09 権限・障害 | 各ロールでログインし、失敗/競合/切断を発生 | 非公開機能、原価秘匿、監査、復旧、二重実行防止 | 基準未決 |

### 6.3 Release判定

1. UAT-01〜09のうち10.0対象がすべて合格し、重大/高課題が0件。
2. 請求・支払・在庫・原価は、標準/軽減/非課税を含む画面値、DB値、帳票値、手計算値が一致。
3. 通常更新とRebuild、初回実行と再実行、取消前後、自社締日境界が一致。
4. 移行リハーサルを同一手順で2回成功し、件数・金額差異・税額差異が説明可能。
5. Preview/Hiddenを含むメニュー一覧と10.1以降の代替運用を利用者へ提示。

---

## 7. 更新ルール

- 本書は「現在地、期別計画、仕様決定、UAT Gate」を更新し、実装の詳細記録は各詳細設計と `Doc/aicoding_log.md` に置く。
- 大メニュー変更時は、参照数、重複View数、ロール別到達性を再集計する。
- 10.0から延期する項目は、代替手段、責任者、10.1/10.2の実施条件を必ず記載する。
- 実装済みへの変更は、コミット、テスト、実画面確認のいずれかを根拠にする。
