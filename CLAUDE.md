# CV10

作業規約は [AGENTS.md](AGENTS.md) に従う。以下は Claude Code 固有の補足のみ。

## スキル

- 共通スキルは `.agents/skills/<名前>/SKILL.md` にある。自動では読み込まれないので、該当する作業のときは `ls .agents/skills` で探して SKILL.md を読んでから着手する。
- `.claude/skills/` は Claude Code 限定のスキルのみを置く（現状は `graphify`。`/graphify` で起動）。
