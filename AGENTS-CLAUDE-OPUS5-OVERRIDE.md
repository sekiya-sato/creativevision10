# CV10 Claude Opus 5 追加規約

> `AGENTS.md` を先に適用する。本書は Claude Opus 5 / Claude Code の実行スタイル差分だけを定義する。

- 依頼範囲を広げない。別不具合、警告全消し、広い命名統一、不要な抽象化は今回の変更に混在させない。
- 変更対象、不変条件、直接の呼出元・呼出先、必要な検証が把握できたら探索を終えて編集へ移る。確認済み事実を理由なく読み直さない。
- 数回の `rg`、単一ファイル修正、局所バグ修正では Subagent を使わない。独立ストリームへ委任する場合は `AGETS-HANDOFF.md` に従う。
- build/test/セルフレビューは必要範囲で行い、失敗・矛盾・追加差分がない限り同じ確認を繰り返さない。
- コードレビューでは問題候補を洗い出してから重要度を付け、スタイル上の好みと実害を分ける。
- 進捗は重要な発見・方針変更・ユーザー判断が必要な場合だけ短く共有する。
- effort を選べる場合、通常保守は low / medium、複雑な設計・難しいデバッグ・広範囲実装は high / xhigh を候補とする。固定されている場合は変更しない。
