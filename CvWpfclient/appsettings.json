{
	"ConnectionStrings": {
		"Url": "https://localhost:5012" // 必須変更: CvServerが動作しているURL
	},
	"Application": {
		"Version": "0.8.6",
		"WeatherRegion": "Tokyo",
		"JmaWeatherAreaCode": "130000",
		"FitPosition": "Left-Bottom",
		"Limit": 800
	},
	"Logging": {
		"LogLevel": {
			"Default": "Warning"
		}
	},
	"NLog": {
		"targets": {
			"async": true,
			"logfile": {
				"type": "File",
				"fileName": "${specialfolder:folder=LocalApplicationData}/CreativeVision10/logs/clientlog.txt",
				"layout": "${longdate}|${level:uppercase=true}|${logger}|${message}",
				"archiveAboveSize": 10485760, // 10MB (10 * 1024 * 1024)
				"maxArchiveFiles": 10, // 古いログを10まで残す
				"archiveNumbering": "Rolling", // 1, 2, 3... と番号を振る
				"keepFileOpen": false // ファイルを掴みっぱなしにしない（リサイクルを確実にするため）
			},
			"logconsole": {
				"type": "Console"
			}
		},
		"rules": [
			{
				"logger": "*",
				"minLevel": "Debug",
				"writeTo": "logfile,logconsole"
			}
		],
		"shutdownTimeout": 5000
	}
}
