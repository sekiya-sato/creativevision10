<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:helpers="clr-namespace:CvWpfclient.Helpers">

	<Style TargetType="{x:Type DatePicker}">
		<Setter Property="helpers:DatePickerTodayButtonBehavior.IsEnabled" Value="True" />
	</Style>

	<Style x:Key="CvDatePickerTodayFooterStyle"
		TargetType="{x:Type Border}">
		<Setter Property="Padding" Value="10,8,10,10" />
		<Setter Property="BorderThickness" Value="0,1,0,0" />
		<Setter Property="BorderBrush" Value="{DynamicResource MaterialDesignDivider}" />
		<Setter Property="Background" Value="{DynamicResource MaterialDesignPaper}" />
	</Style>

	<Style x:Key="CvDatePickerTodayButtonStyle"
		BasedOn="{StaticResource MaterialDesignOutlinedButton}"
		TargetType="{x:Type Button}">
		<Setter Property="MinWidth" Value="88" />
		<Setter Property="Padding" Value="14,4" />
		<Setter Property="HorizontalAlignment" Value="Right" />
		<Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidBrush}" />
	</Style>

</ResourceDictionary>
