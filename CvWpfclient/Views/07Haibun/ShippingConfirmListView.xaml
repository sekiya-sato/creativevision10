<helpers:BaseWindow x:Class="CvWpfclient.Views._07Haibun.ShippingConfirmListView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:helpers="clr-namespace:CvWpfclient.Helpers" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:vm="clr-namespace:CvWpfclient.ViewModels._07Haibun"
	Title="{Binding Title}"
	Width="1280" Height="800"
	Background="{DynamicResource AppCommonBackgroundBrush}"
	FontSize="14"
	Foreground="{DynamicResource MaterialDesignBody}"
	TextOptions.TextFormattingMode="Display" WindowStartupLocation="CenterScreen"
	mc:Ignorable="d">
	<helpers:BaseWindow.DataContext>
		<vm:ShippingConfirmListViewModel />
	</helpers:BaseWindow.DataContext>

	<helpers:BaseWindow.InputBindings>
		<KeyBinding Command="{Binding SearchCommand}"
			Gesture="F5" />
		<KeyBinding Command="{Binding ExitCommand}"
			Gesture="Esc" />
	</helpers:BaseWindow.InputBindings>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<materialDesign:ColorZone Grid.Row="0"
			Padding="12,8"
			materialDesign:ElevationAssist.Elevation="Dp4" Mode="PrimaryMid">
			<StackPanel Orientation="Horizontal">
				<Button Margin="0,0,8,0"
					Command="{Binding ExitCommand}"
					Style="{StaticResource ToolCommandButton}"
					ToolTip="閉じる (Esc)">
					<materialDesign:PackIcon Kind="ArrowLeft" />
				</Button>
				<materialDesign:PackIcon Width="26" Height="26"
					VerticalAlignment="Center"
					Kind="AlertRhombusOutline" />
				<TextBlock Margin="10,0,0,0" VerticalAlignment="Center"
					FontSize="18" FontWeight="SemiBold"
					Text="{Binding Title}" />
				<TextBlock Margin="16,0,0,0" VerticalAlignment="Center"
					Opacity="0.8" Text="確定済みなのに出荷処理されない滞留を検出し、確定取消／強制完了で例外処理します。" />
			</StackPanel>
		</materialDesign:ColorZone>

		<Border Grid.Row="1"
			Padding="12"
			Background="{DynamicResource MaterialDesignPaper}">
			<StackPanel Orientation="Horizontal">
				<TextBlock Margin="0,0,8,0" VerticalAlignment="Center"
					Text="表示" />
				<ComboBox Width="120"
					ItemsSource="{Binding ViewKinds}"
					SelectedItem="{Binding ViewKind}"
					Style="{StaticResource FormComboBox}" />
				<TextBlock Margin="24,0,8,0" VerticalAlignment="Center"
					Text="確定日" />
				<TextBox Width="140" Height="32"
					Style="{StaticResource MaterialDesignTextBox}"
					Text="{Binding KakuteiFromText, UpdateSourceTrigger=PropertyChanged}" />
				<TextBlock Margin="8,0" VerticalAlignment="Center"
					Text="～" />
				<TextBox Width="140" Height="32"
					Style="{StaticResource MaterialDesignTextBox}"
					Text="{Binding KakuteiToText, UpdateSourceTrigger=PropertyChanged}" />
				<TextBlock Margin="24,0,8,0" VerticalAlignment="Center"
					Text="倉庫" />
				<Button Width="60"
					Command="{Binding SelectSokoCommand}"
					Content="選択"
					Style="{StaticResource MaterialDesignOutlinedButton}" />
				<TextBox Width="100" Height="32"
					Margin="8,0,0,0"
					Style="{StaticResource MaterialDesignTextBox}"
					Text="{Binding SokoCode, UpdateSourceTrigger=PropertyChanged}" />
				<TextBlock Margin="24,0,8,0" VerticalAlignment="Center"
					Text="滞留日数≥" />
				<TextBox Width="60" Height="32"
					HorizontalContentAlignment="Right"
					Style="{StaticResource MaterialDesignTextBox}"
					Text="{Binding StagnationDaysText, UpdateSourceTrigger=PropertyChanged}" />
				<CheckBox Margin="16,0,0,0" VerticalAlignment="Center"
					Content="予定日超過のみ"
					IsChecked="{Binding OverdueOnly}" />
				<TextBlock Margin="24,0,8,0" VerticalAlignment="Center"
					Text="件数上限" />
				<TextBox Width="70" Height="32"
					Style="{StaticResource MaterialDesignTextBox}"
					Text="{Binding MaxCountText, UpdateSourceTrigger=PropertyChanged}" />
				<Button Margin="16,0,0,0"
					Command="{Binding SearchCommand}"
					IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"
					Style="{StaticResource MaterialDesignFlatMidBgButton}"
					ToolTip="検索 (F5)">
					<StackPanel Orientation="Horizontal">
						<materialDesign:PackIcon VerticalAlignment="Center"
							Kind="Magnify" />
						<TextBlock Margin="8,0,0,0" VerticalAlignment="Center"
							FontSize="16" Text="検索実行" />
					</StackPanel>
				</Button>
			</StackPanel>
		</Border>

		<DockPanel Grid.Row="2"
			Margin="12">
			<StackPanel Margin="0,0,0,6"
				DockPanel.Dock="Top" Orientation="Horizontal">
				<TextBlock FontSize="16" FontWeight="SemiBold" Text="対象一覧" />
				<TextBlock Margin="16,0,0,0" VerticalAlignment="Center"
					Text="{Binding CheckedCount, StringFormat='チェック {0:N0} 件'}" />
				<Button Margin="24,0,0,0"
					Command="{Binding CancelConfirmCommand}"
					IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"
					Style="{StaticResource MaterialDesignOutlinedButton}"
					ToolTip="選択した滞留の確定を取り消して未確定へ戻す">
					<TextBlock FontSize="16" Text="確定取消" />
				</Button>
				<Button Margin="12,0,0,0"
					Command="{Binding ForceCompleteCommand}"
					IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"
					Style="{StaticResource MaterialDesignFlatMidBgButton}"
					ToolTip="選択した滞留を出荷せず完了（全量欠品）にし引当を解除する">
					<StackPanel Orientation="Horizontal">
						<materialDesign:PackIcon VerticalAlignment="Center"
							Kind="CloseBoxOutline" />
						<TextBlock Margin="8,0,0,0" VerticalAlignment="Center"
							FontSize="16" Text="強制完了" />
					</StackPanel>
				</Button>
				<Button Margin="24,0,0,0"
					Command="{Binding CheckAllCommand}"
					Style="{StaticResource MaterialDesignOutlinedButton}">
					<TextBlock FontSize="16" Text="全チェック" />
				</Button>
				<Button Margin="12,0,0,0"
					Command="{Binding UncheckAllCommand}"
					Style="{StaticResource MaterialDesignOutlinedButton}">
					<TextBlock FontSize="16" Text="全解除" />
				</Button>
				<Button Margin="12,0,0,0"
					Command="{Binding ExportCsvCommand}"
					Style="{StaticResource MaterialDesignOutlinedButton}"
					ToolTip="表示中の一覧をCSVへ出力">
					<StackPanel Orientation="Horizontal">
						<materialDesign:PackIcon Margin="0,0,6,0" VerticalAlignment="Center"
							Kind="FileDelimitedOutline" />
						<TextBlock FontSize="16" Text="CSV出力" />
					</StackPanel>
				</Button>
				<Button Margin="12,0,0,0"
					Command="{Binding ClearConditionsCommand}"
					Style="{StaticResource MaterialDesignOutlinedButton}">
					<TextBlock FontSize="16" Text="条件クリア" />
				</Button>
			</StackPanel>
			<DataGrid AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="False"
				ItemsSource="{Binding Rows}"
				SelectionMode="Single" SelectionUnit="FullRow" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling">
				<DataGrid.Columns>
					<DataGridTemplateColumn Width="50"
						Header="選択">
						<DataGridTemplateColumn.CellTemplate>
							<DataTemplate>
								<CheckBox HorizontalAlignment="Center" VerticalAlignment="Center"
									IsChecked="{Binding IsChecked, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
							</DataTemplate>
						</DataGridTemplateColumn.CellTemplate>
					</DataGridTemplateColumn>
					<DataGridTextColumn Width="100"
						Binding="{Binding KakuteiDayDisplay}"
						Header="確定日" IsReadOnly="True" />
					<DataGridTextColumn Width="100"
						Binding="{Binding NouhinDayDisplay}"
						Header="納品予定日" IsReadOnly="True" />
					<DataGridTextColumn Width="80"
						Binding="{Binding ElapsedDays, StringFormat={}{0:N0}日}"
						Header="経過日数" IsReadOnly="True">
						<DataGridTextColumn.ElementStyle>
							<Style TargetType="TextBlock">
								<Setter Property="HorizontalAlignment" Value="Right" />
							</Style>
						</DataGridTextColumn.ElementStyle>
					</DataGridTextColumn>
					<DataGridTextColumn Width="100"
						Binding="{Binding OverdueDisplay}"
						ElementStyle="{StaticResource OverdueTextBlock}"
						Header="予定日超過" IsReadOnly="True" />
					<DataGridTextColumn Width="170"
						Binding="{Binding SokoDisplay}"
						Header="倉庫" IsReadOnly="True" />
					<DataGridTextColumn Width="190"
						Binding="{Binding TenpoDisplay}"
						Header="出荷先" IsReadOnly="True" />
					<DataGridTextColumn Width="80"
						Binding="{Binding DenKindDisplay}"
						Header="種別" IsReadOnly="True" />
					<DataGridTextColumn Width="190"
						Binding="{Binding ShohinDisplay}"
						Header="商品" IsReadOnly="True" />
					<DataGridTextColumn Width="150"
						Binding="{Binding ColSizDisplay}"
						Header="色/サイズ" IsReadOnly="True" />
					<DataGridTextColumn Width="80"
						Binding="{Binding Su, StringFormat={}{0:N0}}"
						Header="指示数" IsReadOnly="True">
						<DataGridTextColumn.ElementStyle>
							<Style TargetType="TextBlock">
								<Setter Property="HorizontalAlignment" Value="Right" />
							</Style>
						</DataGridTextColumn.ElementStyle>
					</DataGridTextColumn>
					<DataGridTextColumn Width="80"
						Binding="{Binding JitsuSu, StringFormat={}{0:N0}}"
						Header="実数量" IsReadOnly="True">
						<DataGridTextColumn.ElementStyle>
							<Style TargetType="TextBlock">
								<Setter Property="HorizontalAlignment" Value="Right" />
							</Style>
						</DataGridTextColumn.ElementStyle>
					</DataGridTextColumn>
					<DataGridTextColumn Width="80"
						Binding="{Binding ShortSu, StringFormat={}{0:N0}}"
						Header="欠品" IsReadOnly="True">
						<DataGridTextColumn.ElementStyle>
							<Style TargetType="TextBlock">
								<Setter Property="HorizontalAlignment" Value="Right" />
							</Style>
						</DataGridTextColumn.ElementStyle>
					</DataGridTextColumn>
				</DataGrid.Columns>
			</DataGrid>
		</DockPanel>

		<Border Grid.Row="3"
			Padding="12,8"
			Background="{DynamicResource MaterialDesignPaper}">
			<DockPanel>
				<Button Command="{Binding ExitCommand}"
					DockPanel.Dock="Right"
					Style="{StaticResource MaterialDesignOutlinedButton}"
					ToolTip="閉じる (Esc)">
					<TextBlock FontSize="16" Text="閉じる" />
				</Button>
				<TextBlock VerticalAlignment="Center"
					Text="{Binding Message}"
					TextWrapping="Wrap" />
			</DockPanel>
		</Border>
	</Grid>
</helpers:BaseWindow>
