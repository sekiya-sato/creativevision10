<?xml version="1.0" encoding="utf-8" ?>
<helpers:BaseWindow x:Class="CvWpfclient.Views._00System.ConvertSelectedView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:helpers="clr-namespace:CvWpfclient.Helpers" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:vm="clr-namespace:CvWpfclient.ViewModels._00System"
	Title="旧DBからの選択変換処理"
	Width="1150" Height="800"
	Background="{DynamicResource AppCommonBackgroundBrush}"
	FontSize="16"
	Foreground="{DynamicResource MaterialDesignBody}"
	ResizeMode="NoResize" TextOptions.TextFormattingMode="Display"
	mc:Ignorable="d">
	<helpers:BaseWindow.DataContext>
		<vm:ConvertSelectedViewModel />
	</helpers:BaseWindow.DataContext>

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>

		<materialDesign:ColorZone Grid.Row="0"
			Padding="14,10"
			materialDesign:ElevationAssist.Elevation="Dp4" Mode="PrimaryMid">
			<StackPanel Orientation="Horizontal">
				<materialDesign:PackIcon Width="30" Height="30"
					VerticalAlignment="Center"
					Kind="DatabaseImport" />
				<TextBlock Margin="10,0,0,0" VerticalAlignment="Center"
					FontSize="20" FontWeight="SemiBold" Text="旧DBからの選択変換処理" />
			</StackPanel>
		</materialDesign:ColorZone>

		<Grid Grid.Row="1"
			Margin="18">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto" />
				<RowDefinition Height="*" />
			</Grid.RowDefinitions>
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="3*" />
				<ColumnDefinition Width="2*" />
			</Grid.ColumnDefinitions>

			<materialDesign:Card Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
				Padding="24"
				materialDesign:ElevationAssist.Elevation="Dp2" UniformCornerRadius="8">
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
						<RowDefinition Height="Auto" />
					</Grid.RowDefinitions>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="170" />
						<ColumnDefinition Width="*" />
					</Grid.ColumnDefinitions>

					<StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
						Margin="0,0,0,12"
						Orientation="Horizontal">
						<materialDesign:PackIcon Width="24" Height="24"
							VerticalAlignment="Center"
							Kind="Cog" />
						<TextBlock Margin="8,0,0,0" VerticalAlignment="Center"
							FontSize="17" FontWeight="SemiBold" Text="実行設定" />
					</StackPanel>

					<TextBlock Grid.Row="1" Grid.Column="0"
						Style="{StaticResource SettingLabel}"
						Text="テーブル初期化" />
					<StackPanel Grid.Row="1" Grid.Column="1"
						VerticalAlignment="Center"
						Orientation="Horizontal">
						<RadioButton Content="しない" GroupName="InitDbGroupSelected"
							IsChecked="{Binding IsInitDb, Converter={StaticResource InverseBooleanConverter}}" />
						<RadioButton Margin="16,0,0,0"
							Content="する" GroupName="InitDbGroupSelected"
							IsChecked="{Binding IsInitDb}" />
					</StackPanel>

					<TextBlock Grid.Row="2" Grid.Column="0"
						Style="{StaticResource SettingLabel}"
						Text="進捗" />
					<ProgressBar Grid.Row="2" Grid.Column="1"
						Height="18"
						Maximum="100" Minimum="0"
						Value="{Binding ProgressValue}" />

					<TextBlock Grid.Row="3" Grid.Column="0"
						Margin="0,12,0,0"
						Style="{StaticResource SettingLabel}"
						Text="選択操作" />
					<StackPanel Grid.Row="3" Grid.Column="1"
						Margin="0,12,0,0" VerticalAlignment="Center"
						Orientation="Horizontal">
						<Button Width="110" Height="32"
							Command="{Binding SelectAllCommand}"
							Content="全選択"
							Style="{StaticResource MaterialDesignOutlinedButton}" />
						<Button Width="110" Height="32"
							Margin="10,0,0,0"
							Command="{Binding ClearSelectionCommand}"
							Content="全解除"
							Style="{StaticResource MaterialDesignOutlinedButton}" />
					</StackPanel>
				</Grid>
			</materialDesign:Card>

			<GroupBox Grid.Row="1" Grid.Column="0"
				Margin="0,18,9,0"
				Header="変換プログラム一覧">
				<DataGrid Margin="8"
					AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" CanUserReorderColumns="False" CanUserResizeColumns="False" CanUserSortColumns="False" IsReadOnly="True"
					ItemsSource="{Binding Tasks}"
					RowHeaderWidth="0" SelectionMode="Single" SelectionUnit="Cell">
					<DataGrid.Columns>
						<DataGridTemplateColumn Width="80"
							CanUserReorder="False" CanUserResize="False" CanUserSort="False" Header="選択">
							<DataGridTemplateColumn.CellTemplate>
								<DataTemplate>
									<CheckBox HorizontalAlignment="Center" VerticalAlignment="Center"
										IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
										Style="{StaticResource MaterialDesignCheckBox}" />
								</DataTemplate>
							</DataGridTemplateColumn.CellTemplate>
						</DataGridTemplateColumn>
						<DataGridTextColumn Width="*"
							Binding="{Binding DisplayName}"
							Header="プログラム名" />
					</DataGrid.Columns>
				</DataGrid>
			</GroupBox>

			<GroupBox Grid.Row="1" Grid.Column="1"
				Margin="9,18,0,0"
				Header="ストリーミングログ">
				<ListBox Margin="8"
					ItemsSource="{Binding StreamMessages}">
					<ListBox.ItemTemplate>
						<DataTemplate>
							<TextBlock Foreground="{DynamicResource MaterialDesignBody}"
								Text="{Binding}"
								TextWrapping="Wrap" />
						</DataTemplate>
					</ListBox.ItemTemplate>
				</ListBox>
			</GroupBox>
		</Grid>

		<Border Grid.Row="2"
			Padding="18,12"
			Background="{DynamicResource MaterialDesignPaper}">
			<StackPanel HorizontalAlignment="Right"
				Orientation="Horizontal">
				<Button Width="128" Height="38"
					Margin="0,0,10,0"
					Command="{Binding ExecuteCommand}"
					IsDefault="True"
					Style="{StaticResource MaterialDesignRaisedButton}">
					<StackPanel Orientation="Horizontal">
						<materialDesign:PackIcon VerticalAlignment="Center"
							Kind="Play" />
						<TextBlock Margin="6,0,0,0" VerticalAlignment="Center"
							Text="実行" />
					</StackPanel>
				</Button>
				<Button Width="128" Height="38"
					Command="{Binding ExitCommand}"
					IsCancel="True"
					Style="{StaticResource MaterialDesignOutlinedButton}">
					<StackPanel Orientation="Horizontal">
						<materialDesign:PackIcon VerticalAlignment="Center"
							Kind="ArrowLeft" />
						<TextBlock Margin="6,0,0,0" VerticalAlignment="Center"
							Text="戻る" />
					</StackPanel>
				</Button>
			</StackPanel>
		</Border>
	</Grid>
</helpers:BaseWindow>
