<helpers:BaseWindow x:Class="CvWpfclient.Views.Sub.SelectPostalAddressView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:helpers="clr-namespace:CvWpfclient.Helpers"
	xmlns:local="clr-namespace:CvWpfclient.Views.Sub" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:vm="clr-namespace:CvWpfclient.ViewModels.Sub"
	Title="{Binding Title}"
	Width="680" Height="520"
	helpers:OwnerWindowPlacement.IsEnabled="True" helpers:OwnerWindowPlacement.LeftOffset="-40" helpers:OwnerWindowPlacement.TopOffset="-20" BorderBrush="DarkSlateGray"
	BorderThickness="0.5,0,1.5,1.5" FontSize="18" WindowStartupLocation="Manual"
	mc:Ignorable="d">
	<helpers:BaseWindow.DataContext>
		<vm:SelectPostalAddressViewModel />
	</helpers:BaseWindow.DataContext>
	<Grid Background="{DynamicResource AppCommonBackgroundBrush}">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<materialDesign:ColorZone Grid.Row="0"
			Padding="12,8"
			materialDesign:ElevationAssist.Elevation="Dp4" Mode="PrimaryMid">
			<DockPanel>
				<StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
					<Button x:Name="DefaultButton"
						Command="{Binding DoSelectCommand}"
						IsDefault="True"
						Style="{StaticResource MaterialDesignToolForegroundButton}">
						<StackPanel Orientation="Horizontal">
							<materialDesign:PackIcon VerticalAlignment="Center"
								Kind="Check" />
							<TextBlock Margin="6,0,0,0" VerticalAlignment="Center"
								Text="選択" />
						</StackPanel>
					</Button>
				</StackPanel>
				<StackPanel Orientation="Horizontal">
					<Button Margin="0,0,8,0"
						Command="{Binding ExitCommand}"
						IsCancel="True"
						Style="{StaticResource MaterialDesignToolForegroundButton}"
						ToolTip="閉じる">
						<materialDesign:PackIcon Kind="ArrowLeft" />
					</Button>
					<TextBlock VerticalAlignment="Center"
						FontSize="20" FontWeight="SemiBold"
						Text="{Binding Title}" />
				</StackPanel>
			</DockPanel>
		</materialDesign:ColorZone>
		<Grid Grid.Row="1"
			Margin="12">
			<materialDesign:Card Padding="0"
				materialDesign:ElevationAssist.Elevation="Dp2" UniformCornerRadius="8">
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto" />
						<RowDefinition Height="*" />
					</Grid.RowDefinitions>
					<Border Grid.Row="0"
						Padding="12,6"
						Background="{DynamicResource MaterialDesignToolBarBackground}"
						CornerRadius="8,8,0,0">
						<StackPanel Orientation="Vertical">
							<StackPanel Orientation="Horizontal">
								<TextBlock Foreground="{DynamicResource MaterialDesignBodyLight}"
									Text="{Binding Current.PostalCode}" />
								<TextBlock Foreground="{DynamicResource MaterialDesignBodyLight}"
									Text="  " />
								<TextBlock Foreground="{DynamicResource MaterialDesignBodyLight}"
									Text="{Binding Current.FullAddress}" />
							</StackPanel>
						</StackPanel>
					</Border>
					<DataGrid x:Name="SelectGrid"
						Grid.Row="1"
						helpers:DataGridDoubleClick.Command="{Binding DoSelectCommand}"
						helpers:DataGridEnterCommand.TargetButton="{Binding ElementName=DefaultButton}"
						materialDesign:DataGridAssist.CellPadding="12,6" materialDesign:DataGridAssist.ColumnHeaderPadding="12,8" FrozenColumnCount="1" GridLinesVisibility="Horizontal"
						HorizontalGridLinesBrush="{DynamicResource MaterialDesignDivider}"
						IsSynchronizedWithCurrentItem="True"
						ItemsSource="{Binding ListData}"
						RowHeight="35"
						SelectedItem="{Binding Current}"
						SelectionMode="Single"
						Style="{StaticResource MaterialDesignDataGrid}"
						VerticalScrollBarVisibility="Visible">
						<DataGrid.ColumnHeaderStyle>
							<Style BasedOn="{StaticResource MaterialDesignDataGridColumnHeader}"
								TargetType="{x:Type DataGridColumnHeader}">
								<Setter Property="Background" Value="{DynamicResource PrimaryHueMidBrush}" />
								<Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidForegroundBrush}" />
								<Setter Property="Padding" Value="12,8" />
								<Setter Property="FontWeight" Value="SemiBold" />
							</Style>
						</DataGrid.ColumnHeaderStyle>
						<DataGrid.Columns>
							<DataGridTextColumn x:Name="Col01"
								Width="100"
								Binding="{Binding Path=PostalCode}"
								CanUserReorder="True" CanUserResize="True" CanUserSort="True" Header="郵便番号" />
							<DataGridTextColumn x:Name="Col02"
								Width="500"
								Binding="{Binding Path=FullAddress}"
								CanUserReorder="True" CanUserResize="True" CanUserSort="True" Header="住所" />
						</DataGrid.Columns>
					</DataGrid>
				</Grid>
			</materialDesign:Card>
		</Grid>
	</Grid>
</helpers:BaseWindow>