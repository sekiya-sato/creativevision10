<helpers:BaseWindow x:Class="CvWpfclient.Views._02Yosan.ShopBrandBudgetMasterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:helpers="clr-namespace:CvWpfclient.Helpers"
    xmlns:local="clr-namespace:CvWpfclient.Views" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:CvWpfclient.ViewModels._02Yosan"
    Title="{Binding Title}"
    Width="1244" Height="900"
    Background="{DynamicResource AppCommonBackgroundBrush}"
    Foreground="{DynamicResource MaterialDesignBody}"
    TextOptions.TextFormattingMode="Display"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <helpers:BaseWindow.DataContext>
        <vm:ShopBrandBudgetMasterViewModel />
    </helpers:BaseWindow.DataContext>
    <helpers:BaseWindow.InputBindings>
        <KeyBinding Command="{Binding ExitCommand}" Gesture="Esc" />
    </helpers:BaseWindow.InputBindings>
    <helpers:BaseWindow.Resources>
        <Style x:Key="MenteSearchTextBox" TargetType="{x:Type helpers:SearchTextBox}">
            <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Window}}" />
            <Setter Property="Background" Value="{DynamicResource MaterialDesignPaper}" />
            <Setter Property="ButtonBackground" Value="{DynamicResource SearchButtonBackgroundBrush}" />
        </Style>
        <Style x:Key="StatusTextBlockStyle" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBodyLight}" />
        </Style>
    </helpers:BaseWindow.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <materialDesign:ColorZone Grid.Row="0" Padding="12,8" materialDesign:ElevationAssist.Elevation="Dp4" Mode="PrimaryMid">
            <DockPanel>
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button Margin="4,0" Command="{Binding CreateBudgetCommand}" Style="{StaticResource MaterialDesignToolForegroundButton}" ToolTip="予算作成">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon VerticalAlignment="Center" Kind="Calendar" />
                            <TextBlock Margin="6,0,0,0" VerticalAlignment="Center" Text="予算作成" />
                        </StackPanel>
                    </Button>
                    <Button Margin="4,0" Command="{Binding LoadBudgetCommand}" Style="{StaticResource MaterialDesignToolForegroundButton}" ToolTip="予算読込">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon VerticalAlignment="Center" Kind="DatabaseRefresh" />
                            <TextBlock Margin="6,0,0,0" VerticalAlignment="Center" Text="予算読込" />
                        </StackPanel>
                    </Button>
                    <Button Margin="4,0" Command="{Binding SaveBudgetCommand}" Style="{StaticResource MaterialDesignToolForegroundButton}" ToolTip="予算決定">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon VerticalAlignment="Center" Kind="ContentSave" />
                            <TextBlock Margin="6,0,0,0" VerticalAlignment="Center" Text="予算決定" />
                        </StackPanel>
                    </Button>
                    <Button Margin="4,0" Command="{Binding DeleteBudgetCommand}" Style="{StaticResource MaterialDesignToolForegroundButton}" ToolTip="予算削除">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon VerticalAlignment="Center" Kind="Delete" />
                            <TextBlock Margin="6,0,0,0" VerticalAlignment="Center" Text="予算削除" />
                        </StackPanel>
                    </Button>
                    <Button Margin="4,0" Command="{Binding AutoAllocateBudgetCommand}" Style="{StaticResource MaterialDesignToolForegroundButton}" ToolTip="自動予算配分">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon VerticalAlignment="Center" Kind="Calculator" />
                            <TextBlock Margin="6,0,0,0" VerticalAlignment="Center" Text="自動配分" />
                        </StackPanel>
                    </Button>
                    <Button Margin="4,0" Command="{Binding RecalculateTotalsCommand}" Style="{StaticResource MaterialDesignToolForegroundButton}" ToolTip="累計再計算">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon VerticalAlignment="Center" Kind="Sigma" />
                            <TextBlock Margin="6,0,0,0" VerticalAlignment="Center" Text="累計再計算" />
                        </StackPanel>
                    </Button>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Button Margin="0,0,8,0" Command="{Binding ExitCommand}" Style="{StaticResource MaterialDesignToolForegroundButton}" ToolTip="戻る (Esc)">
                        <materialDesign:PackIcon Kind="ArrowLeft" />
                    </Button>
                    <materialDesign:PackIcon Width="28" Height="28" VerticalAlignment="Center" Kind="Calendar" />
                    <TextBlock Margin="10,0,0,0" VerticalAlignment="Center" FontSize="20" FontWeight="SemiBold" Text="{Binding Title}" />
                </StackPanel>
            </DockPanel>
        </materialDesign:ColorZone>

        <materialDesign:Card Grid.Row="1" Margin="12,8,12,8" Padding="12" materialDesign:ElevationAssist.Elevation="Dp2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="280" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="150" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0" Margin="8,0,8,0" VerticalAlignment="Center" Text="年月" />
                <TextBox Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" Text="{Binding SelectedYearMonthString, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Grid.Row="0" Grid.Column="2" Margin="16,0,8,0" VerticalAlignment="Center" Text="店舗" />
                <DockPanel Grid.Row="0" Grid.Column="3">
                    <helpers:SearchTextBox Width="120" Height="36" Command="{Binding SelectShopCommand}" DockPanel.Dock="Left" Style="{StaticResource MenteSearchTextBox}" Text="{Binding SelectedShopCode, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Margin="10,0,0,0" VerticalAlignment="Center">
                        <TextBlock.Text>
                            <MultiBinding StringFormat="{}{0} {1}">
                                <Binding Path="SelectedShopCode" />
                                <Binding Path="SelectedShopName" />
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </DockPanel>
                <TextBlock Grid.Row="0" Grid.Column="4" Margin="16,0,8,0" VerticalAlignment="Center" Text="ブランド" />
                <DockPanel Grid.Row="0" Grid.Column="5">
                    <helpers:SearchTextBox Width="120" Height="36" Command="{Binding SelectBrandCommand}" DockPanel.Dock="Left" Style="{StaticResource MenteSearchTextBox}" Text="{Binding SelectedBrandCode, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Margin="10,0,0,0" VerticalAlignment="Center">
                        <TextBlock.Text>
                            <MultiBinding StringFormat="{}{0} {1}">
                                <Binding Path="SelectedBrandCode" />
                                <Binding Path="SelectedBrandName" />
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                </DockPanel>
                <TextBlock Grid.Row="0" Grid.Column="6" Margin="16,0,8,0" VerticalAlignment="Center" Text="土日指数" />
                <TextBox Grid.Row="0" Grid.Column="7" VerticalAlignment="Center" Text="{Binding SaturdaySundayCoefficient, StringFormat=F1, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Grid.Row="1" Grid.Column="0" Margin="8,8,8,0" VerticalAlignment="Center" Text="店舗月売上予算" />
                <TextBox Grid.Row="1" Grid.Column="1" Margin="0,8,0,0" VerticalAlignment="Center" HorizontalContentAlignment="Right" Text="{Binding MonthlyBudget, StringFormat={}{0:N0}, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Grid.Row="1" Grid.Column="2" Margin="16,8,8,0" VerticalAlignment="Center" Text="千円" />
            </Grid>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="2" Margin="12,0,12,8" Padding="0" materialDesign:ElevationAssist.Elevation="Dp2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Border Grid.Row="0" Padding="12,8" Background="{DynamicResource MaterialDesignToolBarBackground}">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Right" Style="{StaticResource StatusTextBlockStyle}" Text="{Binding Message}" />
                    </DockPanel>
                </Border>
                <DataGrid Grid.Row="1" Margin="8" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" CanUserReorderColumns="False" CanUserResizeRows="False" FrozenColumnCount="1" IsReadOnly="False" ItemsSource="{Binding DailyBudgets}" SelectedItem="{Binding SelectedDailyBudgetRow}" Style="{StaticResource MaterialDesignDataGrid}" VirtualizingPanel.IsVirtualizing="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Binding="{Binding Day}" Header="日付" IsReadOnly="True" />
                        <DataGridTextColumn Binding="{Binding DayOfWeek}" Header="曜日" IsReadOnly="True" />
                        <DataGridTextColumn Binding="{Binding SalesBudget, StringFormat={}{0:N0}}" Header="売上予算" />
                        <DataGridTextColumn Binding="{Binding RunningTotal, StringFormat={}{0:N0}}" Header="累計" IsReadOnly="True" />
                        <DataGridTextColumn Binding="{Binding Coefficient, StringFormat=F1}" Header="指数" IsReadOnly="True" />
                        <DataGridCheckBoxColumn Binding="{Binding IsHoliday}" Header="休業日" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </materialDesign:Card>

        <Border Grid.Row="3" Padding="12,8" Background="{DynamicResource MaterialDesignToolBarBackground}">
            <DockPanel>
                <TextBlock DockPanel.Dock="Right" Margin="16,0,0,0" VerticalAlignment="Center">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="配分残: {0:N0} 千円">
                            <Binding Path="RemainingBudget" />
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
                <TextBlock DockPanel.Dock="Right" Margin="16,0,0,0" VerticalAlignment="Center">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="合計: {0:N0} 千円">
                            <Binding Path="TotalBudget" />
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
            </DockPanel>
        </Border>
    </Grid>
</helpers:BaseWindow>
