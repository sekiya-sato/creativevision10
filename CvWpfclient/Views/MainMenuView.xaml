<Window x:Class="CvWpfclient.Views.MainMenuView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:models="clr-namespace:CvWpfclient.Models" xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework" xmlns:vm="clr-namespace:CvWpfclient.ViewModels"
	Title="{Binding HeaderTitle}"
	Width="1200" Height="780"
	Background="Transparent" Icon="pack://application:,,,/cvnet.ico" ResizeMode="CanResizeWithGrip" SnapsToDevicePixels="True" UseLayoutRounding="True"
	WindowStartupLocation="CenterScreen" WindowStyle="SingleBorderWindow"
	mc:Ignorable="d">

	<Window.DataContext>
		<vm:MainMenuViewModel />
	</Window.DataContext>

	<Window.InputBindings>
		<KeyBinding Key="F12"
			Command="{Binding ShowLoginCommand}" />
		<KeyBinding Key="F11"
			Command="{Binding ShowRefreshCommand}" />
		<KeyBinding Key="F10"
			Command="{Binding ShowSettingCommand}" />
		<KeyBinding Key="F9"
			Command="{Binding ShowUpgradeCommand}" />
	</Window.InputBindings>

	<shell:WindowChrome.WindowChrome>
		<shell:WindowChrome CaptionHeight="120" CornerRadius="80" GlassFrameThickness="0" ResizeBorderThickness="8" UseAeroCaptionButtons="False" />
	</shell:WindowChrome.WindowChrome>

	<behaviors:Interaction.Triggers>
		<behaviors:EventTrigger EventName="ContentRendered">
			<behaviors:InvokeCommandAction Command="{Binding InitCommand}" />
		</behaviors:EventTrigger>
	</behaviors:Interaction.Triggers>

	<Border Padding="2"
		Background="{StaticResource OuterWindowBackground}"
		CornerRadius="40">
		<Border Background="{StaticResource CvnetMainBackgroundBrush}"
			BorderThickness="2" CornerRadius="40">
			<Border.BorderBrush>
				<LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
					<GradientStop Offset="0" Color="{DynamicResource winBorderColor1}" />
					<GradientStop Offset="0.6" Color="{DynamicResource winBorderColor2}" />
					<GradientStop Offset="1" Color="{DynamicResource winBorderColor3}" />
				</LinearGradientBrush>
			</Border.BorderBrush>
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto" />
					<RowDefinition Height="*" />
				</Grid.RowDefinitions>

				<Grid Grid.Row="0"
					Margin="24,16,24,8">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*" />
						<ColumnDefinition Width="Auto" />
						<ColumnDefinition Width="Auto" />
					</Grid.ColumnDefinitions>

					<StackPanel Orientation="Vertical">
						<TextBlock FontSize="36" FontWeight="Bold"
							Foreground="{DynamicResource TitleColor}"
							Text="{Binding HeaderTitle}">
							<TextBlock.Effect>
								<DropShadowEffect BlurRadius="4" Direction="315" Opacity="1" ShadowDepth="3"
									Color="Black" />
							</TextBlock.Effect>
						</TextBlock>
						<TextBlock Padding="8,0,0,0"
							FontSize="14"
							Foreground="{DynamicResource SubTitleColor}"
							Opacity="0.6"
							Text="{Binding SubTitle}" />
					</StackPanel>

					<StackPanel Grid.Column="1"
						Margin="0" VerticalAlignment="Center"
						shell:WindowChrome.IsHitTestVisibleInChrome="True" Orientation="Horizontal">
						<Border Margin="0,0,4,0"
							Style="{StaticResource StatusChip}">
							<StackPanel Orientation="Vertical">
								<TextBlock Foreground="{DynamicResource SubTitleColor}"
									Style="{StaticResource MetricLabel}"
									Text="アクセス有効期限" />
								<!--  アクセス有効期限  -->
								<TextBlock HorizontalAlignment="Center"
									d:Text="2024/12/31 23:59:59"
									Foreground="{DynamicResource plainTextColor0}"
									Text="{Binding ExpireDate}" />
								<StackPanel Orientation="Horizontal">
									<Button Margin="0,4,8,0"
										Command="{Binding ToggleThemeCommand}"
										Style="{StaticResource MainMenuWindowButton}"
										ToolTip="テーマ切り替え (Light/Dark)">
										<materialDesign:PackIcon Kind="ThemeLightDark" Width="24" Height="24" Foreground="{DynamicResource TitleColor}" VerticalAlignment="Center" HorizontalAlignment="Center" />
									</Button>


									<Button Command="{Binding WinMinimizeCommand}"
										Opacity="0.6"
										Style="{StaticResource MinimizeWinBtn}"
										ToolTip="最小化">
										<materialDesign:PackIcon Kind="WindowMinimize" Width="24" Height="24" VerticalAlignment="Center" HorizontalAlignment="Center" />
									</Button>
									<Button Command="{Binding WinMenuOnlyCommand}"
										Opacity="0.6"
										Style="{StaticResource MenuonlyWinBtn}"
										ToolTip="メニューのみ/通常">
										<materialDesign:PackIcon Kind="ViewList" Width="24" Height="24" VerticalAlignment="Center" HorizontalAlignment="Center" />
									</Button>
									<Button Command="{Binding ExitCommand}"
										Opacity="0.6"
										Style="{StaticResource CloseWinBtn}"
										ToolTip="終了">
										<materialDesign:PackIcon Kind="Close" Width="24" Height="24" VerticalAlignment="Center" HorizontalAlignment="Center" />
									</Button>
								</StackPanel>
							</StackPanel>
						</Border>
						<Border HorizontalAlignment="Center"
							Style="{StaticResource StatusChip}">
							<StackPanel Orientation="Vertical">
								<TextBlock Width="100"
									HorizontalAlignment="Center"
									d:Text="99/12/31"
									Foreground="{DynamicResource SubTitleColor}"
									Style="{StaticResource MetricLabel}"
									Text="{Binding CurrentDate, FallbackValue='00/00/00'}" />

								<TextBlock Width="90"
									HorizontalAlignment="Center"
									d:Text="00:00:00" FontSize="18" FontWeight="Bold"
									Foreground="{DynamicResource TitleColor}"
									Text="{Binding CurrentTime, FallbackValue='00:00:00'}" />
							</StackPanel>
						</Border>
					</StackPanel>

				</Grid>

				<Grid Grid.Row="1"
					Margin="24">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="300" />
						<ColumnDefinition Width="20" />
						<ColumnDefinition Width="*" />
					</Grid.ColumnDefinitions>

					<Border Grid.Column="0"
						Padding="10,4,4,4"
						Background="{DynamicResource panelColor}"
						CornerRadius="6">
						<TreeView x:Name="MenuTree"
							Background="Transparent" BorderThickness="0"
							ItemContainerStyle="{StaticResource MenuTreeItemStyle}"
							ItemsSource="{Binding MenuItems}"
							PreviewKeyDown="MenuTree_PreviewKeyDown" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto"
							Style="{StaticResource MenuTreeView}">

							<TreeView.ItemTemplate>
								<HierarchicalDataTemplate DataType="{x:Type models:MenuData}"
									ItemsSource="{Binding SubItems}">
									<StackPanel>
										<TextBlock FontSize="15"
											Foreground="{DynamicResource plainTextColor0}"
											Text="{Binding Header}" />
										<!--
                                        <TextBlock Text="{Binding AddInfo}" FontSize="12" Opacity="0.65" Foreground="{DynamicResource plainTextColor0}"/>
										-->
									</StackPanel>
								</HierarchicalDataTemplate>
							</TreeView.ItemTemplate>

							<behaviors:Interaction.Triggers>
								<behaviors:EventTrigger EventName="MouseDoubleClick">
									<behaviors:InvokeCommandAction Command="{Binding DoMenuCommand}" />
								</behaviors:EventTrigger>
								<behaviors:EventTrigger EventName="SelectedItemChanged">
									<behaviors:InvokeCommandAction Command="{Binding SelectMenuCommand}"
										CommandParameter="{Binding SelectedItem, ElementName=MenuTree}" />
								</behaviors:EventTrigger>
							</behaviors:Interaction.Triggers>
						</TreeView>
					</Border>
					<Grid Grid.Column="2">
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto" />
							<RowDefinition Height="Auto" />
							<RowDefinition Height="*" />
						</Grid.RowDefinitions>
						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*" />
								<ColumnDefinition Width="*" />
							</Grid.ColumnDefinitions>
							<Border Grid.Column="0"
								Margin="0,0,4,0" Padding="4,2,4,8"
								Background="{DynamicResource panelColor}"
								Style="{StaticResource MainMenuDashboardCard}">
								<StackPanel>
									<TextBlock Margin="10,0,0,0"
										FontSize="14"
										Foreground="{DynamicResource MenuTitleColor}"
										Style="{StaticResource SectionTitleText}"
										Text="Server Status" />
									<TextBlock Margin="10,0,10,0"
										FontSize="14"
										Foreground="{DynamicResource plainTextColor0}"
										Text="{Binding ServerStatus}"
										TextWrapping="Wrap" />
								</StackPanel>
							</Border>
							<Border Grid.Column="1"
								Margin="4,0,0,0" Padding="4,2,4,8"
								Background="{DynamicResource panelColor}"
								Style="{StaticResource MainMenuDashboardCard}">
								<StackPanel>
									<TextBlock Margin="10,0,0,0"
										FontSize="14"
										Foreground="{DynamicResource MenuTitleColor}"
										Style="{StaticResource SectionTitleText}"
										Text="User Status" />
									<TextBlock Margin="10,0,10,0"
										FontSize="14"
										Foreground="{DynamicResource plainTextColor0}"
										Text="{Binding ClientStatus}"
										TextWrapping="Wrap" />
								</StackPanel>
							</Border>
						</Grid>
						<Border Grid.Row="1"
							Margin="0,16,0,0" Padding="20"
							Background="{DynamicResource panelColor}"
							Style="{StaticResource MainMenuDashboardCard}">
							<StackPanel>
								<StackPanel Orientation="Horizontal">
									<TextBlock Foreground="{DynamicResource MenuTitleColor}"
										Style="{StaticResource SectionTitleText}"
										Text="選択中メニュー" />
									<TextBlock Margin="10"
										Foreground="{DynamicResource plainTextColor0}"
										Opacity="0.6" Text="    メニューをダブルクリックすると画面が起動します。" TextWrapping="Wrap" />
								</StackPanel>
								<TextBlock FontSize="22" FontWeight="Bold"
									Foreground="{DynamicResource plainTextColor0}"
									Text="{Binding SelectedMenu.Header}" />
								<TextBlock Margin="0,8,0,0"
									Foreground="{DynamicResource plainTextColor0}"
									Opacity="0.8"
									Text="{Binding SelectedMenu.AddInfo}" />
								<TextBlock Margin="0,2,0,2"
									d:Text="マルチウィンドウ画面"
									Foreground="{DynamicResource plainTextColor0}"
									Opacity="0.7" TextWrapping="Wrap">
									<TextBlock.Style>
										<Style TargetType="TextBlock">
											<Setter Property="Text" Value="マルチウィンドウ画面" />
											<Style.Triggers>
												<DataTrigger Binding="{Binding SelectedMenu.IsDialog}"
													Value="True">
													<Setter Property="Text" Value="ダイアログ画面" />
												</DataTrigger>
											</Style.Triggers>
										</Style>
									</TextBlock.Style>
								</TextBlock>
							</StackPanel>
						</Border>

						<Border Grid.Row="2"
							Margin="0,16,0,0" Padding="20"
							Background="{DynamicResource panelColor}"
							Style="{StaticResource MainMenuDashboardCard}">
							<StackPanel>
								<StackPanel Margin="0,0,0,8" HorizontalAlignment="Left"
									Orientation="Horizontal">
									<Button Margin="10,0,10,0"
										Background="{DynamicResource PrimaryButtonColor}"
										Command="{Binding ShowUpgradeCommand}"
										Content="バージョンアップ"
										Style="{StaticResource MainMenuActionButton}"
										ToolTip="システムアップデート" />
									<Button Margin="10,0,10,0"
										Background="{DynamicResource PrimaryButtonColor}"
										Command="{Binding ShowSettingCommand}"
										Content="環境設定"
										Style="{StaticResource MainMenuActionButton}"
										ToolTip="環境設定" />
									<Button Margin="10,0,10,0"
										Background="{DynamicResource PrimaryButtonColor}"
										Command="{Binding ShowRefreshCommand}"
										Content="リフレッシュトークン"
										Style="{StaticResource MainMenuActionButton}"
										ToolTip="トークンリフレッシュ" />
									<Button Margin="10,0,10,0"
										Background="{DynamicResource PrimaryButtonColor}"
										Command="{Binding ShowLoginCommand}"
										Content="ログイン"
										Style="{StaticResource MainMenuActionButton}"
										ToolTip="ログイン画面" />
								</StackPanel>
								<TextBlock Foreground="{DynamicResource StatusTitleColor}"
									Style="{StaticResource SectionTitleText}"
									Text="ステータスログ" />
								<TextBlock FontSize="16"
									Foreground="{DynamicResource plainTextColor0}"
									Text="{Binding StatusMessage}"
									TextWrapping="Wrap" />
							</StackPanel>
						</Border>
					</Grid>
				</Grid>
			</Grid>
		</Border>
	</Border>
</Window>
